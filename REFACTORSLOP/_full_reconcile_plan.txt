
==========================================================================================
custom_components/jackery_solarvault/__init__.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (12) --
   [_async_authenticate] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def _async_authenticate(  # noqa: RUF067, RUF100
   [_async_clean_legacy_entities] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def _async_clean_legacy_entities(  # noqa: RUF067, RUF100
   [_async_remove_entities_with_suffixes] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def _async_remove_entities_with_suffixes(  # noqa: RUF029, RUF067, RUF100
   [_async_remove_stale_energy_helpers] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! async def _async_remove_stale_energy_helpers(hass: HomeAssistant) -> None:  # noqa: RUF029, RUF067, RUF100
        ! if unit not in {None, ""}:
   [_async_start_local_mqtt] (richest: 1\1101\custom_components\jackery_solarvault\__init__.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + hass: HomeAssistant, entry: JackeryConfigEntry
   [_async_stop_local_mqtt] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_async_update_listener] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def _async_update_listener(  # noqa: RUF067, RUF100
   [_legacy_suffix_matches] (richest: 1\__init__.py)  DOC=9 CODE=2
      (A) doc/comment-only:
        + """Return True when ``uid`` is exactly ``<device_id><key_suffix>``.
        + The unique-id contract (local implementation notes §11) says every
        + A plain ``str.endswith`` therefore over-matches when a current key
        + contains a legacy key as its tail — e.g. legacy ``_today_battery_charge``
        + would otherwise also match the current ``_device_today_battery_charge``
        + unique id and delete a live sensor on every reload (the regression that
      (B) CODE-line mismatch [review]:
        ! entity-registry id is ``<device_id>_<key_suffix>`` for the main device or
        ! ``<device_id>_battery_pack_<index>_<key_suffix>`` for an add-on battery.
   [_sink] (richest: pre_transfer)  DOC=3 CODE=3
      (A) doc/comment-only:
        + # Late-bind via entry.runtime_data so a reload doesn't dispatch to
        + # the old coordinator instance if the local MQTT client outlives teardown.
        + entry, "runtime_data", None
      (B) CODE-line mismatch [review]:
        ! current: JackerySolarVaultCoordinator | None = getattr(
        ! if current is None:
        ! await current.async_handle_local_mqtt_message(topic, data)
   [async_setup] (richest: 1\__init__.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Set up global Jackery SolarVault services."""
      (B) CODE-line mismatch [review]:
        ! async def async_setup(hass: HomeAssistant, config: dict) -> bool:
   [async_setup_entry] (richest: pre_transfer)  DOC=11 CODE=7
      (A) doc/comment-only:
        + Performs authentication, constructs the coordinator, runs discovery and the initial refresh, and starts cl
        + """  # noqa: E501, RUF100
        + "tuple[BaseException | None, BaseException | None, BaseException | None]",
        + _async_start_local_mqtt(hass, entry),
        + # Broker explicitly rejected MQTT credentials. HTTP login may have
        + # succeeded, but the user must update credentials regardless —
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(hass: HomeAssistant, entry: JackeryConfigEntry) -> bool:  # noqa: RUF067, RUF1
        ! await _async_clean_legacy_entities(hass, entry)
        ! try:  # noqa: PLW0717
        ! raise refresh_result  # noqa: TRY301
        ! raise mqtt_result  # noqa: TRY301
        ! except Exception:
        ! entry.runtime_data = None  # type: ignore[assignment]
   [async_unload_entry] (richest: pre_transfer)  DOC=1 CODE=4
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! async def async_unload_entry(hass: HomeAssistant, entry: JackeryConfigEntry) -> bool:  # noqa: RUF067, RUF
        ! If platform unload succeeds, shuts down the coordinator (if present) and clears the entry's runtime data t
        ! True if platforms were unloaded and runtime teardown completed, False otherwise.
        ! entry.runtime_data = None  # type: ignore[assignment]

==========================================================================================
custom_components/jackery_solarvault/api.py
==========================================================================================
-- MISSING MODULE CONSTANTS (1) --
   __all__  <- 1\1101\custom_components\jackery_solarvault\api.py, 1\api.py

==========================================================================================
custom_components/jackery_solarvault/binary_sensor.py
==========================================================================================
-- MISSING FUNCTIONS/CLASSES (1) --
   available  <- 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\binary_sensor.py
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (6) --
   [JackeryBinarySensor] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\binary_sensor.py)  DOC=2 CODE=5
      (A) doc/comment-only:
        + """Return whether the entity is currently available."""
        + """Return True when the entity is on."""
      (B) CODE-line mismatch [review]:
        ! def available(self) -> bool:
        ! if self.entity_description.key == "online":
        ! return self.coordinator.last_update_success and self._device_id in (
        ! self.coordinator.data or {}
        ! return super().available
   [JackerySmartPlugStateBinarySensor] (richest: 1\binary_sensor.py)  DOC=4 CODE=2
      (A) doc/comment-only:
        + """Initialise the entity from the coordinator, sorted index and serial."""
        + coordinator, device_id, f"smart_plug_{plug_index}_switch_state"
        + """Return True when the smart plug reports an active output."""
        + """Return diagnostic attributes for the smart-plug state."""
      (B) CODE-line mismatch [review]:
        ! super().__init__(
        ! plug_index, self._plug
   [_collect_entities] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\binary_sensor.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + @callback
      (B) CODE-line mismatch [review]:
        ! for dev_id, _payload in (coordinator.data or {}).items():
   [async_setup_entry] (richest: 1\binary_sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(
   [extra_state_attributes] (richest: 1\binary_sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return diagnostic attributes for the smart-plug state."""
   [is_on] (richest: 1\binary_sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return True when the entity is on."""

==========================================================================================
custom_components/jackery_solarvault/button.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (15) --
   [JackeryDeleteStormAlertButton] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! if not self.available:
        ! self._raise_action_error("storm alert is no longer active")
        ! except Exception as err:  # noqa: BLE001
   [JackeryQueryButton] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! if not self.available:
        ! self._raise_action_error("device is offline")
        ! except Exception as err:  # noqa: BLE001
   [JackeryReadScheduleButton] (richest: pre_transfer)  DOC=0 CODE=5
      (B) CODE-line mismatch [review]:
        ! def __init__(  # noqa: PLR0913
        ! if not self.available:
        ! self._raise_action_error("device is offline")
        ! smart_plug_sn=self._plug_sn or None,
        ! except Exception as err:  # noqa: BLE001
   [JackeryRebootButton] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! if not self.available:
        ! self._raise_action_error("device is offline")
        ! except Exception as err:  # noqa: BLE001
   [JackeryRefreshWeatherPlanButton] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! if not self.available:
        ! self._raise_action_error("device is offline")
        ! except Exception as err:  # noqa: BLE001
   [__init__] (richest: 1\button.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + self, coordinator: JackerySolarVaultCoordinator, device_id: str
        + """Initialise the entity from the coordinator and description."""
      (B) CODE-line mismatch [review]:
        ! super().__init__(coordinator, device_id, "reboot_device")
   [_add_new_entities] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_append_unique] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_collect_entities] (richest: 1\1101\custom_components\jackery_solarvault\button.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Collect reboot button entities for devices managed by the coordinator.
      (B) CODE-line mismatch [review]:
        ! Create a JackeryRebootButton for each device that either supports advanced features or exposes the reboot
   [_raise_action_error] (richest: 1\button.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + "entity": "reboot_device",
   [_smart_plug_device_sn] (richest: custom_components\jackery_solarvault\button.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if raw in {None, ""}:
   [_storm_alert_id] (richest: custom_components\jackery_solarvault\button.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if raw in {None, ""}:
   [async_press] (richest: 1\button.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Forward a button press to the device."""
      (B) CODE-line mismatch [review]:
        ! await self.coordinator.async_reboot_device(self._device_id)
        ! await self.coordinator.async_request_refresh()
   [async_setup_entry] (richest: 1\button.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(
   [extra_state_attributes] (richest: 1\2\jackery_solarvault-test\custom_components\jackery_solarvault\button.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Return app task command metadata."""
      (B) CODE-line mismatch [review]:
        ! attrs: dict[str, Any] = {"taskType": self._task_type}
        ! if self._plug_sn:
        ! attrs[FIELD_DEVICE_SN] = self._plug_sn

==========================================================================================
custom_components/jackery_solarvault/client/__init__.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (1) --
   [__getattr__] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + JackeryMqttPushClient as _JackeryMqttPushClient,
      (B) CODE-line mismatch [review]:
        ! def __getattr__(name: str) -> Any:  # noqa: ANN401, RUF100
        ! from .mqtt_push import (  # noqa: PLC0415

==========================================================================================
custom_components/jackery_solarvault/client/api.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (54) --
   [JackeryApi] (richest: pre_transfer)  DOC=66 CODE=126
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + f"Login returned JSON {type(data).__name__}, expected object"
        + f"Login returned {FIELD_DATA} {type(payload).__name__}, expected object"
      (B) CODE-line mismatch [review]:
        ! class JackeryApi:  # noqa: PLR0904
        ! self.auth_rejection_callback: Callable[[int, object], None] | None = None
        ! raise JackeryAuthError(  # noqa: TRY003
        ! try:  # noqa: PLW0717
        ! if resp.status != 200:  # noqa: PLR2004
        ! raise JackeryApiError(f"Login HTTP {resp.status}")  # noqa: TRY003
        ! body_bytes = await resp.read()
        ! data = self._decode_response_json(body_bytes)
   [__init__] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + @staticmethod
      (B) CODE-line mismatch [review]:
        ! self.auth_rejection_callback: Callable[[int, object], None] | None = None
   [_aes_ecb_encrypt] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! cipher = Cipher(algorithms.AES(key), modes.ECB())  # noqa: RUF100, S305
   [_async_get_device_period_stat] (richest: 1\client\api.py)  DOC=4 CODE=9
      (A) doc/comment-only:
        + """GET a device-level app chart endpoint.
        + # implementation notes §2: Periodenabfragen use explicit full ranges.
        + data,
        + payload.setdefault(APP_REQUEST_META, request_meta)
      (B) CODE-line mismatch [review]:
        ! The Android app uses these device endpoints for the PV/battery/home/CT
        ! statistic pages, while the older ``sys/*/trends`` endpoints are system
        ! summaries. Keep request metadata on the payload for diagnostics.
        ! request_meta = {
        ! self.last_device_period_stat_responses[f"{path}:{device_id}:{date_type}"] = (
        ! self._response_with_request_context(
        ! path=path,
        ! params=params,
   [_auth_failure_message] (richest: custom_components\jackery_solarvault\client\api.py)  DOC=2 CODE=5
      (A) doc/comment-only:
        + method: str, path: str, status: int, data: object
        + else:
      (B) CODE-line mismatch [review]:
        ! def _auth_failure_message(
        ! ) -> str:
        ! if isinstance(data, dict):
        ! code = None
        ! msg = data
   [_do] (richest: pre_transfer)  DOC=8 CODE=18
      (A) doc/comment-only:
        + ) as err:
        + f"{HTTP_METHOD_GET} {path} returned invalid JSON: {raw!r}"
        + f"{HTTP_METHOD_GET} {path} returned JSON "
        + f"{type(body).__name__}, expected object"
        + HTTP_METHOD_GET, path, _do
        + HTTP_METHOD_GET, path, _do
      (B) CODE-line mismatch [review]:
        ! async def _do() -> tuple[int, dict[str, Any]]:
        ! body_bytes = await resp.read()
        ! body = self._decode_response_json(body_bytes)
        ! raw = self._truncated_response_text(body_bytes)
        ! raise JackeryApiError(  # noqa: TRY003
        ! if not isinstance(body, dict):
        ! raise JackeryApiError(  # noqa: TRY003
        ! status, data = await self._request_json_with_retry(
   [_emit_payload_debug] (richest: 1\client\api.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! result = callback(event_or_factory)
   [_ensure_token] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise JackeryAuthError("Login succeeded without returning a token")  # noqa: TRY003
   [_extract_code] (richest: 1\client\api.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Normalize API `code` to int when possible."""
      (B) CODE-line mismatch [review]:
        ! def _extract_code(data: dict[str, Any] | Any) -> int | None:
        ! return first_nonblank_int(code)
   [_generate_udid] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! md5_digest = hashlib.md5(seed.encode("utf-8")).digest()  # noqa: RUF100, S324
   [_get_json] (richest: pre_transfer)  DOC=8 CODE=21
      (A) doc/comment-only:
        + ) as err:
        + f"{HTTP_METHOD_GET} {path} returned invalid JSON: {raw!r}"
        + f"{HTTP_METHOD_GET} {path} returned JSON "
        + f"{type(body).__name__}, expected object"
        + HTTP_METHOD_GET, path, _do
        + HTTP_METHOD_GET, path, _do
      (B) CODE-line mismatch [review]:
        ! async def _get_json(
        ! self, path: str, params: dict[str, Any] | None = None
        ! ) -> dict[str, Any]:
        ! async def _do() -> tuple[int, dict[str, Any]]:
        ! body_bytes = await resp.read()
        ! body = self._decode_response_json(body_bytes)
        ! raw = self._truncated_response_text(body_bytes)
        ! raise JackeryApiError(  # noqa: TRY003
   [_headers] (richest: 1\client\api.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Headers matching the Android app values documented in implementation notes §2."""
   [_http_payload_debug] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! def _http_payload_debug(  # noqa: PLR0913
        ! params: dict[str, Any] | None = None,
        ! body: dict[str, Any] | None = None,
   [_is_auth_failure_response] (richest: pre_transfer)  DOC=2 CODE=15
      (A) doc/comment-only:
        + CODE_OK,
        + None,
      (B) CODE-line mismatch [review]:
        ! if status in {401, 403}:
        ! if self.auth_rejection_callback is not None:
        ! self.auth_rejection_callback(status, data)
        ! if self.auth_rejection_callback is not None:
        ! self.auth_rejection_callback(status, data)
        ! if status != 200:  # noqa: PLR2004
        ! is_auth_failure = self._response_has_auth_failure_text(data)
        ! if is_auth_failure and self.auth_rejection_callback is not None:
   [_is_token_expired_response] (richest: 1\client\api.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Detect token-expired responses across backend variants."""
      (B) CODE-line mismatch [review]:
        ! def _is_token_expired_response(
        ! self, status: int, data: dict[str, Any] | Any
        ! ) -> bool:
   [_normalize_mqtt_mac_id] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! raise JackeryAuthError(  # noqa: TRY003
   [_payload_dict] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise JackeryApiError(f"Jackery {path} returned null data")  # noqa: TRY003
   [_payload_list] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! raise JackeryApiError(f"Jackery {path} returned null data")  # noqa: TRY003
   [_post_form] (richest: pre_transfer)  DOC=5 CODE=16
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + HTTP_METHOD_POST, path, _do
        + HTTP_METHOD_POST, path, _do
        + f"{HTTP_METHOD_POST} {path} token still expired after re-login"
        + f"{HTTP_METHOD_POST} {path} HTTP {status} "
      (B) CODE-line mismatch [review]:
        ! async def _post_form(self, path: str, fields: dict[str, Any]) -> dict[str, Any]:
        ! async def _do() -> tuple[int, dict[str, Any]]:
        ! body_bytes = await resp.read()
        ! data = self._decode_response_json(body_bytes)
        ! data = {FIELD_RAW_TEXT: self._truncated_response_text(body_bytes)}
        ! status, data = await self._request_json_with_retry(
        ! raise JackeryApiError(  # noqa: TRY003
        ! status, data = await self._request_json_with_retry(
   [_put_json] (richest: pre_transfer)  DOC=5 CODE=16
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + HTTP_METHOD_PUT, path, _do
        + HTTP_METHOD_PUT, path, _do
        + f"{HTTP_METHOD_PUT} {path} token still expired after re-login"
        + f"{HTTP_METHOD_PUT} {path} HTTP {status} "
      (B) CODE-line mismatch [review]:
        ! async def _put_json(self, path: str, payload: dict[str, Any]) -> dict[str, Any]:
        ! async def _do() -> tuple[int, dict[str, Any]]:
        ! body_bytes = await resp.read()
        ! body = self._decode_response_json(body_bytes)
        ! body = {FIELD_RAW_TEXT: self._truncated_response_text(body_bytes)}
        ! status, data = await self._request_json_with_retry(
        ! raise JackeryApiError(  # noqa: TRY003
        ! status, data = await self._request_json_with_retry(
   [_resolve_login_mac_id] (richest: 1\client\api.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Resolve the macId used in login and MQTT username derivation."""
   [_response_has_auth_failure_text] (richest: 1\1101\custom_components\jackery_solarvault\client\api.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _response_has_auth_failure_text(data: dict[str, Any] | Any) -> bool:
   [_rsa_pkcs1v15_encrypt] (richest: 1\client\api.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! if not isinstance(public_key, RSAPublicKey):
        ! raise JackeryApiError("Jackery login public key is not an RSA key")
   [_select_ota_item] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! return {}
   [async_control_shelly_device] (richest: pre_transfer)  DOC=2 CODE=3
      (A) doc/comment-only:
        + action: str | int | None,
        + function: str | int | None,
      (B) CODE-line mismatch [review]:
        ! raise JackeryApiError("Shelly control is not allowed for this device")  # noqa: TRY003
        ! if action is None or function is None:
        ! raise JackeryApiError("Shelly control requires action and function")  # noqa: TRY003
   [async_get_alarm] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + self, system_id: str | int
      (B) CODE-line mismatch [review]:
        ! async def async_get_alarm(
        ! ) -> Any:  # parsed JSON response, indexed by callers  # noqa: ANN401, RUF100
   [async_get_battery_pack_list] (richest: 1\client\api.py)  DOC=5 CODE=6
      (A) doc/comment-only:
        + """GET /v1/device/battery/pack/list — sub-battery pack status.
        + App decompile (BatteryPackApi + BatteryPackSub):
        + request: deviceSn
        + fields: batSoc, cellTemp, inPw, outPw, version, isFirmwareUpgrade
        + data,
      (B) CODE-line mismatch [review]:
        ! params = {FIELD_DEVICE_SN: str(device_sn)}
        ! data = await self._get_json(BATTERY_PACK_PATH, params=params)
        ! self.last_battery_pack_responses[str(device_sn)] = (
        ! self._response_with_request_context(
        ! path=BATTERY_PACK_PATH,
        ! params=params,
   [async_get_battery_trends] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! ) -> dict[str, Any]:
   [async_get_device_ct_stat] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\client\api.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """GET /v1/device/stat/ct — app CT/smart-meter statistics."""
   [async_get_device_eps_stat] (richest: 27.05\custom_components\jackery_solarvault\client\api.py)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """GET /v1/device/stat/eps — app EPS / off-grid in/out statistics.
        + Per ``docs/Markdown/APP_POLLING_MQTT.md`` §HTTP-Pfade and
        + ``docs/html/jackery_http_api_endpoints_v2.html`` (``EpsStatApi``).
        + Returns ``totalInEpsEnergy``/``totalOutEpsEnergy`` plus
      (B) CODE-line mismatch [review]:
        ! ``x``/``y``/``y1``/``y2`` chart series for the requested period.
   [async_get_device_home_stat] (richest: 1\client\api.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """GET /v1/device/stat/onGrid — app on-grid/home statistics."""
   [async_get_device_meter_stat] (richest: 1\client\api.py)  DOC=3 CODE=6
      (A) doc/comment-only:
        + """GET /v1/device/stat/meter — app Smart-Meter panel totals.
        + not the SolarVault main deviceId.
        + data,
      (B) CODE-line mismatch [review]:
        ! The Android app calls this with the Smart-Meter/CT accessory deviceId,
        ! params = {FIELD_DEVICE_ID: str(device_id)}
        ! data = await self._get_json(DEVICE_METER_STAT_PATH, params=params)
        ! ] = self._response_with_request_context(
        ! path=DEVICE_METER_STAT_PATH,
        ! params=params,
   [async_get_device_property] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_get_device_property(self, device_id: str | int) -> dict[str, Any]:
   [async_get_device_pv_stat] (richest: 1\client\api.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """GET /v1/device/stat/pv — app PV statistics for one device."""
   [async_get_device_socket_stat] (richest: 1\client\api.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """GET /v1/device/stat/socket — app socket chart statistics."""
   [async_get_device_socket_statistic] (richest: 1\client\api.py)  DOC=3 CODE=6
      (A) doc/comment-only:
        + """GET /v1/device/stat/smartSocketStatistic — socket panel totals.
        + expects fields like ``todayEgy`` and ``totalEgy``.
        + data,
      (B) CODE-line mismatch [review]:
        ! The Android app calls this with the smart socket accessory id and
        ! params = {FIELD_SMART_SOCKET_ID: str(smart_socket_id)}
        ! data = await self._get_json(DEVICE_SOCKET_STATISTIC_PATH, params=params)
        ! ] = self._response_with_request_context(
        ! path=DEVICE_SOCKET_STATISTIC_PATH,
        ! params=params,
   [async_get_device_statistic] (richest: pre_transfer)  DOC=5 CODE=1
      (A) doc/comment-only:
        + The returned dictionary maps metric keys (strings) to their values as numeric strings representing kilowat
        + Typical keys include: `pvEgy`, `inEpsEgy`, `ongridOtBatEgy`, `pvOtBatEgy`, `inOngridEgy`, `outOngridEgy`,
        + `outEpsEgy`, `batDisChgEgy`, `acOtBatEgy`, `batOtAcEgy`, and `batChgEgy`.
        + Keys present may vary by device and backend response.
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! async def async_get_device_statistic(self, device_id: str | int) -> dict[str, Any]:
   [async_get_home_trends] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! ) -> dict[str, Any]:
   [async_get_location] (richest: 1\client\api.py)  DOC=2 CODE=0
      (A) doc/comment-only:
        + """GET /v1/device/location — GPS coordinates set by the user."""
        + # --- HTTP write endpoints documented in implementation notes §2 --------------
   [async_get_mqtt_credentials] (richest: 1\client\api.py)  DOC=6 CODE=13
      (A) doc/comment-only:
        + """Return MQTT credentials for the active REST login session.
        + Runtime-verified app algorithm from implementation notes §3:
        + in-memory MQTT fields are already populated (e.g. hydrated from
        + :meth:`hydrate_mqtt_session`). The broker validates the AES-derived
        + password independently of the REST JWT, so a cloud outage that blocks
        + ``async_login`` does not have to block MQTT reconnects.
      (B) CODE-line mismatch [review]:
        ! async def async_get_mqtt_credentials(
        ! self, *, allow_stale: bool = False
        ! ) -> dict[str, str]:
        ! clientId = f"{userId}@APP"
        ! username = f"{userId}@{macId}"
        ! seed     = base64_decode(mqttPassWord)   # 32 bytes
        ! key      = seed                           # AES-256 key
        ! iv       = seed[:16]
   [async_get_ota_info] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! async def async_get_ota_info(self, device_sn: str) -> dict[str, Any]:
   [async_get_power_price] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_get_power_price(self, system_id: str | int) -> dict[str, Any]:
   [async_get_price_history_config] (richest: 1\client\api.py)  DOC=2 CODE=0
      (A) doc/comment-only:
        + """GET /v1/device/dynamic/historyConfig — provider auth/status metadata."""
        + # --- Additional app-statistic endpoints from implementation notes §2 ----------
   [async_get_pv_trends] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! ) -> dict[str, Any]:
   [async_get_system_statistic] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_get_system_statistic(self, system_id: str | int) -> dict[str, Any]:
   [async_get_today_energy] (richest: 27.05\custom_components\jackery_solarvault\client\api.py)  DOC=5 CODE=1
      (A) doc/comment-only:
        + """GET /v1/device/stat/today — compact today KPIs.
        + Per ``docs/Markdown/APP_POLLING_MQTT.md`` and the Smali
        + ``TodayEnergyApi``. The bean carries ``de`` (feed-in), ``dg``
        + as doubles. The endpoint is keyed by ``deviceSn``, not
        + ``deviceId``.
      (B) CODE-line mismatch [review]:
        ! (grid import), ``dh`` (home load) and ``ds`` (battery energy)
   [async_list_devices_legacy] (richest: 1\client\api.py)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """GET /v1/device/bind/list — Explorer-series only, kept for compat.
        + Authentication failures must propagate so HA can open the reauth
        + account, transient backend errors) are swallowed into an empty list
        + because this is a best-effort fallback path.
      (B) CODE-line mismatch [review]:
        ! flow; only generic API failures (endpoint not exposed for this
   [async_login] (richest: pre_transfer)  DOC=3 CODE=15
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + f"Login returned JSON {type(data).__name__}, expected object"
        + f"Login returned {FIELD_DATA} {type(payload).__name__}, expected object"
      (B) CODE-line mismatch [review]:
        ! try:  # noqa: PLW0717
        ! if resp.status != 200:  # noqa: PLR2004
        ! raise JackeryApiError(f"Login HTTP {resp.status}")  # noqa: TRY003
        ! body_bytes = await resp.read()
        ! data = self._decode_response_json(body_bytes)
        ! raw = self._truncated_response_text(body_bytes)
        ! raise JackeryApiError(  # noqa: TRY003
        ! raise JackeryApiError(  # noqa: TRY003
   [async_set_dynamic_mode] (richest: 1\client\api.py)  DOC=3 CODE=4
      (A) doc/comment-only:
        + platform_company_id: int | str,
        + """POST /v1/device/dynamic/saveDynamicMode."""
        + FIELD_PLATFORM_COMPANY_ID: company_id,
      (B) CODE-line mismatch [review]:
        ! company_id = first_nonblank_int(platform_company_id)
        ! if company_id is None:
        ! raise JackeryApiError("platform_company_id must be an integer")
        ! return _write_accepted(data)
   [async_set_max_power] (richest: custom_components\jackery_solarvault\client\api.py)  DOC=5 CODE=2
      (A) doc/comment-only:
        + """POST /v1/device/deviceMaxPowerRecord/saveRecord (experimental).
        + The endpoint name ("saveRecord") suggests this might be a history
        + log, not the live setter. May require specific value ranges or
        + additional fields we haven't identified yet.
        + Set the device's maximum allowed power using the experimental max-power endpoint.
      (B) CODE-line mismatch [review]:
        ! Captured body: `maxPower=<int>&deviceId=<long>` as form-urlencoded.
        ! ⚠️  Only failed responses (code=10600) have been observed so far.
   [async_set_single_mode] (richest: pre_transfer)  DOC=2 CODE=7
      (A) doc/comment-only:
        + JackeryApiError: If `single_price` is invalid, negative, or `currency` is empty.
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! try:
        ! except TypeError as err:
        ! raise JackeryApiError("single_price must be a valid number") from err  # noqa: TRY003
        ! except ValueError as err:
        ! raise JackeryApiError("single_price must be a valid number") from err  # noqa: TRY003
        ! raise JackeryApiError("single_price must be >= 0")  # noqa: TRY003
        ! raise JackeryApiError("currency must be a non-empty string")  # noqa: TRY003
   [async_set_system_name] (richest: 1\client\api.py)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """PUT /v1/device/system/name — rename a system.
        + Captured body: {"systemName": "SolarVault", "id": "<systemId>"}
        + Response payload is a boolean: `data: true`.
      (B) CODE-line mismatch [review]:
        ! return data.get(FIELD_DATA) is True
   [mqtt_mac_id_source] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return the source identifier for the current MQTT MAC ID (login vs cached)."""  # noqa: E501, RUF100
   [mqtt_session_snapshot] (richest: 1\client\api.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + @property

==========================================================================================
custom_components/jackery_solarvault/client/ble.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (12) --
   [_validate_key_len] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise ValueError(  # noqa: TRY003
   [aes_decrypt] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise ValueError(f"aes_decrypt: iv must be {BLE_AES_IV_LEN} bytes")  # noqa: TRY003
   [aes_encrypt] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise ValueError(f"aes_encrypt: iv must be {BLE_AES_IV_LEN} bytes")  # noqa: TRY003
   [build_binary_frame] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def build_binary_frame(  # noqa: PLR0913
   [build_plaintext_frame] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise ValueError("chunk_payload must serialise to an even hex length")  # noqa: TRY003
   [chunk_size_for_mtu] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise ValueError(  # noqa: TRY003
   [decrypt_binary_notify] (richest: pre_transfer)  DOC=4 CODE=10
      (A) doc/comment-only:
        + _LOGGER.warning(
        + "BLE frame has unexpected version 0x%s (expected 0x%s); "
        + "parsing continues (occurrence #%d)",
        + count,
      (B) CODE-line mismatch [review]:
        ! raise ValueError(f"notify too short: {len(raw)} bytes")  # noqa: TRY003
        ! raise ValueError(  # noqa: TRY003
        ! raise ValueError(  # noqa: TRY003
        ! _unrecognised_version_count[0] += 1
        ! count = _unrecognised_version_count[0]
        ! if count == 1 or count % 100 == 0:
        ! plaintext[2:4].hex(),
        ! _BINARY_FRAME_VERSION_BE.hex(),
   [decrypt_frame] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! raise ValueError("ciphertext blob too short")  # noqa: TRY003
        ! raise ValueError("plaintext too short to carry random tag + CRC")  # noqa: TRY003
        ! raise ValueError(  # noqa: TRY003
   [hex16] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! if not 0 <= value <= 0xFFFF:  # noqa: PLR2004
        ! raise ValueError(f"hex16: {value} does not fit into 16 bits")  # noqa: TRY003
   [hex_encode] (richest: 1\client\ble.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Upper-case hex encode a byte string (no separators, no prefix)."""
   [parse_hex16] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! raise ValueError(  # noqa: TRY003
        ! raise ValueError("parse_hex16: expected hex chars") from err  # noqa: TRY003
   [parse_plaintext_frame] (richest: pre_transfer)  DOC=0 CODE=5
      (B) CODE-line mismatch [review]:
        ! raise ValueError("frame too short")  # noqa: TRY003
        ! raise ValueError(f"frame does not start with {BLE_FRAME_MAGIC!r}")  # noqa: TRY003
        ! raise ValueError(f"unexpected protocol version {version!r}")  # noqa: TRY003
        ! raise ValueError(f"unexpected payload marker {marker!r}")  # noqa: TRY003
        ! raise ValueError(  # noqa: TRY003

==========================================================================================
custom_components/jackery_solarvault/client/ble_transport.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (17) --
   [BleFrameObservation] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! source_started_at: datetime | None = None
   [BleListenerStats] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! writes_with_response_failed: int = 0
   [JackeryBleListener] (richest: pre_transfer)  DOC=116 CODE=80
      (A) doc/comment-only:
        + """Construct a Jackery BLE listener that observes notifications and forwards parsed frames to a sink.
        + Parameters:
        + """  # noqa: E501, RUF100
        + """Get the cached BLE MAC address for the given device id.
        + Returns:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! hass (HomeAssistant): Home Assistant instance used for bluetooth callbacks and background tasks.
        ! sink (FrameSink): Async callable invoked with (device_id, BleFrameObservation) for each observed frame.
        ! key_resolver (Callable[[str], bytes | None]): Returns the 16- or 32-byte AES key for a given device_id, or
        ! ble_address_resolver (Callable[[str], str | None]): Returns the BLE MAC for a given device_id, or None; th
        ! serial_resolver (Callable[[str], str | None] | None): Maps a BLE-broadcast serial string to a Jackery devi
        ! self._aborted = False
        ! self._pending_property_query_starts: dict[str, deque[datetime]] = {}
        ! The MAC address string for the device, or `None` if no cached address exists.
   [__init__] (richest: pre_transfer)  DOC=3 CODE=7
      (A) doc/comment-only:
        + """Construct a Jackery BLE listener that observes notifications and forwards parsed frames to a sink.
        + Parameters:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! hass (HomeAssistant): Home Assistant instance used for bluetooth callbacks and background tasks.
        ! sink (FrameSink): Async callable invoked with (device_id, BleFrameObservation) for each observed frame.
        ! key_resolver (Callable[[str], bytes | None]): Returns the 16- or 32-byte AES key for a given device_id, or
        ! ble_address_resolver (Callable[[str], str | None]): Returns the BLE MAC for a given device_id, or None; th
        ! serial_resolver (Callable[[str], str | None] | None): Maps a BLE-broadcast serial string to a Jackery devi
        ! self._aborted = False
        ! self._pending_property_query_starts: dict[str, deque[datetime]] = {}
   [_async_keep_alive_loop] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! try:  # noqa: PLW0717
        ! return  # stop_event fired  # noqa: TRY300
        ! except asyncio.CancelledError:  # noqa: TRY203
   [_async_run_connection] (richest: pre_transfer)  DOC=61 CODE=24
      (A) doc/comment-only:
        + """Maintain a persistent BLE GATT session for the given device, subscribing to notifications and reconnect
        + Raises:
        + """  # noqa: E501, RUF100
        + BLEAK_RETRY_EXCEPTIONS,
        + establish_connection,
        + _LOGGER.warning(
      (B) CODE-line mismatch [review]:
        ! async def _async_run_connection(self, device_id: str, address: str) -> None:  # noqa: PLR0912, PLR0915
        ! This coroutine opens and publishes a Bleak client for the given address, subscribes to the notify characte
        ! asyncio.CancelledError: if the task is cancelled during shutdown.
        ! from bleak.exc import BleakError  # noqa: PLC0415
        ! from bleak_retry_connector import (  # noqa: PLC0415
        ! from homeassistant.components import bluetooth  # noqa: PLC0415
        ! consecutive_failures = 0
        ! try:  # noqa: PLW0717
   [_device_id_from_service_info] (richest: pre_transfer)  DOC=5 CODE=2
      (A) doc/comment-only:
        + """Resolve a BLE advertisement to a known Jackery device id and cache the device's BLE MAC address.
        + Returns:
        + """  # noqa: E501, RUF100
        + "Jackery BLE: advertisement for serial %s @ %s — no device "
        + "id mapping yet",
      (B) CODE-line mismatch [review]:
        ! If the advertisement corresponds to a device the integration knows about, the function records device_id -
        ! `device_id` if the advertisement maps to a known device, `None` otherwise.
   [_handle_notification] (richest: pre_transfer)  DOC=7 CODE=8
      (A) doc/comment-only:
        + """Process a BLE notification: decode into a BleFrameObservation, update per-device statistics, resolve an
        + """  # noqa: E501, RUF100
        + "DEFAULT_BLE_CONNECT_TIMEOUT_SEC",
        + "BleFrameObservation",
        + "BleListenerStats",
        + "FrameSink",
      (B) CODE-line mismatch [review]:
        ! If a per-device AES key is available the method attempts to decrypt the raw payload and, on decryption fai
        ! if parsed is not None and parsed.cmd == 107:  # noqa: PLR2004
        ! q = self._pending_property_query_starts.get(device_id)
        ! observation.source_started_at = q.popleft() if q else None
        ! if decode_error is not None:
        ! stats.last_error = f"notify: {decode_error}"
        ! except Exception as err:  # pragma: no cover — sink misbehaviour  # noqa: BLE001
        ! __all__ = [
   [_notify_callback] (richest: pre_transfer)  DOC=26 CODE=5
      (A) doc/comment-only:
        + _characteristic: object, data: bytearray
        + # Park the connection until the device drops it or we are
        + # asked to stop. We poll ``client.is_connected`` (a bleak
        + # property with no awaitable) alongside the stop event: the
        + # disconnect callback only records stats, and bleak backends
        + # do not all fire it reliably, so the 1s poll is a deliberate
      (B) CODE-line mismatch [review]:
        ! try:  # noqa: PLW0717
        ! consecutive_failures = 0
        ! while not self._stop_event.is_set() and client.is_connected:  # noqa: ASYNC110, RUF100
        ! self._stop_event.wait(), timeout=_LOST_LINK_BACKOFF_SEC
        ! self._stop_event.is_set(),
   [_on_advertisement] (richest: 01.06\custom_components\jackery_solarvault\client\ble_transport.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + change: BluetoothChange,
   [_on_disconnect] (richest: pre_transfer)  DOC=10 CODE=0
      (A) doc/comment-only:
        + """Handle a peripheral disconnect for the given device.
        + Updates the device's `BleListenerStats.last_disconnect_at` to the current time and emits an info-level log
        + Parameters:
        + device_id (str): Identifier of the device whose disconnect is being recorded.
        + """  # noqa: E501, RUF100
        + # Promoted from DEBUG to INFO: peripheral disconnects are the
   [_record_negotiated_mtu] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! def _record_negotiated_mtu(self, device_id: str, client: BleakClient) -> None:
        ! if isinstance(value, int) and value > ble._BLE_FRAME_OVERHEAD:  # noqa: SLF001
   [_register_pending_ack] (richest: pre_transfer)  DOC=8 CODE=10
      (A) doc/comment-only:
        + """Register a pending ACK wait record for the given device.
        + Parameters:
        + device_id (str): Identifier of the device the ACK is expected from.
        + If `None`, any decoded frame will satisfy the pending ACK.
        + Returns:
        + _PendingAck: A record containing `expected_cmds` (a `frozenset` of the provided command IDs or `None`)
      (B) CODE-line mismatch [review]:
        ! ack_cmds (tuple[int, ...] | None): Optional sequence of acceptable command IDs that will satisfy the ACK.
        ! and `future`, an `asyncio.Future` that will be resolved with the matching `ble.BleBinaryFrame`.
        ! expected_cmds: frozenset[int] | None = None
        ! if ack_cmds:
        ! parsed_cmds: set[int] = set()
        ! for ack_cmd in ack_cmds:
        ! if isinstance(ack_cmd, bool) or not isinstance(ack_cmd, int):
        ! raise ValueError("ack_cmds must be an integer")  # noqa: TRY003, TRY004
   [address_for_device_id] (richest: pre_transfer)  DOC=6 CODE=1
      (A) doc/comment-only:
        + """Get the cached BLE MAC address for the given device id.
        + Returns:
        + """  # noqa: E501, RUF100
        + # ------------------------------------------------------------------
        + # Phase 3b: write path — send a command frame to the device
        + # ------------------------------------------------------------------
      (B) CODE-line mismatch [review]:
        ! The MAC address string for the device, or `None` if no cached address exists.
   [async_send_command] (richest: pre_transfer)  DOC=11 CODE=25
      (A) doc/comment-only:
        + """Send a logical command frame to the device over BLE and optionally wait for a matching acknowledgement.
        + Parameters:
        + device_id (str): Target device identifier.
        + cmd (int): Logical command identifier to send.
        + body (bytes): Command payload bytes.
        + flags (int): Frame flags included in the sent binary frame.
      (B) CODE-line mismatch [review]:
        ! async def async_send_command(  # noqa: PLR0912, PLR0913, PLR0915
        ! ack_timeout_sec: float = DEFAULT_BLE_ACK_TIMEOUT_SEC,
        ! wait_for_ack (bool): If True, wait for a matching decoded notify frame before returning.
        ! ack_timeout_sec (float): Timeout in seconds to wait for the ACK when `wait_for_ack` is True.
        ! ack_cmds (tuple[int, ...] | None): Optional set of `cmd` values that qualify as the ACK; when omitted, any
        ! mtu_override (int | None): Optional MTU to use instead of the negotiated or default MTU (used for tests/di
        ! bool: `True` if the GATT write completed (and, when requested, a matching ACK was received); `False` if no
        ! RuntimeError: When the payload cannot be chunked for the selected MTU, on GATT-layer failures (including w
   [async_start] (richest: pre_transfer)  DOC=11 CODE=2
      (A) doc/comment-only:
        + """Start BLE advertisement monitoring and register callbacks that spawn per-device connection runners when
        + Parameters:
        + """  # noqa: E501, RUF100
        + # Surface the exact matcher tuple so the user can cross-check it
        + # against ``bluetooth.async_scanner_devices_by_address`` /
        + # ``bluetooth.async_discovered_service_info`` output in the
      (B) CODE-line mismatch [review]:
        ! device_ids (list[str]): Device IDs to monitor; a background connection task will be created lazily for a d
        ! from homeassistant.components import bluetooth  # noqa: PLC0415
   [async_stop] (richest: pre_transfer)  DOC=2 CODE=3
      (A) doc/comment-only:
        + """Stop the BLE listener and release its resources.
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! Signals the listener to stop, unregisters Bluetooth advertisement callbacks, cancels active connection run
        ! self._aborted = True
        ! except Exception as err:  # pragma: no cover — HA callback contract is sync  # noqa: BLE001

==========================================================================================
custom_components/jackery_solarvault/client/local_mqtt.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (16) --
   [JackeryLocalMqttClient] (richest: pre_transfer)  DOC=14 CODE=16
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! def __init__(  # noqa: PLR0913
        ! with contextlib.suppress(asyncio.CancelledError, Exception):
        ! from aiomqtt import Client, MqttError  # noqa: PLC0415
        ! except ImportError as err:
        ! _LOGGER.debug("Jackery local MQTT import failed: %s", err)
        ! async with Client(
        ! await self._async_consume_session(client, MqttError)
        ! rc = self._extract_mqtt_code(err)
   [__init__] (richest: 27.05\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=2 CODE=0
      (A) doc/comment-only:
        + hass: Any,
        + """Hold the local broker target without opening any sockets."""
   [_async_run_session] (richest: pre_transfer)  DOC=2 CODE=9
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + else:
      (B) CODE-line mismatch [review]:
        ! from aiomqtt import Client, MqttError  # noqa: PLC0415
        ! except ImportError as err:
        ! _LOGGER.debug("Jackery local MQTT import failed: %s", err)
        ! async with Client(
        ! await self._async_consume_session(client, MqttError)
        ! rc = self._extract_mqtt_code(err)
        ! if rc:
        ! self._handle_connect_failure(rc)
   [_extract_mqtt_code] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Extract the integer MQTT return code from an aiomqtt error."""
      (B) CODE-line mismatch [review]:
        ! def _extract_mqtt_code(err: Exception) -> int:
   [_handle_connect_failure] (richest: 27.05\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Map a CONNACK rejection to an actionable diagnostics string."""
   [_handle_disconnect_error] (richest: 27.05\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Record a passive disconnect or aiomqtt I/O failure mid-session."""
   [_handle_message] (richest: 27.05\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=2 CODE=2
      (A) doc/comment-only:
        + """Record a message, log first-seen topics, dispatch to the sink."""
        + _LOGGER.info("Jackery local MQTT: first message on topic %r", topic)
      (B) CODE-line mismatch [review]:
        ! text: str | None = payload
        ! data = parsed
   [_log_task_result] (richest: pre_transfer)  DOC=2 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + _LOGGER.exception("Jackery local MQTT %s handler failed", label)
      (B) CODE-line mismatch [review]:
        ! except Exception:
   [_looks_like_home_assistant_event_payload] (richest: 1\2\jackery_solarvault-test\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=3 CODE=1
      (A) doc/comment-only:
        + b'"event_type"' in head
        + and b'"event_data"' in head
        + and (b'"state_changed"' in head or b'"entity_id"' in head)
      (B) CODE-line mismatch [review]:
        ! return (
   [_schedule_coroutine] (richest: pre_transfer)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + _LOGGER.exception("Jackery local MQTT %s handler failed", label)
      (B) CODE-line mismatch [review]:
        ! except Exception:
   [_utc_now_iso] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [async_start] (richest: 27.05\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Open the broker session in a background task and wait briefly."""
   [async_stop] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! with contextlib.suppress(asyncio.CancelledError, Exception):
   [diagnostics_snapshot] (richest: pre_transfer)  DOC=2 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + "topic_filter": REDACTED_VALUE if redact else self._topic_filter,
   [is_connected] (richest: 27.05\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return True when the broker session is currently open."""
   [is_started] (richest: 27.05\custom_components\jackery_solarvault\client\local_mqtt.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return True once a session task has been spawned at least once."""

==========================================================================================
custom_components/jackery_solarvault/client/mqtt_push.py
==========================================================================================
-- MISSING FUNCTIONS/CLASSES (1) --
   _on_done  <- jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (33) --
   [JackeryMqttPushClient] (richest: pre_transfer)  DOC=60 CODE=98
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + f"{MQTT_TOPIC_PREFIX}/{user_id}/{_MQTT_AVAILABILITY_TOPIC_SUFFIX}"
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + qos: MQTT Quality of Service level (default 1).
      (B) CODE-line mismatch [review]:
        ! self._availability_topic: str | None = None
        ! self._connection_state = "disconnected"
        ! self._subscribed_count = 0
        ! self._topic_count = 0
        ! self._consecutive_connect_failures = 0
        ! self._stopping = False
        ! self._message_tasks: deque[asyncio.Task[None]] = deque(
        ! maxlen=_MAX_PENDING_MESSAGE_TASKS
   [_AioMqttPassiveDisconnectFilter] (richest: jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + """Suppress expected aiomqtt passive socket-reset log messages.
        + Evaluates a LogRecord and filters out known benign "failed to receive on socket" messages that include com
      (B) CODE-line mismatch [review]:
        ! bool: `True` if the record should be logged, `False` otherwise.
   [__init__] (richest: pre_transfer)  DOC=1 CODE=8
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! self._availability_topic: str | None = None
        ! self._connection_state = "disconnected"
        ! self._subscribed_count = 0
        ! self._topic_count = 0
        ! self._consecutive_connect_failures = 0
        ! self._stopping = False
        ! self._message_tasks: deque[asyncio.Task[None]] = deque(
        ! maxlen=_MAX_PENDING_MESSAGE_TASKS
   [_async_run_session] (richest: pre_transfer)  DOC=14 CODE=41
      (A) doc/comment-only:
        + availability_topic: str,
        + """  # noqa: E501, RUF100
        + "Jackery MQTT connected; subscribing to %d topic(s) [TLS source=%s]",  # noqa: E501, RUF100
        + topic.endswith(f"/{suffix}")
        + ):
        + topic,
      (B) CODE-line mismatch [review]:
        ! async def _async_run_session(  # noqa: PLR0912, PLR0915
        ! try:  # noqa: PLW0717
        ! will=aiomqtt.Will(
        ! topic=availability_topic,
        ! payload=_MQTT_AVAILABILITY_OFFLINE,
        ! qos=1,
        ! retain=True,
        ! self._connection_state = "connected"
   [_async_stop_locked] (richest: jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py)  DOC=3 CODE=4
      (A) doc/comment-only:
        + # Cancel any fire-and-forget tasks the runner spawned via
        + # _schedule_coroutine. Leaving them attached to the loop is the
        + # accumulation path documented in BUGFIXTODO.
      (B) CODE-line mismatch [review]:
        ! for active in list(self._active_tasks):
        ! if not active.done():
        ! active.cancel()
        ! self._active_tasks.clear()
   [_async_wait_connected] (richest: jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py)  DOC=2 CODE=2
      (A) doc/comment-only:
        + """Wait until the MQTT client becomes connected or raise a RuntimeError on timeout or connection failure.
        + "publish timeout waiting for MQTT connect" when there is no prior error and raises RuntimeError("MQTT not
      (B) CODE-line mismatch [review]:
        ! If the internal connected event is not set within timeout_sec, sets self._last_error to
        ! If the event is set but the client is not marked connected, raises RuntimeError including the current self
   [_build_ssl_context_blocking] (richest: 27.05\custom_components\jackery_solarvault\client\mqtt_push.py)  DOC=69 CODE=78
      (A) doc/comment-only:
        + """Build a verified TLS context with the Jackery MQTT CA trust anchor."""
        + # Strenger Default-Context für Server-Auth
        + # Hostname- und Zertifikatsprüfung bleiben voll aktiv
        + # Optional: moderne Protokollversion erzwingen
        + # WICHTIG: VERIFY_X509_STRICT NICHT mehr abschalten
        + self,
      (B) CODE-line mismatch [review]:
        ! def _handle_message(
        ! ) -> None:
        ! if isinstance(payload, str):
        ! text = payload
        ! text = bytes(payload).decode("utf-8")
        ! data = json.loads(text)
        ! except (UnicodeDecodeError, json.JSONDecodeError, TypeError, ValueError) as err:
        ! self._messages_dropped += 1
   [_credential_fingerprint] (richest: 1\client\mqtt_push.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return a stable non-secret signature for MQTT credential changes."""
   [_extract_mqtt_code] (richest: jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py)  DOC=2 CODE=2
      (A) doc/comment-only:
        + """Return the integer MQTT return code contained in a MqttCodeError.
        + Extracts an integer code from `err.rc` or `err.rc.value`. If neither yields an integer, returns `0`.
      (B) CODE-line mismatch [review]:
        ! err (MqttCodeError): Exception instance that may expose a numeric return code.
        ! int: The extracted MQTT return code, or `0` if no integer code is present.
   [_handle_connect_failure] (richest: pre_transfer)  DOC=2 CODE=2
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + _LOGGER.warning("Jackery MQTT connect failed: %s", message)
      (B) CODE-line mismatch [review]:
        ! self._connection_state = "disconnected"
        ! self._consecutive_connect_failures += 1
   [_handle_disconnect_error] (richest: 1\client\mqtt_push.py)  DOC=5 CODE=0
      (A) doc/comment-only:
        + """Handle a passive disconnect or aiomqtt I/O failure mid-session."""
        + # Already mapped by ``_handle_connect_failure`` — keep the
        + # actionable reason instead of overwriting it with the generic
        + # disconnect message that some brokers emit immediately after a
        + # rejected CONNACK.
   [_handle_message] (richest: pre_transfer)  DOC=2 CODE=9
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + "message",
      (B) CODE-line mismatch [review]:
        ! except UnicodeDecodeError as err:
        ! self._last_message_error = f"invalid UTF-8 payload: {err}"
        ! except json.JSONDecodeError as err:
        ! except TypeError as err:
        ! self._last_message_error = f"invalid payload type: {err}"
        ! except ValueError as err:
        ! self._schedule_coroutine(
        ! self._message_callback(topic, data),
   [_is_connect_auth_failure_rc] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! return rc in {4, 5, 134, 135}
   [_is_connect_failure_error] (richest: 1\client\mqtt_push.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return True when disconnect should not hide a failed connect."""
   [_log_task_result] (richest: pre_transfer)  DOC=2 CODE=3
      (A) doc/comment-only:
        + tracked_tasks.remove(done)
        + _LOGGER.exception("Jackery MQTT %s handler failed", label)
      (B) CODE-line mismatch [review]:
        ! if tracked_tasks is not None:
        ! with contextlib.suppress(ValueError, KeyError):
        ! except Exception:
   [_mqtt_silent_for_too_long] (richest: 1\client\mqtt_push.py)  DOC=8 CODE=1
      (A) doc/comment-only:
        + """Return True when the broker is "connected" but no message arrives.
        + Real Jackery devices emit at least one heartbeat per ~30 s. A
        + sustained silence of ``MQTT_SILENT_THRESHOLD_SEC`` (default 300 s)
        + subscription is broken even though TCP is alive — surface it in
        + diagnostics so the user can investigate without enabling DEBUG.
        + # Still waiting for the first frame after connect — only
      (B) CODE-line mismatch [review]:
        ! while the connection is still open is a strong signal the
   [_redact_topic] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\client\mqtt_push.py)  DOC=2 CODE=0
      (A) doc/comment-only:
        + """Redact the userId segment from Jackery MQTT topics."""
        + @property
   [_runner] (richest: pre_transfer)  DOC=3 CODE=2
      (A) doc/comment-only:
        + tracked_tasks.append(task)
        + else:
        + tracked_tasks.add(task)
      (B) CODE-line mismatch [review]:
        ! if tracked_tasks is not None:
        ! if isinstance(tracked_tasks, deque):
   [_schedule_coroutine] (richest: pre_transfer)  DOC=8 CODE=11
      (A) doc/comment-only:
        + self,
        + label: str,
        + *,
        + tracked_tasks.append(task)
        + else:
        + tracked_tasks.add(task)
      (B) CODE-line mismatch [review]:
        ! def _schedule_coroutine(
        ! coro: Awaitable[None],
        ! tracked_tasks: set[asyncio.Task[None]]
        ! | deque[asyncio.Task[None]]
        ! | None = None,
        ! ) -> None:
        ! if tracked_tasks is not None:
        ! if isinstance(tracked_tasks, deque):
   [_seconds_since_last_message] (richest: 1\client\mqtt_push.py)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """Return seconds elapsed since the last inbound MQTT frame, or None.
        + ``None`` means we have not received a single message yet on this
        + client lifetime — the broker connect may have succeeded but the
        + topic subscriptions may not have been honoured. The
      (B) CODE-line mismatch [review]:
        ! ``mqtt_silent_for_too_long`` flag combines this with a threshold.
   [_utc_now_iso] (richest: 1\client\mqtt_push.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return a compact UTC timestamp for diagnostics."""
   [async_publish_json] (richest: pre_transfer)  DOC=4 CODE=8
      (A) doc/comment-only:
        + qos: MQTT Quality of Service level (default 1).
        + """  # noqa: E501, RUF100
        + # JSON command frames (propertyChange) default to at-least-once
        + # delivery; aiomqtt 2.5.x takes a plain int QoS.
      (B) CODE-line mismatch [review]:
        ! qos: int = 1,
        ! raise RuntimeError("MQTT client is not running")  # noqa: TRY003
        ! payload_bytes = text.encode()
        ! await client.publish(topic, payload_bytes, qos=qos, retain=retain)
        ! except _MqttErrorTuple as err:
        ! raise RuntimeError(f"MQTT publish failed: {err}") from err  # noqa: TRY003
        ! except Exception as err:
        ! raise RuntimeError(f"MQTT publish failed: {err}") from err  # noqa: TRY003
   [async_start] (richest: pre_transfer)  DOC=2 CODE=6
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + f"{MQTT_TOPIC_PREFIX}/{user_id}/{_MQTT_AVAILABILITY_TOPIC_SUFFIX}"
      (B) CODE-line mismatch [review]:
        ! self._availability_topic = (
        ! self._topic_count = len(self._topics)
        ! self._subscribed_count = 0
        ! self._connection_state = "disconnected"
        ! self._stopping = False
        ! availability_topic=self._availability_topic,
   [async_stop] (richest: 1\client\mqtt_push.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Stop MQTT connection."""
   [async_wait_until_connected] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! raise RuntimeError("MQTT client is not running")  # noqa: TRY003
   [consecutive_auth_failures] (richest: 1\client\mqtt_push.py)  DOC=4 CODE=2
      (A) doc/comment-only:
        + """Public read-only view of the auth-failure streak counter.
        + The coordinator reads this to decide whether a transient credential
        + app) or surfaced as ``ConfigEntryAuthFailed`` so HA opens the reauth
        + successful broker handshake.
      (B) CODE-line mismatch [review]:
        ! rejection should be tolerated (token-rotation race with the official
        ! flow. Increments on CONNACK rc=4/5/134/135, resets on the first
   [diagnostics] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\client\mqtt_push.py)  DOC=24 CODE=1
      (A) doc/comment-only:
        + """Return a redacted snapshot of the MQTT client state for diagnostics."""
        + "connected": self._connected,
        + "started": self._runner_task is not None,
        + "messages_seen": self._messages_seen,
        + "messages_dropped": self._messages_dropped,
        + "topics": [self._redact_topic(topic) for topic in self._topics],
      (B) CODE-line mismatch [review]:
        ! return {
   [diagnostics_snapshot] (richest: pre_transfer)  DOC=6 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + "connection_state": self._connection_state,
        + "topic_count": self._topic_count,
        + "subscribed_count": self._subscribed_count,
        + "consecutive_connect_failures": self._consecutive_connect_failures,
   [filter] (richest: jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + """Suppress expected aiomqtt passive socket-reset log messages.
        + Evaluates a LogRecord and filters out known benign "failed to receive on socket" messages that include com
      (B) CODE-line mismatch [review]:
        ! bool: `True` if the record should be logged, `False` otherwise.
   [is_connected] (richest: jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Return whether the MQTT client currently has an active connection.
      (B) CODE-line mismatch [review]:
        ! `true` if the client is connected to the MQTT broker, `false` otherwise.
   [is_started] (richest: jackery_solarvault-Bigdaddy1990-patch-1\client\mqtt_push.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Report whether the MQTT push client's background runner task has been started.
      (B) CODE-line mismatch [review]:
        ! True if a background runner task exists, False otherwise.
   [seconds_since_last_message] (richest: 1\client\mqtt_push.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + """Public read-only view of the last-message age helper.
        + Coordinator-side adaptive polling reads this property to gate
      (B) CODE-line mismatch [review]:
        ! fast HTTP refreshes while MQTT push is delivering fresh frames.
   [topic_value] (richest: pre_transfer)  DOC=5 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + "connection_state": self._connection_state,
        + "topic_count": self._topic_count,
        + "subscribed_count": self._subscribed_count,
        + "consecutive_connect_failures": self._consecutive_connect_failures,

==========================================================================================
custom_components/jackery_solarvault/client/third_party_mqtt_codec.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (2) --
   [decode_third_party_mqtt_field] (richest: pre_transfer)  DOC=0 CODE=6
      (B) CODE-line mismatch [review]:
        ! raise ValueError(  # noqa: TRY003
        ! envelope = base64.b64decode(value, validate=True)
        ! iv, ciphertext = _split_iv_envelope(envelope)
        ! plaintext = aes_decrypt(ciphertext, bluetooth_key, iv)
        ! except (binascii.Error, ValueError, UnicodeDecodeError) as err:
        ! raise ValueError("invalid app-encoded third-party MQTT field") from err  # noqa: TRY003
   [encode_third_party_mqtt_field] (richest: pre_transfer)  DOC=1 CODE=5
      (A) doc/comment-only:
        + is generated per call so repeated secrets do not produce repeated ciphertext.
      (B) CODE-line mismatch [review]:
        ! The wire envelope is ``iv || ciphertext`` encoded with Base64. A fresh IV
        ! raise ValueError(  # noqa: TRY003
        ! iv = secrets.token_bytes(BLE_AES_IV_LEN)
        ! ciphertext = aes_encrypt(value.encode("utf-8"), bluetooth_key, iv)
        ! return base64.b64encode(iv + ciphertext).decode("ascii")

==========================================================================================
custom_components/jackery_solarvault/config_flow.py
==========================================================================================
-- MISSING MODULE CONSTANTS (3) --
   _BOOL_OPTION_DEFAULTS  <- 01.06\custom_components\jackery_solarvault\config_flow.py, 27.05\custom_components\jackery_solarvault\config_flow.py, jackery_solarvault-Bigdaddy1990-patch-1\config_flow.py
   _INT_OPTION_DEFAULTS  <- 01.06\custom_components\jackery_solarvault\config_flow.py, 27.05\custom_components\jackery_solarvault\config_flow.py, jackery_solarvault-Bigdaddy1990-patch-1\config_flow.py
   _STR_OPTION_DEFAULTS  <- 01.06\custom_components\jackery_solarvault\config_flow.py, 27.05\custom_components\jackery_solarvault\config_flow.py, jackery_solarvault-Bigdaddy1990-patch-1\config_flow.py
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (11) --
   [JackeryConfigFlow] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=26 CODE=21
      (A) doc/comment-only:
        + """Handle the initial user-driven configuration step and authenticate the provided Jackery account.
        + Parameters:
        + Returns:
        + """Reconfigure an existing config entry by validating provided account credentials and updating stored use
        + Returns:
        + vol.Required(
      (B) CODE-line mismatch [review]:
        ! Validates and normalizes the submitted username, prevents creating a duplicate entry for the same account,
        ! user_input (dict[str, Any] | None): Form input submitted by the user. Expected keys include `CONF_USERNAME
        ! ConfigFlowResult: A flow result that either shows the user form with errors or creates the new configurati
        ! account = _normalize_account(user_input[CONF_USERNAME])
        ! Validates that the submitted username matches the entry being reconfigured, verifies credentials with the
        ! A ConfigFlowResult that shows the reconfigure form with any errors, aborts with a specific reason, or upda
        ! account = _normalize_account(user_input[CONF_USERNAME])
        ! CONF_USERNAME, default=entry.data.get(CONF_USERNAME, "")
   [JackeryOptionsFlow] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=14 CODE=17
      (A) doc/comment-only:
        + """Present the options form for the integration or create an options entry from submitted values.
        + Parameters:
        + Returns:
        + ConfigFlowResult: The created options entry result, or a form result to display to the user.
        + CONF_ENABLE_UNREDACTED_DIAGNOSTICS
        + CONF_ENABLE_BLE_WRITES,
      (B) CODE-line mismatch [review]:
        ! When `user_input` is provided, merge the submitted values with the current stored options and create an op
        ! user_input (dict[str, Any] | None): Submitted form values, or None to render the form.
        ! return self.async_create_entry(
        ! title="",
        ! data=_flow_options(user_input, current_options),
        ! current_enable_ble_writes = current_options[CONF_ENABLE_BLE_WRITES]
        ! current_enable_unredacted_diagnostics = current_options[
        ! default=current_enable_ble_writes,
   [_current_option_values] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=4 CODE=11
      (A) doc/comment-only:
        + """Resolve the current option values for a configuration entry.
        + Parameters:
        + entry (ConfigEntry): Configuration entry to read option and legacy setup-data values from.
        + Returns:
      (B) CODE-line mismatch [review]:
        ! def _current_option_values(entry: ConfigEntry) -> dict[str, Any]:
        ! For each known option key (grouped by boolean, string, and integer types), the value is taken from the ent
        ! dict[str, Any]: Mapping of option keys to their resolved current values.
        ! values: dict[str, Any] = {}
        ! for key, bool_default in _BOOL_OPTION_DEFAULTS.items():
        ! values[key] = config_entry_bool_option(entry, key, bool_default)
        ! for key, str_default in _STR_OPTION_DEFAULTS.items():
        ! values[key] = config_entry_str_option(entry, key, str_default)
   [_flow_options] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! current_options: dict[str, Any] | None = None,
   [_normalize_account] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=2 CODE=3
      (A) doc/comment-only:
        + """Normalize an account identifier by stripping leading and trailing whitespace.
        + Returns:
      (B) CODE-line mismatch [review]:
        ! def _normalize_account(value: str) -> str:
        ! The account identifier with leading and trailing whitespace removed.
        ! return value.strip()
   [async_step_bluetooth] (richest: pre_transfer)  DOC=2 CODE=6
      (A) doc/comment-only:
        + self, discovery_info: BluetoothServiceInfoBleak
        + """Handle Bluetooth discovery."""
      (B) CODE-line mismatch [review]:
        ! async def async_step_bluetooth(
        ! ) -> ConfigFlowResult:
        ! await self.async_set_unique_id(discovery_info.address)
        ! self._abort_if_unique_id_configured()
        ! if self._async_current_entries():
        ! return self.async_abort(reason="already_configured")
   [async_step_dhcp] (richest: pre_transfer)  DOC=2 CODE=4
      (A) doc/comment-only:
        + self, discovery_info: DhcpServiceInfo
        + """Handle DHCP discovery."""
      (B) CODE-line mismatch [review]:
        ! async def async_step_dhcp(
        ! ) -> ConfigFlowResult:
        ! if self._async_current_entries():
        ! return self.async_abort(reason="already_configured")
   [async_step_init] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=14 CODE=17
      (A) doc/comment-only:
        + """Present the options form for the integration or create an options entry from submitted values.
        + Parameters:
        + Returns:
        + ConfigFlowResult: The created options entry result, or a form result to display to the user.
        + CONF_ENABLE_UNREDACTED_DIAGNOSTICS
        + CONF_ENABLE_BLE_WRITES,
      (B) CODE-line mismatch [review]:
        ! When `user_input` is provided, merge the submitted values with the current stored options and create an op
        ! user_input (dict[str, Any] | None): Submitted form values, or None to render the form.
        ! return self.async_create_entry(
        ! title="",
        ! data=_flow_options(user_input, current_options),
        ! current_enable_ble_writes = current_options[CONF_ENABLE_BLE_WRITES]
        ! current_enable_unredacted_diagnostics = current_options[
        ! default=current_enable_ble_writes,
   [async_step_reauth_confirm] (richest: pre_transfer)  DOC=8 CODE=3
      (A) doc/comment-only:
        + # The cloud may have rotated the MQTT seed on password change.
        + # Clear the cached session so the next setup derives fresh
        + # MQTT credentials from the new login instead of replaying
        + # a potentially stale AES-256 seed.
        + _LOGGER.debug(
        + "Jackery reauth: failed to clear MQTT session; "
      (B) CODE-line mismatch [review]:
        ! account=entry.data[CONF_USERNAME],
        ! await async_clear_mqtt_session(self.hass, entry.entry_id)
        ! except Exception:  # noqa: BLE001
   [async_step_reconfigure] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=18 CODE=15
      (A) doc/comment-only:
        + """Reconfigure an existing config entry by validating provided account credentials and updating stored use
        + Returns:
        + vol.Required(
        + CONF_ENABLE_BLE_WRITES,
        + CONF_ENABLE_UNREDACTED_DIAGNOSTICS,
        + CONF_THIRD_PARTY_MQTT_ENABLE,
      (B) CODE-line mismatch [review]:
        ! Validates that the submitted username matches the entry being reconfigured, verifies credentials with the
        ! A ConfigFlowResult that shows the reconfigure form with any errors, aborts with a specific reason, or upda
        ! account = _normalize_account(user_input[CONF_USERNAME])
        ! CONF_USERNAME, default=entry.data.get(CONF_USERNAME, "")
        ! ): vol.All(str, vol.Length(min=1)),
        ! default=current_options[CONF_ENABLE_BLE_WRITES],
        ! default=current_options[CONF_ENABLE_UNREDACTED_DIAGNOSTICS],
        ! default=current_options[CONF_THIRD_PARTY_MQTT_ENABLE],
   [async_step_user] (richest: 01.06\custom_components\jackery_solarvault\config_flow.py)  DOC=3 CODE=4
      (A) doc/comment-only:
        + """Handle the initial user-driven configuration step and authenticate the provided Jackery account.
        + Parameters:
        + Returns:
      (B) CODE-line mismatch [review]:
        ! Validates and normalizes the submitted username, prevents creating a duplicate entry for the same account,
        ! user_input (dict[str, Any] | None): Form input submitted by the user. Expected keys include `CONF_USERNAME
        ! ConfigFlowResult: A flow result that either shows the user form with errors or creates the new configurati
        ! account = _normalize_account(user_input[CONF_USERNAME])

==========================================================================================
custom_components/jackery_solarvault/coordinator.py
==========================================================================================
-- MISSING FUNCTIONS/CLASSES (4) --
   _bump_polling_diag  <- 21.05\21.05\custom_components\jackery_solarvault\coordinator.py, 24.05\24.05\custom_components\jackery_solarvault\coordinator.py
   _current_soc_limit  <- 24.05\24.05\custom_components\jackery_solarvault\coordinator.py
   _should_skip_refresh_for_live_mqtt  <- 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py, 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py
   _soc_limit  <- 24.05\24.05\custom_components\jackery_solarvault\coordinator.py
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (164) --
   [JackerySolarVaultCoordinator] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=340 CODE=381
      (A) doc/comment-only:
        + per-device payload as the HTTP path. HTTP property polling stays active
        + every coordinator cycle; MQTT only overlays fresher live values.
        + # Last MQTT session snapshot written to the persistent cache. Compared
        + # against ``api.mqtt_session_snapshot()`` after every successful
        + # connect to avoid redundant Store writes when the credentials did
        + # not actually rotate. Seeded with whatever the API already holds —
      (B) CODE-line mismatch [review]:
        ! _THIRD_PARTY_MQTT_CONFIG_KEYS = frozenset({
        ! _DEVICE_YEAR_BACKFILL_STAT_KEYS = {
        ! _SYSTEM_YEAR_BACKFILL_STAT_KEYS = {
        ! config_entry=entry,
        ! interval_sec = max(15, safe_int(update_interval.total_seconds()) or 15)
        ! switch and a dedicated DEBUG logger. They are useful for parser/source
        ! if not dev_mode_redactions_disabled():
        ! if await self._async_load_cached_discovery(
   [__init__] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=23 CODE=2
      (A) doc/comment-only:
        + # Last MQTT session snapshot written to the persistent cache. Compared
        + # against ``api.mqtt_session_snapshot()`` after every successful
        + # connect to avoid redundant Store writes when the credentials did
        + # not actually rotate. Seeded with whatever the API already holds —
        + # either fields hydrated from the cache in ``_async_authenticate`` or
        + # fields populated by a successful cloud login in the same setup pass.
      (B) CODE-line mismatch [review]:
        ! config_entry=entry,
        ! interval_sec = max(15, safe_int(update_interval.total_seconds()) or 15)
   [_apply_local_price_patch] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! price = merge_live_properties(price, updates)
   [_apply_local_property_patch] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! props = self._merge_main_properties(props, clean_updates)
   [_apply_local_smart_plug_patch] (richest: 1\coordinator.py)  DOC=3 CODE=3
      (A) doc/comment-only:
        + plug.get(FIELD_DEVICE_SN)
        + or plug.get(FIELD_DEV_SN)
        + or plug.get(FIELD_SN)
      (B) CODE-line mismatch [review]:
        ! sn = (
        ! if str(sn) == str(plug_sn):
        ! self.async_set_updated_data(new_data)
   [_apply_local_weather_plan_patch] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! weather = merge_live_properties(weather, updates)
   [_async_add_app_chart_statistics] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=8 CODE=6
      (A) doc/comment-only:
        + ))
        + # Clamp the prior-sum anchor to >= 0. ``states`` are already non-negative
        + # (``max(0.0, round(point.value, 5))`` above), but a poisoned recorder
        + # row (e.g. a previous bug that wrote a negative ``sum``) would otherwise
        + # propagate as negative external statistics for every subsequent bucket.
        + # ``state_class=total_increasing`` plus an energy ``device_class``
      (B) CODE-line mismatch [review]:
        ! signature_key = ":".join((
        ! starts[0].isoformat(),
        ! starts[-1].isoformat(),
        ! if self._stat_import_last_sig.get(signature_key) == series_signature:
        ! cumulative = max(0.0, offset)
        ! self._stat_import_last_sig[signature_key] = series_signature
   [_async_compiled_statistic_hour_starts] (richest: pre_transfer)  DOC=2 CODE=8
      (A) doc/comment-only:
        + _LOGGER.exception("Recorder run markers unavailable")
        + _LOGGER.exception("Could not read Recorder run markers")
      (B) CODE-line mismatch [review]:
        ! from homeassistant.components.recorder import get_instance  # noqa: I001, PLC0415
        ! from homeassistant.components.recorder.db_schema import StatisticsRuns  # noqa: PLC0415
        ! from homeassistant.helpers.recorder import session_scope  # noqa: PLC0415
        ! except ImportError, RuntimeError:
        ! except Exception:
        ! _LOGGER.exception("Recorder instance unavailable for run markers")
        ! if item[0] is not None and item[0].minute == 55  # noqa: PLR2004
        ! except Exception:
   [_async_enrich_battery_pack_ota] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=4
      (A) doc/comment-only:
        + @staticmethod
      (B) CODE-line mismatch [review]:
        ! ) -> None:
        ! return
        ! if not isinstance(pack, dict):
        ! return
   [_async_ensure_mqtt] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=19 CODE=9
      (A) doc/comment-only:
        + CoreState.final_write,
        + CoreState.not_running,
        + CoreState.stopped,
        + CoreState.stopping,
        + # Cloud login unreachable (timeout, 5xx, DNS). The MQTT password
        + # derivation is local, so retry with the cached session from
      (B) CODE-line mismatch [review]:
        ! if self.hass.state in (
        ! used_stale_session = False
        ! creds = await self.api.async_get_mqtt_credentials(allow_stale=True)
        ! except JackeryError as stale_err:
        ! used_stale_session = True
        ! if used_stale_session:
        ! await self._async_invalidate_mqtt_session_cache(
        ! if self._mqtt is mqtt:
   [_async_entity_statistic_offsets] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=16 CODE=23
      (A) doc/comment-only:
        + statistics_during_period,
        + # 370 days covers a full year of gaps; shorter windows would return
        + # (0.0, 0.0) on any multi-day outage and break monotonic sum continuity
        + # for entity statistics, producing negative deltas in the Energy
        + # Dashboard once cumulative_sum restarts from zero.
        + statistics_during_period,
      (B) CODE-line mismatch [review]:
        ! from homeassistant.components.recorder.statistics import (
        ! prior_start = start - timedelta(days=370)
        ! existing = await recorder.async_add_executor_job(
        ! self.hass,
        ! rows = existing.get(statistic_id, []) if isinstance(existing, dict) else []
        ! previous: tuple[float, float, float, float | None] | None = None
        ! for row in rows:
        ! if not isinstance(row, dict):
   [_async_fetch_historical_app_chart_source] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if date_type == DATE_TYPE_MONTH:
   [_async_handle_mqtt_disconnect] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=3 CODE=1
      (A) doc/comment-only:
        + CoreState.final_write,
        + CoreState.not_running,
        + ):
      (B) CODE-line mismatch [review]:
        ! if self.hass.state in (
   [_async_handle_mqtt_message] (richest: pre_transfer)  DOC=33 CODE=32
      (A) doc/comment-only:
        + _LOGGER.debug(
        + "Jackery MQTT: could not resolve device_id from payload on %s; "
        + "payload keys=%s",
        + topic,
        + _LOGGER.debug(
        + "Jackery MQTT: device_id %s not in coordinator data (discovery "
      (B) CODE-line mismatch [review]:
        ! async def _async_handle_mqtt_message(  # noqa: C901, PLR0912, PLR0914, PLR0915
        ! list(payload.keys()),
        ! if topic.endswith(("/device", "/config")):
        ! if is_subdevice:
        ! incoming_alarm = body or payload
        ! existing_alarm = updated.get(PAYLOAD_ALARM)
        ! if isinstance(existing_alarm, dict) and isinstance(incoming_alarm, dict):
        ! updated[PAYLOAD_ALARM] = {**existing_alarm, **incoming_alarm}
   [_async_import_and_repair_app_chart_statistics] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Repair missed buckets and import the current app chart snapshot."""
      (B) CODE-line mismatch [review]:
        ! today = dt_util.now().date()
        ! if not isinstance(payload, dict):
        ! changed = bool(repair_counts)
   [_async_import_app_chart_entity_statistics_for_device] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=18 CODE=27
      (A) doc/comment-only:
        + """Import app buckets into HA-owned entity statistics safely.
        + DAY/WEEK/MONTH/YEAR batches reach this importer. Current-payload calls keep
        + HA's compiled rows authoritative. Historical day repair can set
        + rebuilt from app power curves.
        + str,
        + candidate_prefix,
      (B) CODE-line mismatch [review]:
        ! replace_existing_hours: bool = False,
        ! include_current_day_completed: bool = False,
        ! ``replace_existing_hours`` to replace first-run spike rows with buckets
        ! contributions: dict[
        ! dict[tuple[datetime, str, bool], tuple[float, int]],
        ! ] = {}
        ! points: list[Any] = []
        ! ) in self._metric_source_candidates(
   [_async_import_app_chart_statistics] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=45 CODE=45
      (A) doc/comment-only:
        + APP_POLLING_MQTT.md defines the source endpoints and period ranges.
        + StatisticData,
        + StatisticMeanType,
        + StatisticMetaData,
        + async_add_external_statistics,
        + (payload.get(PAYLOAD_SYSTEM) or {}).get(FIELD_DEVICE_NAME)
      (B) CODE-line mismatch [review]:
        ! ) -> None:
        ! return
        ! try:
        ! from homeassistant.components.recorder.models import (
        ! from homeassistant.components.recorder.statistics import (
        ! from homeassistant.const import UnitOfEnergy
        ! from homeassistant.util.unit_conversion import EnergyConverter
        ! except (ImportError, RuntimeError) as err:
   [_async_import_current_app_chart_entity_statistics] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=9 CODE=19
      (A) doc/comment-only:
        + *,
        + day_imported,
        + day_failed,
        + # Day curves are already complete per elapsed hour. Replace
        + # compiled rows so a startup period-total spike cannot stay
        + # in Recorder after the cloud minute curve is available.
      (B) CODE-line mismatch [review]:
        ! replace_period_hours: bool = False,
        ! if not isinstance(payload, dict):
        ! day_batches = [
        ! batch for batch in source_batches if batch[0] == DATE_TYPE_DAY
        ! period_batches = [
        ! batch for batch in source_batches if batch[0] != DATE_TYPE_DAY
        ! imported_rows = 0
        ! failed_rows = 0
   [_async_import_current_app_chart_statistics_job] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=5 CODE=3
      (A) doc/comment-only:
        + """Import only current app chart buckets."""
        + successful_devices.update(
        + # ------------------------------------------------------------------
        + # Coordinator update cycle (merge of HTTP + MQTT + caches)
        + # ------------------------------------------------------------------
      (B) CODE-line mismatch [review]:
        ! successful_devices = await self._async_import_app_chart_statistics(snapshot)
        ! await self._async_import_day_chart_statistics(snapshot)
        ! await self._async_import_current_app_chart_entity_statistics(snapshot)
   [_async_import_day_chart_statistics] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if not isinstance(payload, dict):
   [_async_mqtt_backfill_queries] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + _LOGGER.exception("Jackery MQTT backfill query failed")
      (B) CODE-line mismatch [review]:
        ! except Exception:
   [_async_mqtt_connected] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! await self._async_query_system_info_for_missing(force=True, ensure_mqtt=False)
        ! await self._async_query_weather_plan_for_missing(force=True, ensure_mqtt=False)
        ! await self._async_query_subdevices_for_missing(force=True, ensure_mqtt=False)
   [_async_mqtt_poll_queries] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=4 CODE=0
      (A) doc/comment-only:
        + _LOGGER.debug("Jackery MQTT polling query failed: %s", err)
        + # ------------------------------------------------------------------
        + # Statistics import & data-quality reporting
        + # ------------------------------------------------------------------
   [_async_payload_debug_event] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=9 CODE=2
      (A) doc/comment-only:
        + Raw payload diagnostics are deliberately gated behind both an options
        + bugs, but writing every HTTP/MQTT payload on normal installations is
        + unnecessary disk churn.
        + # Payload-debug capture is a developer-only feature. Gated on the
        + # ``JACKERY_DEV_MODE=1`` environment variable so it cannot be toggled
        + # by a UI checkbox a regular user might leave on accidentally.
      (B) CODE-line mismatch [review]:
        ! switch and a dedicated DEBUG logger. They are useful for parser/source
        ! if not dev_mode_redactions_disabled():
   [_async_price_sources_for_device] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! except JackeryError:
        ! _LOGGER.exception("price source fetch failed for %s", device_id)
   [_async_publish_command] (richest: pre_transfer)  DOC=2 CODE=8
      (A) doc/comment-only:
        + FIELD_ID: ts,
        + "Jackery credentials were rejected while refreshing MQTT command credentials",  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! async def _async_publish_command(  # noqa: PLR0912, PLR0913
        ! raise HomeAssistantError("MQTT client not initialized")  # noqa: TRY003
        ! raise HomeAssistantError(  # noqa: TRY003
        ! try:  # noqa: PLW0717
        ! raise RuntimeError("MQTT client is not connected")  # noqa: TRY003, TRY301
        ! raise RuntimeError("MQTT client is not running")  # noqa: TRY003, TRY301
        ! await self._mqtt.async_publish_json(topic, payload, qos=1, retain=False)
        ! return  # noqa: TRY300
   [_async_publish_command_ble_first] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=9 CODE=7
      (A) doc/comment-only:
        + "Jackery BLE command unavailable for %s actionId=%s cmd=%s; "
        + "falling back to MQTT",
        + cmd,
        + "Jackery BLE command failed for %s actionId=%s cmd=%s; "
        + "MQTT fallback also failed: BLE=%s MQTT=%s",
        + cmd,
      (B) CODE-line mismatch [review]:
        ! ble_error: RuntimeError | ValueError | None = None
        ! cmd_int = _transport_cmd(cmd)
        ! if cmd_int > 0:
        ! cmd=cmd_int,
        ! body=self._command_body_for_transport(body_fields, cmd=cmd_int),
        ! cmd=cmd_int,
        ! except Exception as err:
   [_async_query_subdevices_for_missing] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Query MQTT sub-device status for smart meter and pack payloads."""
      (B) CODE-line mismatch [review]:
        ! if self._mqtt is None or not self._mqtt.is_connected:
        ! if not should_query_meter and not should_query_packs:
   [_async_query_system_info_for_missing] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! if has_all and not force:
        ! await self.async_query_device_info(device_id, ensure_mqtt=ensure_mqtt)
        ! await self.async_query_system_info(device_id, ensure_mqtt=ensure_mqtt)
   [_async_query_weather_plan_for_missing] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! self._last_weather_plan_query[device_id] = now  # stamp AFTER success
   [_async_refresh_battery_pack_ota] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + _LOGGER.exception("Jackery pack OTA background refresh failed")
      (B) CODE-line mismatch [review]:
        ! try:  # noqa: PLW0717
        ! except Exception:
   [_async_refresh_discovery_if_due] (richest: pre_transfer)  DOC=2 CODE=3
      (A) doc/comment-only:
        + _LOGGER.exception("Jackery runtime discovery refresh failed")
        + _LOGGER.exception("Jackery runtime discovery refresh failed")
      (B) CODE-line mismatch [review]:
        ! self._last_discovery_refresh_monotonic = now  # stamp AFTER success so a failed discover doesn't burn the
        ! except JackeryError:
        ! except UpdateFailed:
   [_async_repair_missing_app_chart_statistics] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + over a calendar boundary, previous daily/monthly buckets must be fetched
   [_async_retry_after_invalid_discovery_devices] (richest: pre_transfer)  DOC=3 CODE=0
      (A) doc/comment-only:
        + # ------------------------------------------------------------------
        + # Coordinator update cycle (merge of HTTP + MQTT + caches)
        + # ------------------------------------------------------------------
   [_async_save_statistics_backfill_state] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Persist external-statistics repair state."""
   [_async_statistic_sum_offset] (richest: pre_transfer)  DOC=2 CODE=7
      (A) doc/comment-only:
        + _LOGGER.exception("Recorder statistics API unavailable")
        + _LOGGER.exception("Recorder instance unavailable")
      (B) CODE-line mismatch [review]:
        ! from homeassistant.components.recorder import get_instance  # noqa: PLC0415
        ! from homeassistant.components.recorder.statistics import (  # noqa: PLC0415
        ! except ImportError, RuntimeError:
        ! except Exception:
        ! except Exception as err:  # noqa: BLE001
        ! row_sum = safe_float(
        ! row.get("sum") if isinstance(row, dict) else getattr(row, "sum", None)
   [_async_statistics_import_job] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=5 CODE=13
      (A) doc/comment-only:
        + """Run recorder statistic imports in the background."""
        + diag.pop("last_error", None)
        + _LOGGER.debug(
        + "Jackery recorder-statistics import failed: %s",
        + _exception_debug_message(err),
      (B) CODE-line mismatch [review]:
        ! diag = self._statistics_import_diagnostics
        ! diag["task_active"] = True
        ! diag["last_started_at"] = utc_now().isoformat()
        ! diag["last_status"] = "running"
        ! await self._async_import_current_app_chart_statistics_job(snapshot)
        ! diag["last_status"] = "completed"
        ! diag["last_finished_at"] = utc_now().isoformat()
        ! diag["last_status"] = "cancelled"
   [_async_update_data] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=50 CODE=50
      (A) doc/comment-only:
        + "started_at": utc_now().isoformat(),
        + "skipped_fast_property_fetch": skip_fast_property_fetch,
        + "slow_metrics_interval_seconds": self._slow_metrics_interval_sec,
        + "price_interval_seconds": self._price_config_interval_sec,
        + "statistics_import_ready": self._statistics_import_ready,
        + "cache_hits": {},
      (B) CODE-line mismatch [review]:
        ! polling_diag: dict[str, Any] = {
        ! def _bump_polling_diag(bucket: str, cache_key: str) -> None:
        ! values = polling_diag.setdefault(bucket, {})
        ! if not isinstance(values, dict):
        ! values = {}
        ! polling_diag[bucket] = values
        ! key = str(cache_key)
        ! current = safe_int(values.get(key)) or 0
   [_async_update_data_quality_issue] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=3 CODE=7
      (A) doc/comment-only:
        + and existing_issue_id.endswith(issue_suffix)
        + ):
        + @property
      (B) CODE-line mismatch [review]:
        ! issue_suffix = f"_{REPAIR_ISSUE_APP_DATA_INCONSISTENCY}"
        ! registry = ir.async_get(self.hass)
        ! for domain, existing_issue_id in tuple(registry.issues):
        ! if (
        ! domain == DOMAIN
        ! and existing_issue_id != issue_id
        ! ir.async_delete_issue(self.hass, DOMAIN, existing_issue_id)
   [_battery_packs_from_source] (richest: pre_transfer)  DOC=2 CODE=2
      (A) doc/comment-only:
        + cls,
        + source: Any,  # noqa: ANN401, RUF100
      (B) CODE-line mismatch [review]:
        ! def _battery_packs_from_source(
        ! ) -> list[dict[str, Any]] | None:
   [_battery_packs_need_query] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! expected_raw = safe_int(props.get(FIELD_BAT_NUM))
        ! expected = max(0, expected_raw or 0)
   [_ble_address_for_device] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! address = self._ble_listener.address_for_device_id(device_id)
        ! return str(address) if address is not None else None
   [_ble_writes_enabled] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=2 CODE=3
      (A) doc/comment-only:
        + """Return whether developer-only BLE writes are allowed."""
        + config_entry_bool_option(
      (B) CODE-line mismatch [review]:
        ! from .util import dev_mode_redactions_disabled
        ! return (
        ! and dev_mode_redactions_disabled()
   [_coerce_transport_cmd] (richest: custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + cmd: Any,
      (B) CODE-line mismatch [review]:
        ! def _coerce_transport_cmd(
        ! ) -> int:  # arbitrary cmd input, coerced at runtime
   [_command_body_for_transport] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! body_fields: dict[str, Any], *, cmd: int
        ! cmd_int = _transport_cmd(cmd)
        ! if cmd_int > 0:
        ! body[FIELD_CMD] = cmd_int
   [_completed_entity_app_points] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=9 CODE=9
      (A) doc/comment-only:
        + """Filter app points to completed buckets for entity-stat imports.
        + Current day rows are normally skipped so HA Recorder keeps owning live
        + state history. Repair callers may include only already-completed
        + current-day hours, which replaces first-run total spikes without
        + writing the still-open hour.
        + include_current_day_completed
      (B) CODE-line mismatch [review]:
        ! include_current_day_completed: bool = False,
        ! current_hour_start = self._local_now().replace(
        ! minute=0,
        ! second=0,
        ! microsecond=0,
        ! tzinfo=None,
        ! if (
        ! and reset_period == DATE_TYPE_DAY
   [_current_app_chart_entity_source_batches] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=10 CODE=10
      (A) doc/comment-only:
        + DATE_TYPE_DAY,
        + DATE_TYPE_WEEK,
        + DATE_TYPE_MONTH,
        + DATE_TYPE_YEAR,
        + ):
        + continue
      (B) CODE-line mismatch [review]:
        ! enabled_date_types = self._enabled_app_chart_date_types()
        ! for date_type in (
        ! if date_type not in enabled_date_types:
        ! if (
        ! section_prefix == APP_SECTION_HOME_TRENDS
        ! and date_type == DATE_TYPE_DAY
        ! payload_key = PAYLOAD_HOME_TRENDS
        ! payload_key = f"{section_prefix}_{date_type}"
   [_day_chart_points_for_metric] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=2 CODE=6
      (A) doc/comment-only:
        + _LOGGER.debug(
        + "Using derived home-energy day fallback from %s.%s",
      (B) CODE-line mismatch [review]:
        ! if self._is_derived_home_energy_candidate(
        ! metric_key=metric_key,
        ! section_prefix=section_prefix,
        ! stat_key=stat_key,
        ! candidate_prefix=section.rsplit(f"_{DATE_TYPE_DAY}", 1)[0],
        ! candidate_stat_key=source_stat_key,
   [_day_chart_source_candidates] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=12 CODE=2
      (A) doc/comment-only:
        + self,
        + candidate_prefix,
        + candidate_stat_key,
        + section_prefix,
        + stat_key,
        + metric_key,
      (B) CODE-line mismatch [review]:
        ! for (
        ! ) in self._metric_source_candidates(
   [_derived_home_energy_fallback_enabled] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! from .util import config_entry_bool_option
   [_device_country_code] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=3 CODE=3
      (A) doc/comment-only:
        + section.get(FIELD_COUNTRY_CODE),
        + section.get(FIELD_COUNTRY),
        + section.get(FIELD_SYSTEM_REGION),
      (B) CODE-line mismatch [review]:
        ! raw = first_nonblank(
        ! if raw is not None:
        ! return raw.upper()
   [_drop_stale_battery_packs] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + that we never invent device state, only document silence.
      (B) CODE-line mismatch [review]:
        ! silent for the full threshold (default 7 days) before it is
        ! rationale and ``docs/STRICT_WORK_INSTRUCTIONS.md`` for the rule
   [_enabled_app_chart_date_types] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! from .util import config_entry_bool_option
   [_enrich_meter_head_statistics] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=93 CODE=95
      (A) doc/comment-only:
        + _raise_config_entry_auth_failed(
        + "Jackery credentials were rejected during property refresh", err
        + invalid_device_ids.append(dev_id)
        + _LOGGER.warning(
        + "property fetch failed for %s: %s",
        + dev_id,
      (B) CODE-line mismatch [review]:
        ! result: dict[str, dict[str, Any]] = {}
        ! invalid_device_ids: list[str] = []
        ! property_fetch_completed = False
        ! property_fetch_fallback_count = 0
        ! for dev_id, idx in self._device_index.items():
        ! old_entry: dict[str, Any] = {}
        ! if self.data:
        ! old_entry = self.data.get(dev_id) or {}
   [_enrich_smart_plug_statistics] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! lambda sid=stat_id: self.api.async_get_device_socket_statistic(sid),
   [_entity_statistic_ids_by_key] (richest: pre_transfer)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! from homeassistant.components.sensor import DOMAIN as SENSOR_DOMAIN  # noqa: I001, PLC0415
        ! from homeassistant.helpers import entity_registry as er  # noqa: PLC0415
        ! except ImportError, RuntimeError:
        ! _LOGGER.exception("Entity registry unavailable for entity repair")
   [_entity_statistics_from_contributions] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=10 CODE=3
      (A) doc/comment-only:
        + """Build non-negative HA entity statistics from app bucket contributions.
        + Normal current-payload imports keep HA's own state-history compilation
        + authoritative and only fill missing hours. Historical day repair may
        + deliberately rewrite existing rows, because a first-run period total can
        + otherwise leave the whole daily value in one compiled spike hour.
        + # ``state_offset`` is the previously-recorded period state from
      (B) CODE-line mismatch [review]:
        ! replace_existing_hours: bool = False,
        ! if not replace_existing_hours and hour_start in compiled_hour_starts:
        ! running_state = max(0.0, state_offset) if not statistics else 0.0
   [_entity_targets_for_app_points] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=4 CODE=5
      (A) doc/comment-only:
        + """Return entity-key/reset/cumulative-state targets for app buckets.
        + Each app endpoint owns its matching HA period. Cross-period repairs
        + The only year repair lives in the same-endpoint month backfill before
        + @staticmethod
      (B) CODE-line mismatch [review]:
        ! (day -> week, week -> month, month -> year) double-count and can
        ! overwrite the correct app aggregate with a different bucket shape.
        ! this import path sees the payload.
        ! key = periods.get(date_type)
        ! return ((key, date_type, True),) if key else ()
   [_fetch_device_extras] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=56 CODE=84
      (A) doc/comment-only:
        + _raise_config_entry_auth_failed(
        + "Jackery credentials were rejected during property refresh", err
        + invalid_device_ids.append(dev_id)
        + # Pull SN from either the fresh property payload or the discovery
        + # metadata — needed for the OTA endpoint (which keys on SN).
        + idx.get(PAYLOAD_DEVICE_META) or {}
      (B) CODE-line mismatch [review]:
        ! values = await asyncio.gather(*tasks)
        ! await self._async_enrich_battery_pack_ota(dev_id, packs, dev_sn)
        ! return await _get_with_ttl_for(
        ! return await _get_with_ttl_for(
        ! return await _get_with_ttl_for(
        ! *(_fetch_device_month(prefix, month) for month in previous_months)
        ! result: dict[str, dict[str, Any]] = {}
        ! invalid_device_ids: list[str] = []
   [_fetch_device_month] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=58 CODE=86
      (A) doc/comment-only:
        + _raise_config_entry_auth_failed(
        + "Jackery credentials were rejected during property refresh", err
        + invalid_device_ids.append(dev_id)
        + # Pull SN from either the fresh property payload or the discovery
        + # metadata — needed for the OTA endpoint (which keys on SN).
        + idx.get(PAYLOAD_DEVICE_META) or {}
      (B) CODE-line mismatch [review]:
        ! return await _get_with_ttl_for(
        ! return await _get_with_ttl_for(
        ! return await _get_with_ttl_for(
        ! *(_fetch_device_month(prefix, month) for month in previous_months)
        ! result: dict[str, dict[str, Any]] = {}
        ! invalid_device_ids: list[str] = []
        ! for dev_id, idx in self._device_index.items():
        ! try:
   [_fetch_previous_home_month] (richest: 1\1101\custom_components\jackery_solarvault\coordinator.py)  DOC=6 CODE=1
      (A) doc/comment-only:
        + # BUGFIXTODO Bug C: a single 404 (e.g. the device was
        + # bought mid-year and earlier months legitimately do
        + # not exist) used to abort the whole year backfill.
        + # ``return_exceptions`` lets the ``isinstance(source,
        + # dict)`` filter below quietly skip BaseException
        + # entries.
      (B) CODE-line mismatch [review]:
        ! ) -> Any:
   [_fetch_system] (richest: jackery_solarvault-Bigdaddy1990-patch-1\coordinator.py)  DOC=11 CODE=6
      (A) doc/comment-only:
        + # BUGFIXTODO Bug A: gather without return_exceptions previously
        + # let a single TimeoutError / aiohttp.ClientError / SSL drop
        + # propagate out and abort the whole update cycle, marking every
        + # Jackery entity unavailable. We isolate failures per slot and
        + # map them to the same defaults already wired in the
        + # BUGFIXTODO Bug C: a single 404 (e.g. the device was
      (B) CODE-line mismatch [review]:
        ! _slow_results = await asyncio.gather(
        ! _slow_defaults: tuple[Any, ...] = (
        ! _slow_safe = tuple(
        ! for value, default in zip(_slow_results, _slow_defaults, strict=True)
        ! ) = _slow_safe
        ! ) -> Any:
   [_find_dict_with_any_key] (richest: 1\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + obj: Any,
   [_find_list_for_key] (richest: pre_transfer)  DOC=2 CODE=2
      (A) doc/comment-only:
        + obj: Any,  # noqa: ANN401, RUF100
        + key: str,
      (B) CODE-line mismatch [review]:
        ! def _find_list_for_key(
        ! ) -> list[dict[str, Any]] | None:
   [_find_matching_price_source] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=8
      (A) doc/comment-only:
        + }:
      (B) CODE-line mismatch [review]:
        ! company_id = first_nonblank_int(current.get(FIELD_PLATFORM_COMPANY_ID))
        ! if company_id is None:
        ! region = first_nonblank(current.get(FIELD_SYSTEM_REGION))
        ! if first_nonblank_int(source.get(FIELD_PLATFORM_COMPANY_ID)) == company_id
        ! if region is not None:
        ! normalized_region = region.upper()
        ! if normalized_region in {
        ! item.upper() for item in self._source_regions(source)
   [_get_with_ttl] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=3 CODE=5
      (A) doc/comment-only:
        + default: Any,
        + *,
        + 5.0,
      (B) CODE-line mismatch [review]:
        ! timeout_sec: float | None = None,
        ! ) -> Any:
        ! timeout_sec=timeout_sec,
        ! system_trend_timeout_sec = min(
        ! max(2.0, self._configured_update_interval.total_seconds() / 2),
   [_get_with_ttl_for] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=11 CODE=10
      (A) doc/comment-only:
        + default: Any,
        + _bump_polling_diag("cache_hits", cache_key)
        + else:
        + _bump_polling_diag("failures", cache_key)
        + _LOGGER.debug("%s failed: %s", cache_key, _exception_debug_message(err))
        + _bump_polling_diag("failures", cache_key)
      (B) CODE-line mismatch [review]:
        ! timeout_sec: float | None = None,
        ! ) -> Any:
        ! if timeout_sec is None:
        ! value = await asyncio.wait_for(fetcher(), timeout=timeout_sec)
        ! except TimeoutError as err:
        ! cache[cache_key] = (now, entry[1])
        ! cache[cache_key] = (now, default)
        ! cache[cache_key] = (now, entry[1])
   [_has_smart_meter_accessory] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + @staticmethod
   [_is_battery_pack_lifetime_ble_payload] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + @staticmethod
   [_is_property_device_candidate] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=3 CODE=4
      (A) doc/comment-only:
        + and safe_bool(dev.get(FIELD_IS_CLOUD)) is True
        + ):
        + dev.get(FIELD_MODEL_CODE) in (None, "") and not dev.get(FIELD_DEV_MODEL)
      (B) CODE-line mismatch [review]:
        ! if safe_bool(dev.get(FIELD_BIND_KEY)) is False:
        ! if (
        ! str(dev.get(FIELD_DEV_TYPE) or "") == SUBDEVICE_TYPE_SMART_METER
        ! return not (
   [_is_subdevice_payload] (richest: 1\coordinator.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + pass
      (B) CODE-line mismatch [review]:
        ! try:
        ! if int(action_id) in MQTT_ACTION_IDS_SUBDEVICE:
        ! except (TypeError, ValueError):
   [_iter_calendar_months] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return first-of-month dates intersecting an inclusive date range."""
   [_load_compiled_hours] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=8 CODE=0
      (A) doc/comment-only:
        + # HA's short-term recorder writes one row per 5-minute slot
        + # (:00, :05, ..., :55). The :55 slot is the one that CLOSES
        + # the hour — its presence is the marker that HA has compiled
        + # the hourly statistic for that hour. Subtracting 55 minutes
        + # from the :55 timestamp yields the hour-boundary timestamp
        + # the HA-wins filter compares against. Filtering on any
   [_load_mqtt_push_client] (richest: 1\coordinator.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! module = importlib.import_module(".mqtt_push", __package__)
        ! return module.JackeryMqttPushClient
   [_load_offsets] (richest: 1\coordinator.py)  DOC=2 CODE=0
      (A) doc/comment-only:
        + session.query(StatisticsMeta.id)
        + session.query(
   [_local_statistic_start] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=4
      (A) doc/comment-only:
        + """Return a UTC timestamp for a local app-statistic bucket date."""
      (B) CODE-line mismatch [review]:
        ! def _local_statistic_start(self, bucket_date: date) -> datetime:
        ! timezone = dt_util.get_time_zone(self.hass.config.time_zone)
        ! if timezone is None:
        ! timezone = dt_util.DEFAULT_TIME_ZONE
   [_local_timezone] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _local_timezone(self) -> Any:
   [_looks_like_battery_pack] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _looks_like_battery_pack(cls, item: Any) -> bool:
   [_merge_battery_pack_lifetime_from_ble] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=5 CODE=10
      (A) doc/comment-only:
        + SolarVault and MQTT does not always announce the pack before
        + BLE sends these counters. Merge into the matching pack when it
        + exists; otherwise create a minimal pack entry keyed by
        + ``deviceSn`` so the opt-in lifetime sensors can expose the
        + counters instead of counting the frames as unrouted.
      (B) CODE-line mismatch [review]:
        ! ``/v1/device/battery/pack/list`` can return ``data: null`` for
        ! packs = []
        ! if not matched:
        ! if sum(1 for pack in merged_packs if isinstance(pack, dict)) >= 5:
        ! new_pack = {
        ! if body.get(FIELD_DEV_TYPE) is not None:
        ! new_pack[FIELD_DEV_TYPE] = body.get(FIELD_DEV_TYPE)
        ! if body.get(FIELD_SUB_TYPE) is not None:
   [_merge_battery_pack_lists] (richest: pre_transfer)  DOC=8 CODE=7
      (A) doc/comment-only:
        + current: Any,  # noqa: ANN401, RUF100
        + continue
        + # Tag each pack that is reporting as online with the current UTC
        + # timestamp. Packs with commState=0 (offline) keep their previous
        + # _last_seen_at so a brief outage does not look like a removal.
        + # Packs that were never seen online (no commState) are also
      (B) CODE-line mismatch [review]:
        ! updates: list[Any],
        ! for update_idx, raw_update in enumerate(updates):
        ! if not isinstance(raw_update, dict):
        ! merged[target_idx] = merge_live_properties(merged[target_idx], update)
        ! for pack in merged:
        ! if comm_state == "1" or PACK_FIELD_LAST_SEEN_AT not in pack:
        ! return merged
   [_merge_battery_pack_ota_lists] (richest: pre_transfer)  DOC=1 CODE=3
      (A) doc/comment-only:
        + current: Any,  # noqa: ANN401, RUF100
      (B) CODE-line mismatch [review]:
        ! ota_updates: list[Any],
        ! if not isinstance(raw_update, dict):
        ! return merged
   [_merge_main_properties] (richest: pre_transfer)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """Sanitize, merge, and normalize main-device property payloads.
        + Uses merge_live_properties so sparse MQTT/BLE push frames never blank
        + populated values that the HTTP poll already delivered (ingest.py §rule 1).
      (B) CODE-line mismatch [review]:
        ! merged = merge_live_properties(
   [_merge_pack_ota] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! for key in (FIELD_TARGET_VERSION, FIELD_UPDATE_STATUS, FIELD_UPDATE_CONTENT):
   [_merge_smart_plug_lists] (richest: 1\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + current: Any,
   [_merge_subdevice_data] (richest: pre_transfer)  DOC=6 CODE=5
      (A) doc/comment-only:
        + f"{device_id}_battery_pack_{pack_index}",
        + # Shelly Pro 3EM wraps volt/curr/freq/fact/ap/rep inside a nested
        + # AccCTBody dict. The outer dict already matched CT_METER_KEYS
        + # (e.g. aPhasePw), so DFS stopped there and never descended.
        + # Merge AccCTBody keys up so sensors that read volt/curr/... find them.
        + @classmethod
      (B) CODE-line mismatch [review]:
        ! def _merge_subdevice_data(  # noqa: PLR0912, PLR0914
        ! acc_ct = ct.get(FIELD_ACC_CT_BODY)
        ! if isinstance(acc_ct, dict):
        ! ct = {**ct, **acc_ct}
        ! updated[PAYLOAD_CT_METER] = merge_live_properties(current_ct, ct)
   [_merge_subdevice_list_by_identity] (richest: custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! current: Any,  # loose prior-state list, duck-typed via `current or []`
   [_merge_subdevice_lists_by_sn] (richest: pre_transfer)  DOC=2 CODE=3
      (A) doc/comment-only:
        + current: Any,  # noqa: ANN401, RUF100
        + continue
      (B) CODE-line mismatch [review]:
        ! updates: list[Any],
        ! if not isinstance(raw_update, dict):
        ! merged[target_idx] = merge_live_properties(merged[target_idx], update)
   [_normalize_battery_pack_payload] (richest: pre_transfer)  DOC=2 CODE=3
      (A) doc/comment-only:
        + cls,
        + item: Any,  # noqa: ANN401, RUF100
      (B) CODE-line mismatch [review]:
        ! def _normalize_battery_pack_payload(
        ! ) -> dict[str, Any]:
        ! normalized = merge_live_properties(normalized, nested)
   [_parse_statistics_backfill_date] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _parse_statistics_backfill_date(value: Any) -> date | None:
   [_pause_mqtt_after_auth_failure] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! self.rejection_metrics.increment("mqtt_broker_rejections", str(message))
   [_push_partial_update] (richest: pre_transfer)  DOC=7 CODE=11
      (A) doc/comment-only:
        + """Push updated coordinator data through HA's coordinator mechanism.
        + rather than replacing it. This prevents concurrent MQTT/BLE/HTTP writers
        + snapshot before writer B fired, B's fields survive because the merge
        + here runs against the live state (not against A's stale read).
        + Device-level entries absent from ``new_data`` are preserved unchanged.
        + Per-field merge uses ``merge_live_properties`` so neither transport can
      (B) CODE-line mismatch [review]:
        ! Merges ``new_data`` against the *current* ``self.data`` at push time
        ! from silently discarding each other's changes: if writer A read the
        ! blank a populated field with a None/empty value.
        ! if not self.data:
        ! merged = dict(self.data)
        ! for device_id, incoming in new_data.items():
        ! current = merged.get(device_id)
        ! if isinstance(current, dict) and isinstance(incoming, dict):
   [_raise_config_entry_auth_failed] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! raise ConfigEntryAuthFailed(f"{message}. Re-authentication is required.") from err  # noqa: TRY003
   [_resolve_device_id_from_payload] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! for key in (FIELD_DEVICE_ID, "device_id", FIELD_ID):
        ! props = payload.get(PAYLOAD_PROPERTIES)
        ! for key in (FIELD_DEVICE_ID, "device_id"):
   [_sanitize_main_properties] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + and key not in cls._THIRD_PARTY_MQTT_CONFIG_KEYS
   [_schedule_battery_pack_ota_enrichment] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! self._battery_pack_ota_tasks[device_id] = self.hass.async_create_task(
   [_schedule_mqtt_backfill_queries] (richest: 1\coordinator.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! if self._mqtt is None or not self._mqtt.is_connected:
        ! self._mqtt_backfill_task = self.hass.async_create_task(
   [_schedule_statistics_import] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=2 CODE=12
      (A) doc/comment-only:
        + SLOW_METRICS_INTERVAL_SEC - since_last,
        + 3,
      (B) CODE-line mismatch [review]:
        ! diag = self._statistics_import_diagnostics
        ! diag["last_schedule_check_at"] = utc_now().isoformat()
        ! diag["last_snapshot_device_count"] = len(snapshot)
        ! diag["last_schedule_decision"] = "not_ready"
        ! diag["last_schedule_decision"] = "already_running"
        ! since_last = now_monotonic - self._last_stat_import_monotonic
        ! if since_last < SLOW_METRICS_INTERVAL_SEC:
        ! diag["last_schedule_decision"] = "throttled"
   [_shelly_cloud_api_device_id] (richest: custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if direct_id not in {None, ""} and not str(direct_id).isdecimal():
   [_shelly_cloud_device_ids] (richest: 01.06\custom_components\jackery_solarvault\coordinator.py)  DOC=3 CODE=4
      (A) doc/comment-only:
        + or scan_name.startswith("shelly")
        + ):
        + continue
      (B) CODE-line mismatch [review]:
        ! scan_name = str(candidate.get(FIELD_SCAN_NAME) or "").lower()
        ! if not (
        ! str(candidate.get(FIELD_IS_CLOUD)).lower() in {"1", "true"}
        ! dev_id = cls._subdevice_id(candidate)
   [_should_skip_fast_property_fetch] (richest: 01.06\custom_components\jackery_solarvault\coordinator.py)  DOC=8 CODE=9
      (A) doc/comment-only:
        + cadence) is the only call we may suppress when MQTT is delivering
        + state at < ``MQTT_LIVE_THRESHOLD_SEC`` cadence. Slow stat endpoints,
        + trends, day-cache rollover and the Recorder statistic imports stay
        + on their own slow cadence regardless of MQTT liveness — they are
        + gated by ``SLOW_METRICS_INTERVAL_SEC`` in their own TTL caches.
        + fetch is skipped; the rest of the refresh cycle continues so the
      (B) CODE-line mismatch [review]:
        ! slow path keeps producing fresh ``dateType=day`` payloads (and the
        ! Recorder backfill keeps running) even while MQTT is live.
        ! if self._mqtt is None:
        ! if not self._mqtt.is_connected:
        ! elapsed = self._mqtt.seconds_since_last_message
        ! if elapsed is None or elapsed > MQTT_LIVE_THRESHOLD_SEC:
        ! since_last_refresh = (
        ! time.monotonic() - self._last_http_refresh_completed_monotonic
   [_sink] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=36 CODE=18
      (A) doc/comment-only:
        + (observed 2026-05-16 17:41–17:44 production log).
        + * ``120`` battery-pack lifetime snapshots merge
        + ``inEgy``/``outEgy`` into pack data; unsupported ``120``
        + variants stay counted as unrouted diagnostics.
        + # Coordinator has not run a successful HTTP refresh for this
        + # device yet — typically because the Jackery cloud is down
      (B) CODE-line mismatch [review]:
        ! idx = self._device_index.get(device_id)
        ! if not idx:
        ! device_meta = idx.get(PAYLOAD_DEVICE_META) or {}
        ! system_meta = idx.get(PAYLOAD_SYSTEM_META) or {}
        ! current_device = {
        ! routed = False
        ! routed = True
        ! touched = True
   [_source_regions] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! raw = first_nonblank(source.get(FIELD_SYSTEM_REGION), source.get(FIELD_COUNTRY))
        ! if raw is None:
        ! return [part.strip() for part in raw.split(",") if part.strip()]
   [_stable_payload_debug_signature] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=19 CODE=13
      (A) doc/comment-only:
        + """Return a content-only hash for payload-debug dedup.
        + Body and response_data are the only fields that can contain
        + All other fields are flat scalars, so they are joined directly
        + without a full recursive json.dumps on the outer list — this keeps
        + the function off the hot path on the main event loop.
        + match body:
      (B) CODE-line mismatch [review]:
        ! arbitrary nested dicts and need sort_keys for key-order stability.
        ! case dict() as b:
        ! body_sig: Any = {k: v for k, v in b.items() if k != "messageId"}
        ! case dict() as r:
        ! response_data: Any = r.get("data")
        ! response_data = None
        ! body_part = json.dumps(body_sig, sort_keys=True, default=str)
        ! response_part = (
   [_stat_row_start] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! def _stat_row_start(row: Any) -> float | None:  # noqa: ANN401, RUF100
        ! start = (
        ! row.get("start") if isinstance(row, dict) else getattr(row, "start", None)
   [_statistics_repair_from_date] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Return the persisted recovery start date for one device, if needed."""
      (B) CODE-line mismatch [review]:
        ! if last_success is None or last_success >= today:
   [_subdevice_dev_type] (richest: custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if raw_type not in {None, ""}:
   [_subdevice_identity_values] (richest: custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if value not in {None, ""}:
   [_third_party_mqtt_config_from_options] (richest: 01.06\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + FIELD_THIRD_PARTY_MQTT_TOKEN: str(
   [_trend_query_kwargs] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + APP_POLLING_MQTT.md requires explicit app ranges:
      (B) CODE-line mismatch [review]:
        ! def _trend_query_kwargs(date_type: str) -> dict[str, str]:
        ! return app_period_request_kwargs(date_type, today=dt_util.now().date())
   [_valid_price_sources] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + item.get(FIELD_COUNTRY), item.get(FIELD_SYSTEM_REGION)
      (B) CODE-line mismatch [review]:
        ! def _valid_price_sources(sources: Any) -> list[dict[str, Any]]:
        ! region = first_nonblank(
        ! if first_nonblank_int(company_id) is None or region is None:
   [app_chart_import_diagnostics] (richest: custom_components\jackery_solarvault\coordinator.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + # "hour" is not a documented app 2.1.1 REST dateType; this label
        + # only names the explicitly-disabled hour endpoint in diagnostics.
      (B) CODE-line mismatch [review]:
        ! hour_section = self._app_period_section(section_prefix, "hour")
   [as_dict] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! _STATISTICS_BACKFILL_STORE_VERSION = 1
        ! _STATISTICS_BACKFILL_STORE_KEY = "statistics_backfill"
   [async_cleanup_pending_device_removals] (richest: 1\1101\custom_components\jackery_solarvault\coordinator.py)  DOC=5 CODE=0
      (A) doc/comment-only:
        + # BUGFIXTODO Bug D: registry removal alone leaks per-device cache
        + # entries. Extract the parent device id from the battery-pack
        + # identifier and drop the matching ``_slow_cache`` /
        + # ``_last_*_query`` rows so the next refresh does not keep
        + # paying memory for a device that no longer exists.
   [async_delete_storm_alert] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! body_fields={"alertId": alert_id},
   [async_discover] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=6 CODE=8
      (A) doc/comment-only:
        + f"system/list failed: {_exception_debug_message(err)}"
        + ):
        + f"legacy bind/list failed: {_exception_debug_message(err)}"
        + ):
        + "cloud discovery returned no devices"
        + ):
      (B) CODE-line mismatch [review]:
        ! if await self._async_load_cached_discovery(
        ! self._discovery_source = "cloud_system_list"
        ! await self._async_save_discovery_cache()
        ! if await self._async_load_cached_discovery(
        ! raise UpdateFailed(f"legacy bind/list failed: {err}") from err
        ! self._discovery_source = "cloud_legacy_bind_list" if new_index else "none"
        ! await self._async_save_discovery_cache()
        ! if await self._async_load_cached_discovery(
   [async_get_time_zone] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Get time zone."""
      (B) CODE-line mismatch [review]:
        ! async def async_get_time_zone(self, device_id: str) -> None:
        ! await self._async_publish_command(
   [async_handle_local_mqtt_message] (richest: pre_transfer)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """Route LAN MQTT JSON into the shared cloud-MQTT payload merge path.
        + The local broker delivers the same UploadCombineData/device-property
        + shapes as the cloud push, so it reuses the cloud router rather than
        + duplicating the cmd-dispatch and merge logic.
      (B) CODE-line mismatch [review]:
        ! await self._async_handle_mqtt_message(topic, payload)
   [async_load_statistics_backfill_state] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Load persistent external-statistics repair state."""
   [async_query_battery_packs] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Query battery packs."""
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
        ! body_fields={FIELD_DEV_TYPE: 1},
   [async_query_device_ota_version] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Query device OTA version."""
      (B) CODE-line mismatch [review]:
        ! async def async_query_device_ota_version(self, device_id: str) -> None:
        ! await self._async_publish_command(
   [async_query_meter_heads] (richest: 1\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_query_smart_meter] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Query smart meter."""
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
        ! body_fields={FIELD_DEV_TYPE: 3},
   [async_query_smart_plugs] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + ``plugs`` array (see local implementation notes §2 and implementation notes §3).
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_query_subdevice_combo] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Query subdevice combo."""
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
        ! body_fields={FIELD_DEV_TYPE: 2},
   [async_query_third_party_mqtt_config] (richest: pre_transfer)  DOC=5 CODE=1
      (A) doc/comment-only:
        + """Read back the device's third-party MQTT bridge config (experimental).
        + _LOGGER.warning(
        + "Jackery: publishing experimental GET_THIRD_PARTY_MQTT_CONFIG "
        + "(3047) to %s — check payload_debug log for the response",
        + # MQTT-only — the query cmd is not ACKed over BLE (5 s timeout waste).
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_query_weather_plan] (richest: pre_transfer)  DOC=30 CODE=1
      (A) doc/comment-only:
        + # ------------------------------------------------------------------
        + # Experimental: third-party MQTT bridge (actionId 3046/3047)
        + # ------------------------------------------------------------------
        + # Per ``HomeCmdAction.smali``: ``SET_THIRD_PARTY_MQTT_CONFIG``
        + # (cmd=113 ``ThirdPartMQTTConfig``) and ``GET_THIRD_PARTY_MQTT_CONFIG``
        + # (cmd=114 ``QueryThirdPartMQTTConfig``). Body schema from
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_query_wifi_config] (richest: 01.06\custom_components\jackery_solarvault\coordinator.py)  DOC=4 CODE=0
      (A) doc/comment-only:
        + # HomeDeviceController.g1(...) sends ``userName``/``password``/``token``
        + # through ``Lbb/c;->d(String)`` before publishing. For SolarVault home
        + # devices the concrete codec is ``bb/e.d``: AES/CBC/PKCS7 with the decoded
        + # bluetoothKey as key+IV and Base64 ciphertext output.
   [async_query_wifi_list] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Query WiFi list."""
      (B) CODE-line mismatch [review]:
        ! async def async_query_wifi_list(self, device_id: str) -> None:
        ! await self._async_publish_command(
   [async_read_device_schedule] (richest: pre_transfer)  DOC=4 CODE=9
      (A) doc/comment-only:
        + """Read device schedule."""
        + "taskType": task_type,
        + "reqType": req_type,
        + "readType": read_type,
      (B) CODE-line mismatch [review]:
        ! req_type: int = 0,
        ! read_type: int = 0,
        ! smart_plug_sn: str | None = None,
        ! if smart_plug_sn:
        ! body[FIELD_DEVICE_SN] = smart_plug_sn
        ! await self._async_publish_command(
        ! message_type=MQTT_MESSAGE_DOWNLOAD_DEVICE_SCHEDULE,
        ! cmd=MQTT_CMD_DOWNLOAD_DEVICE_SCHEDULE,
   [async_send_ble_command] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=2 CODE=6
      (A) doc/comment-only:
        + connect_timeout_sec > 0
        + ):
      (B) CODE-line mismatch [review]:
        ! ack_timeout_sec: float = 5.0,
        ! connect_timeout_sec: float = 0.0,
        ! if (
        ! and not await self._ble_listener.async_ensure_connected(
        ! timeout_sec=connect_timeout_sec,
        ! return await self._ble_listener.async_send_command(
   [async_send_device_schedule] (richest: pre_transfer)  DOC=1 CODE=5
      (A) doc/comment-only:
        + """Send device schedule."""
      (B) CODE-line mismatch [review]:
        ! self, device_id: str, action_id: int, body: dict[str, Any] | str
        ! parsed_body: dict[str, Any] = (
        ! body if isinstance(body, dict) else json.loads(body)
        ! await self._async_publish_command(
        ! body_fields=parsed_body,
   [async_send_time_zone] (richest: pre_transfer)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Send time zone."""
      (B) CODE-line mismatch [review]:
        ! async def async_send_time_zone(self, device_id: str) -> None:
        ! await self._async_publish_command(
        ! body_fields={},
   [async_set_auto_standby_hours] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_ct_phase] (richest: pre_transfer)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Assign a CT (current transformer) sub-device to a phase (1..4).
      (B) CODE-line mismatch [review]:
        ! if phase_int not in {1, 2, 3, 4}:
        ! raise HomeAssistantError(f"CT phase must be 1..4 (got {phase_int})")  # noqa: TRY003
   [async_set_default_power] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_eps] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_follow_meter] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_max_feed_grid] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_max_output_power] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Set max output power."""
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
        ! message_type=MQTT_MESSAGE_CONTROL_COMBINE,
        ! cmd=MQTT_CMD_CONTROL_COMBINE,
   [async_set_off_grid_shutdown] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_off_grid_time] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_price_mode_dynamic] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=5 CODE=9
      (A) doc/comment-only:
        + device_id,
        + {
        + FIELD_DYNAMIC_OR_SINGLE: 1,
        + FIELD_PLATFORM_COMPANY_ID: company_id,
        + FIELD_SYSTEM_REGION: region,
      (B) CODE-line mismatch [review]:
        ! company_id = first_nonblank_int(current.get(FIELD_PLATFORM_COMPANY_ID))
        ! region = first_nonblank(current.get(FIELD_SYSTEM_REGION))
        ! if company_id is None or region is None:
        ! accepted = await self.api.async_set_dynamic_mode(
        ! platform_company_id=company_id,
        ! system_region=region,
        ! if not accepted:
        ! f"Jackery API rejected dynamic tariff update for {device_id}"
   [async_set_price_mode_single] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! price = safe_float(single_price)
        ! if price is None:
        ! f"Cannot switch to single tariff for {device_id}: invalid singlePrice"
        ! await self.async_set_single_price(device_id, price)
   [async_set_price_source] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=4 CODE=6
      (A) doc/comment-only:
        + FIELD_PLATFORM_COMPANY_ID: company_id,
        + FIELD_COMPANY_NAME: first_nonblank(
        + source.get(FIELD_COMPANY_NAME),
        + source.get(FIELD_NAME),
      (B) CODE-line mismatch [review]:
        ! company_id = first_nonblank_int(source.get(FIELD_PLATFORM_COMPANY_ID))
        ! if company_id is None or not region:
        ! accepted = await self.api.async_set_dynamic_mode(
        ! platform_company_id=company_id,
        ! if not accepted:
        ! f"Jackery API rejected dynamic tariff update for {device_id}"
   [async_set_single_price] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=9
      (A) doc/comment-only:
        + FIELD_SINGLE_PRICE: round(price, 4),
      (B) CODE-line mismatch [review]:
        ! price = safe_float(price_value)
        ! if price is None or price < 0:
        ! raise HomeAssistantError(
        ! f"Cannot set single tariff for {device_id}: invalid singlePrice"
        ! accepted = await self.api.async_set_single_mode(
        ! single_price=price,
        ! if not accepted:
        ! raise HomeAssistantError(
   [async_set_soc_limits] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=7 CODE=15
      (A) doc/comment-only:
        + _soc_limit(charge_limit)
        + else _current_soc_limit(FIELD_SOC_CHG_LIMIT, FIELD_SOC_CHARGE_LIMIT, 100)
        + _soc_limit(discharge_limit)
        + else _current_soc_limit(
        + FIELD_SOC_DISCHG_LIMIT,
        + FIELD_SOC_DISCHARGE_LIMIT,
      (B) CODE-line mismatch [review]:
        ! def _soc_limit(value: Any) -> int | None:
        ! parsed = safe_int(value)
        ! if parsed is None or parsed < 0 or parsed > 100:
        ! return None
        ! return parsed
        ! def _current_soc_limit(primary: str, legacy: str, default: int) -> int:
        ! for raw in (current.get(primary), current.get(legacy)):
        ! parsed = _soc_limit(raw)
   [async_set_standby] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_storm_minutes] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! value = _control_int(minutes, FIELD_MINS_INTERVAL)
   [async_set_temp_unit] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_set_third_party_mqtt_config] (richest: pre_transfer)  DOC=11 CODE=5
      (A) doc/comment-only:
        + """Configure the device's third-party MQTT bridge (experimental).
        + Publishes ``SET_THIRD_PARTY_MQTT_CONFIG`` (actionId 3046, cmd 113)
        + FIELD_THIRD_PARTY_MQTT_TOKEN: str(token),
        + _LOGGER.warning(
        + "Jackery: publishing experimental SET_THIRD_PARTY_MQTT_CONFIG "
        + "(3046) to %s — enable=%s ip=%s:%s user=%r (plaintext credentials)",
      (B) CODE-line mismatch [review]:
        ! async def async_set_third_party_mqtt_config(  # noqa: PLR0913
        ! with a plaintext body. See PROTOCOL.md §15 for the open questions.
        ! self._third_party_mqtt_plaintext.setdefault(device_id, {}).update(body)
        ! body = self._encode_third_party_mqtt_secrets(device_id, body)
        ! await self._async_publish_command(
   [async_set_work_model] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await self._async_publish_command(
   [async_shutdown] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=5
      (B) CODE-line mismatch [review]:
        ! self._mqtt_poll_task,
        ! self._mqtt_poll_task = None
        ! for unsubscribe in self._local_mqtt_unsubs:
        ! unsubscribe()
        ! self._local_mqtt_unsubs.clear()
   [async_start_ble_transport] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=35 CODE=18
      (A) doc/comment-only:
        + (observed 2026-05-16 17:41–17:44 production log).
        + * ``120`` battery-pack lifetime snapshots merge
        + ``inEgy``/``outEgy`` into pack data; unsupported ``120``
        + variants stay counted as unrouted diagnostics.
        + # Coordinator has not run a successful HTTP refresh for this
        + # device yet — typically because the Jackery cloud is down
      (B) CODE-line mismatch [review]:
        ! idx = self._device_index.get(device_id)
        ! if not idx:
        ! device_meta = idx.get(PAYLOAD_DEVICE_META) or {}
        ! system_meta = idx.get(PAYLOAD_SYSTEM_META) or {}
        ! current_device = {
        ! routed = False
        ! routed = True
        ! touched = True
   [async_start_mqtt] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! from .mqtt_push import JackeryMqttPushClient
        ! self._mqtt = JackeryMqttPushClient(
   [async_sync_mqtt_connect_info] (richest: pre_transfer)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Sync MQTT connect info."""
      (B) CODE-line mismatch [review]:
        ! async def async_sync_mqtt_connect_info(self, device_id: str) -> None:
        ! await self._async_publish_command(
        ! body_fields={},
   [async_update_third_party_mqtt_config] (richest: pre_transfer)  DOC=2 CODE=8
      (A) doc/comment-only:
        + """Update third-party MQTT config with arbitrary fields."""
        + # MQTT-only — see async_set_third_party_mqtt_config (BLE never ACKs this).
      (B) CODE-line mismatch [review]:
        ! self, device_id: str, body_fields: dict[str, Any]
        ! self._third_party_mqtt_plaintext.setdefault(device_id, {}).update(body_fields)
        ! body_fields = self._encode_third_party_mqtt_secrets(device_id, body_fields)
        ! await self._async_publish_command(
        ! message_type=MQTT_MESSAGE_THIRD_PARTY_MQTT_CONFIG,
        ! action_id=ACTION_ID_SET_THIRD_PARTY_MQTT_CONFIG,
        ! cmd=MQTT_CMD_THIRD_PARTY_MQTT_CONFIG,
        ! body_fields=body_fields,
   [ble_observations] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=21 CODE=18
      (A) doc/comment-only:
        + Used by diagnostics + the optional BLE-status sensor. Known devices
        + are included even before the first BLE advertisement so the status
        + surface can distinguish disabled, idle and running BLE transport.
        + "enabled": transport_enabled,
        + "write_enabled": write_enabled,
        + "running": listener is not None,
      (B) CODE-line mismatch [review]:
        ! transport_enabled = config_entry_bool_option(
        ! write_enabled = self._ble_writes_enabled()
        ! listener = self._ble_listener
        ! stats_by_device = listener.all_stats() if listener is not None else {}
        ! for device_id in sorted(set(self._device_index) | set(stats_by_device)):
        ! stats = stats_by_device.get(device_id)
        ! stats.advertisements_seen if stats is not None else 0
        ! stats.frames_decode_failed if stats is not None else 0
   [device_bluetooth_key] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! from .client.ble import BLE_AES_KEY_LENGTHS  # noqa: PLC0415
        ! import base64  # noqa: PLC0415
   [device_supports_third_party_mqtt] (richest: pre_transfer)  DOC=4 CODE=0
      (A) doc/comment-only:
        + # ------------------------------------------------------------------
        + # Property merging & payload helpers
        + # ------------------------------------------------------------------
        + @classmethod
   [mqtt_diagnostics] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\coordinator.py)  DOC=0 CODE=15
      (B) CODE-line mismatch [review]:
        ! if self._mqtt is None:
        ! return {"enabled": False}
        ! diag = dict(self._mqtt.diagnostics)
        ! diag["enabled"] = True
        ! diag["credential_mac_id_source"] = self.api.mqtt_mac_id_source
        ! diag["slow_metrics_interval_seconds"] = self._slow_metrics_interval_sec
        ! diag["price_interval_seconds"] = self._price_config_interval_sec
        ! diag["subdevice_query_interval_seconds"] = self._subdevice_query_interval_sec
   [mqtt_diagnostics_snapshot] (richest: 24.05\24.05\custom_components\jackery_solarvault\coordinator.py)  DOC=2 CODE=6
      (A) doc/comment-only:
        + "chain_hostname_enabled_x509_strict_disabled"
        + else "chain_hostname_enabled"
      (B) CODE-line mismatch [review]:
        ! diag["coordinator_polling_seconds"] = (
        ! safe_int(self._configured_update_interval.total_seconds()) or 0
        ! diag["tls_certificate_verification"] = (
        ! if diag.get("tls_x509_strict_disabled")
        ! pause_remaining = safe_int(self._mqtt_paused_until_monotonic - now_mono) or 0
        ! diag["app_conflict_pause_remaining_seconds"] = max(0, pause_remaining)
   [statistics_backfill_diagnostics] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! sorted(devices, key=str),
   [statistics_import_diagnostics] (richest: 21.05\21.05\custom_components\jackery_solarvault\coordinator.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + @staticmethod
   [third_party_mqtt_config_plaintext] (richest: pre_transfer)  DOC=5 CODE=5
      (A) doc/comment-only:
        + """Return the device's third-party MQTT config for the UI entities.
        + Merges the config the device reported over MQTT
        + values the user entered. The local mirror wins, because the device never
        + echoes the plaintext password/token back — so a just-entered secret is
        + not erased by a device report that omits it.
      (B) CODE-line mismatch [review]:
        ! (``PAYLOAD_THIRD_PARTY_MQTT_CONFIG``) with the local plaintext mirror of
        ! reported = ((self.data or {}).get(device_id, {}) or {}).get(
        ! merged: dict[str, Any] = dict(reported) if isinstance(reported, dict) else {}
        ! merged.update(self._third_party_mqtt_plaintext.get(device_id, {}))
        ! return merged

==========================================================================================
custom_components/jackery_solarvault/diagnostics.py
==========================================================================================
-- MISSING FUNCTIONS/CLASSES (3) --
   _filtered_payload_map  <- 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\diagnostics.py
   _jackery_device_ids  <- 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\diagnostics.py
   async_get_device_diagnostics  <- 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\diagnostics.py
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (1) --
   [async_get_config_entry_diagnostics] (richest: 1\diagnostics.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_get_config_entry_diagnostics(

==========================================================================================
custom_components/jackery_solarvault/discovery_cache.py
==========================================================================================
-- MISSING FUNCTIONS/CLASSES (1) --
   _entry_lock  <- pre_transfer
-- MISSING MODULE CONSTANTS (1) --
   _ENTRY_LOCKS  <- pre_transfer
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (1) --
   [_store] (richest: 1\discovery_cache.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return the HA Store used for cached discovery metadata."""

==========================================================================================
custom_components/jackery_solarvault/entity.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (5) --
   [JackeryEntity] (richest: 24.05\24.05\custom_components\jackery_solarvault\entity.py)  DOC=27 CODE=28
      (A) doc/comment-only:
        + """Return device-registry metadata for this entity."""
        + "SolarVault",
        + """Build a static device_info for a smart-plug subdevice.
        + Smart-plug subdevices receive a per-plug device row that hangs under
        + the SolarVault via the ``via_device`` link. The metadata is derived
        + from the plug payload captured at entity construction; later metadata
      (B) CODE-line mismatch [review]:
        ! def _alarm(self) -> Any:
        ! name = first_nonblank(
        ! self._system.get(FIELD_DEVICE_NAME),
        ! self._discovery.get(FIELD_DEVICE_NAME),
        ! self._properties.get(FIELD_WNAME),
        ! f"Jackery {self._device_id}",
        ! model = first_nonblank(
        ! self._discovery.get(FIELD_DEV_MODEL),
   [_alarm] (richest: 1\entity.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _alarm(self) -> Any:
   [_build_smart_plug_device_info] (richest: 21.05\21.05\custom_components\jackery_solarvault\entity.py)  DOC=17 CODE=11
      (A) doc/comment-only:
        + """Build a static device_info for a smart-plug subdevice.
        + Smart-plug subdevices receive a per-plug device row that hangs under
        + the SolarVault via the ``via_device`` link. The metadata is derived
        + from the plug payload captured at entity construction; later metadata
        + updates flow through Home Assistant's device-registry merge, so this
        + method is meant to be called once per entity (typically from
      (B) CODE-line mismatch [review]:
        ! self, plug_index: int, plug: dict[str, Any]
        ! base_name = first_nonblank(
        ! self._system.get(FIELD_DEVICE_NAME),
        ! self._discovery.get(FIELD_DEVICE_NAME),
        ! self._properties.get(FIELD_WNAME),
        ! sn = first_nonblank(
        ! display_name = first_nonblank(
        ! model = first_nonblank(
   [available] (richest: 24.05\24.05\custom_components\jackery_solarvault\entity.py)  DOC=8 CODE=5
      (A) doc/comment-only:
        + """Return whether the entity is currently available.
        + Cloud-derived ``onlineStatus`` / ``onlineState`` flags are only
        + advisory: during a Jackery cloud outage the device cannot heartbeat
        + back to ``/v1/device/system/list`` and the cloud returns ``0`` even
        + listener owns a live GATT session. Per the HA Bluetooth API
        + (https://developers.home-assistant.io/docs/core/bluetooth/api) a
      (B) CODE-line mismatch [review]:
        ! while the device is still broadcasting on BLE and the integration's
        ! authoritative source for "is this BLE device reachable right now",
        ! if self.coordinator.is_device_locally_reachable(self._device_id):
        ! if self.coordinator.has_recent_push_data(self._device_id):
        ! return True
   [device_info] (richest: 21.05\21.05\custom_components\jackery_solarvault\entity.py)  DOC=2 CODE=12
      (A) doc/comment-only:
        + """Return device-registry metadata for this entity."""
        + "SolarVault",
      (B) CODE-line mismatch [review]:
        ! name = first_nonblank(
        ! self._system.get(FIELD_DEVICE_NAME),
        ! self._discovery.get(FIELD_DEVICE_NAME),
        ! self._properties.get(FIELD_WNAME),
        ! f"Jackery {self._device_id}",
        ! model = first_nonblank(
        ! self._discovery.get(FIELD_DEV_MODEL),
        ! self._device_meta.get(FIELD_MODEL_NAME),

==========================================================================================
custom_components/jackery_solarvault/local_daily_cache.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (6) --
   [_isoformat_day] (richest: 1\local_daily_cache.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return ISO ``YYYY-MM-DD`` for the supplied local date."""
   [_store] (richest: 1\local_daily_cache.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return the HA Store backing the local-daily cache."""
   [async_save_daily_cache] (richest: 01.06\custom_components\jackery_solarvault\local_daily_cache.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! snapshots: dict[str, Any],
   [daily_delta] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + current_lifetime_wh: float | None,
      (B) CODE-line mismatch [review]:
        ! def daily_delta(  # noqa: PLR0911
   [is_new_day] (richest: 1\local_daily_cache.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return True when ``snapshot`` belongs to a different day than ``today``."""
   [snapshot_day] (richest: 1\local_daily_cache.py)  DOC=3 CODE=0
      (A) doc/comment-only:
        + """Return the ISO day stored in ``snapshot`` or None.
        + Used by diagnostics so the user can see when the midnight anchor was
        + last rotated without needing the raw Store JSON.

==========================================================================================
custom_components/jackery_solarvault/mqtt_session_cache.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (4) --
   [_store] (richest: 1\mqtt_session_cache.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return the HA Store backing the MQTT-session cache."""
   [async_clear_mqtt_session] (richest: pre_transfer)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """Drop the cached MQTT session for ``entry_id``.
        + Called after the broker explicitly rejects cached credentials so the next
        + setup pass forces a fresh login instead of replaying stale values.
      (B) CODE-line mismatch [review]:
        ! async with _CACHE_LOCK:
   [async_load_mqtt_session] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_load_mqtt_session(  # noqa: PLR0911
   [async_save_mqtt_session] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_save_mqtt_session(  # noqa: PLR0913

==========================================================================================
custom_components/jackery_solarvault/number.py
==========================================================================================
-- MISSING FUNCTIONS/CLASSES (1) --
   _set_max_power_experimental  <- 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\number.py, 1\jackery_solarvault-main\custom_components\jackery_solarvault\number.py
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (14) --
   [JackeryNumber] (richest: pre_transfer)  DOC=15 CODE=21
      (A) doc/comment-only:
        + else:
        + """The entity's unit of measurement, using a dynamic unit computed from the current payload when available
        + Returns:
        + """  # noqa: E501, RUF100
        + """Get the discrete native values allowed for this number entity.
        + Returns:
      (B) CODE-line mismatch [review]:
        ! if self.entity_description.source_section == PAYLOAD_THIRD_PARTY_MQTT_CONFIG:
        ! section = self.coordinator.third_party_mqtt_config_plaintext(
        ! self._device_id
        ! section = self._payload.get(self.entity_description.source_section) or {}
        ! return section if isinstance(section, dict) else {}
        ! parsed = safe_float(val)
        ! if parsed is not None:
        ! return parsed
   [JackeryNumberDescription] (richest: 01.06\custom_components\jackery_solarvault\number.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! value_transform: Callable[[float], Any] = lambda v: round(v)
        ! validate_range: bool = False
   [_add_new_entities] (richest: pre_transfer)  DOC=2 CODE=1
      (A) doc/comment-only:
        + """Rebuilds and adds number entities when the coordinator's entity signature changes.
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! Computes the coordinator's entity signature and, if it differs from the last-seen signature, collects new
   [_allowed_values] (richest: pre_transfer)  DOC=3 CODE=2
      (A) doc/comment-only:
        + """Get the discrete native values allowed for this number entity.
        + Returns:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! If the description's `allowed_values` is None, returns an empty tuple. If it is a callable, returns the tu
        ! tuple[float, ...]: Allowed native float values, or an empty tuple when no discrete constraint is defined.
   [_collect_entities] (richest: pre_transfer)  DOC=4 CODE=2
      (A) doc/comment-only:
        + """Collect JackeryNumber entities for devices whose payloads satisfy their gating predicates.
        + Returns:
        + """  # noqa: E501, RUF100
        + @callback
      (B) CODE-line mismatch [review]:
        ! Iterates coordinator data and instantiates a JackeryNumber for each entry in NUMBER_DESCRIPTIONS when the
        ! list[NumberEntity]: Instantiated number entities ready to be added to Home Assistant.
   [_has_price_or_system] (richest: 01.06\custom_components\jackery_solarvault\number.py)  DOC=3 CODE=0
      (A) doc/comment-only:
        + "default_power_set": lambda p: _has_props(
        + p, FIELD_DEFAULT_PW, FIELD_MAX_OUT_PW
        + "third_party_mqtt_port": lambda _p: True,
   [_max_feed_grid_allowed_values] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if _max_feed_grid_dynamic_max(payload) <= 800:  # noqa: PLR2004
   [_max_feed_grid_dynamic_max] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! if feed_limit is not None and feed_limit > 800:  # noqa: PLR2004
        ! return 800.0 if max_out_int <= 800 else 2500.0  # noqa: PLR2004
   [_section] (richest: 01.06\custom_components\jackery_solarvault\number.py)  DOC=1 CODE=5
      (A) doc/comment-only:
        + else:
      (B) CODE-line mismatch [review]:
        ! if self.entity_description.source_section == PAYLOAD_THIRD_PARTY_MQTT_CONFIG:
        ! section = self.coordinator.third_party_mqtt_config_plaintext(
        ! self._device_id
        ! section = self._payload.get(self.entity_description.source_section) or {}
        ! return section if isinstance(section, dict) else {}
   [async_set_native_value] (richest: pre_transfer)  DOC=8 CODE=6
      (A) doc/comment-only:
        + """Write the given native numeric value to the device, enforcing description-driven validation and invokin
        + Parameters:
        + value (float): The native numeric value to write.
        + Raises:
        + ConfigEntryAuthFailed: If the setter reports an authentication failure.
        + HomeAssistantError: For invalid range or allowed-value violations, or when `raise_on_setter_error` is True
      (B) CODE-line mismatch [review]:
        ! Validates the value against the description's min/max when `validate_range` is True and against discrete `
        ! if self.entity_description.validate_range and (
        ! value < self.native_min_value or value > self.native_max_value
        ! if allowed and round(value) not in {round(v) for v in allowed}:
        ! allowed_text = ", ".join(f"{int(v)}" for v in allowed)
        ! except Exception as err:  # noqa: BLE001
   [async_setup_entry] (richest: 01.06\custom_components\jackery_solarvault\number.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(  # noqa: RUF029  # HA awaits this entry point
   [native_max_value] (richest: 01.06\custom_components\jackery_solarvault\number.py)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! max_value = self.entity_description.native_max_value
        ! if max_value is None:
        ! return 0.0
        ! return float(max_value)
   [native_unit_of_measurement] (richest: pre_transfer)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """The entity's unit of measurement, using a dynamic unit computed from the current payload when available
        + Returns:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! The unit of measurement string, or None if no unit is configured.
   [native_value] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! parsed = safe_float(val)
        ! if parsed is not None:
        ! return parsed

==========================================================================================
custom_components/jackery_solarvault/repairs.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (4) --
   [AppDataInconsistencyRepairFlow] (richest: pre_transfer)  DOC=4 CODE=5
      (A) doc/comment-only:
        + "data_entry_flow.FlowResult",
        + _LOGGER.warning(
        + "Jackery repair flow: coordinator not found for entry %s; "
        + "repair action had no effect",
      (B) CODE-line mismatch [review]:
        ! return cast("data_entry_flow.FlowResult", self.async_create_entry(data={}))
        ! return cast(
        ! self.async_show_form(
        ! self._entry_id,
        ! except (HomeAssistantError, TimeoutError, OSError, JackeryError) as err:
   [_async_force_refresh] (richest: pre_transfer)  DOC=3 CODE=2
      (A) doc/comment-only:
        + _LOGGER.warning(
        + "Jackery repair flow: coordinator not found for entry %s; "
        + "repair action had no effect",
      (B) CODE-line mismatch [review]:
        ! self._entry_id,
        ! except (HomeAssistantError, TimeoutError, OSError, JackeryError) as err:
   [async_create_fix_flow] (richest: 1\repairs.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_create_fix_flow(
   [async_step_confirm] (richest: pre_transfer)  DOC=1 CODE=3
      (A) doc/comment-only:
        + "data_entry_flow.FlowResult",
      (B) CODE-line mismatch [review]:
        ! return cast("data_entry_flow.FlowResult", self.async_create_entry(data={}))
        ! return cast(
        ! self.async_show_form(

==========================================================================================
custom_components/jackery_solarvault/select.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (28) --
   [JackerySelect] (richest: 1\select.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _warn_unknown_once(self, value: Any) -> None:
   [_add_new_entities] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_collect_entities] (richest: pre_transfer)  DOC=2 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + @callback
   [_ct_phase_current] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! ct = entity._payload.get(PAYLOAD_CT_METER) or {}  # noqa: SLF001
   [_ct_phase_select] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! ct = entity._payload.get(PAYLOAD_CT_METER) or {}  # noqa: SLF001
        ! ct_sn = str(ct.get(FIELD_DEVICE_SN) or ct.get(FIELD_DEV_SN) or "").strip()
        ! await entity.coordinator.async_set_ct_phase(entity._device_id, ct_sn, phase)  # noqa: SLF001
   [_gate] (richest: 21.05\21.05\custom_components\jackery_solarvault\select.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + _price_sources_from_payload(payload)
        + ) or current_company not in (
      (B) CODE-line mismatch [review]:
        ! return bool(
   [_island_auto_off_current] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + entity._task_plan,  # noqa: SLF001
      (B) CODE-line mismatch [review]:
        ! raw = entity._properties.get(FIELD_OFF_GRID_TIME)  # noqa: SLF001
   [_island_auto_off_select] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await entity.coordinator.async_set_off_grid_time(entity._device_id, hours * 60)  # noqa: SLF001
   [_price_mode_current] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + entity._warn_unknown_once(mode)  # noqa: SLF001
   [_price_mode_current_int] (richest: pre_transfer)  DOC=3 CODE=4
      (A) doc/comment-only:
        + entity._task_plan,  # noqa: SLF001
        + FIELD_DYNAMIC_OR_SINGLE,
        + FIELD_PRICE_MODE,
      (B) CODE-line mismatch [review]:
        ! raw = entity._price.get(FIELD_DYNAMIC_OR_SINGLE)  # noqa: SLF001
        ! work_mode = safe_int(entity._properties.get(FIELD_WORK_MODEL))  # noqa: SLF001
        ! if work_mode == 7:  # noqa: PLR2004
        ! if entity._price.get(FIELD_SINGLE_PRICE) is not None:  # noqa: SLF001
   [_price_mode_dynamic_available] (richest: pre_transfer)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! company_id = entity._price.get(FIELD_PLATFORM_COMPANY_ID)  # noqa: SLF001
        ! region = entity._price.get(FIELD_SYSTEM_REGION)  # noqa: SLF001
        ! if company_id not in {None, ""} and bool(region):
        ! return bool(_price_sources_from_payload(entity._payload))  # noqa: SLF001
   [_price_mode_select] (richest: pre_transfer)  DOC=1 CODE=3
      (A) doc/comment-only:
        + else:
      (B) CODE-line mismatch [review]:
        ! await entity.coordinator.async_set_price_mode_dynamic(entity._device_id)  # noqa: SLF001
        ! elif mode == 2:  # noqa: PLR2004
        ! await entity.coordinator.async_set_price_mode_single(entity._device_id)  # noqa: SLF001
   [_price_provider_current] (richest: 21.05\21.05\custom_components\jackery_solarvault\select.py)  DOC=4 CODE=5
      (A) doc/comment-only:
        + source, normalized_company_id, normalized_region
        + ):
        + FIELD_PLATFORM_COMPANY_ID: normalized_company_id,
        + FIELD_COUNTRY: normalized_region,
      (B) CODE-line mismatch [review]:
        ! company_id = first_nonblank_int(entity._price.get(FIELD_PLATFORM_COMPANY_ID))
        ! if company_id is None:
        ! normalized_company_id = str(company_id)
        ! normalized_region = None if region is None else str(region).strip()
        ! if _price_source_matches_current(
   [_price_provider_options] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! for source in _price_sources_from_payload(entity._payload)  # noqa: SLF001
   [_price_provider_select] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! for source in _price_sources_from_payload(entity._payload):  # noqa: SLF001
        ! await entity.coordinator.async_set_price_source(entity._device_id, source)  # noqa: SLF001
   [_price_source_label] (richest: 21.05\21.05\custom_components\jackery_solarvault\select.py)  DOC=6 CODE=5
      (A) doc/comment-only:
        + first_nonblank(
        + source.get(FIELD_COMPANY_NAME),
        + source.get(FIELD_NAME),
        + source.get(FIELD_CID),
        + source.get(FIELD_PLATFORM_COMPANY_ID),
        + "Provider",
      (B) CODE-line mismatch [review]:
        ! name = (
        ! country = first_nonblank(source.get(FIELD_COUNTRY), source.get(FIELD_SYSTEM_REGION))
        ! company_id = first_nonblank_int(source.get(FIELD_PLATFORM_COMPANY_ID))
        ! label = f"{name} ({country})" if country is not None else name
        ! if company_id is not None:
   [_price_source_regions] (richest: 21.05\21.05\custom_components\jackery_solarvault\select.py)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! raw = first_nonblank(source.get(FIELD_COUNTRY), source.get(FIELD_SYSTEM_REGION))
        ! if raw is None:
        ! return [part.strip() for part in raw.split(",") if part.strip()]
   [_price_sources_from_payload] (richest: 21.05\21.05\custom_components\jackery_solarvault\select.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + item
        + ):
      (B) CODE-line mismatch [review]:
        ! if first_nonblank_int(company_id) is not None and _price_source_regions(
   [_storm_minutes_current_value] (richest: pre_transfer)  DOC=6 CODE=0
      (A) doc/comment-only:
        + entity._properties,  # noqa: SLF001
        + entity._weather_plan,  # noqa: SLF001
        + entity._task_plan,  # noqa: SLF001
        + entity._properties,  # noqa: SLF001
        + entity._weather_plan,  # noqa: SLF001
        + entity._task_plan,  # noqa: SLF001
   [_storm_minutes_select] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await entity.coordinator.async_set_storm_minutes(entity._device_id, minutes)  # noqa: SLF001
   [_storm_minutes_value] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _storm_minutes_value(  # noqa: PLR0912
   [_temp_unit_current] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! val = safe_int(entity._properties.get(FIELD_TEMP_UNIT))  # noqa: SLF001
   [_temp_unit_select] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + entity._device_id,  # noqa: SLF001
      (B) CODE-line mismatch [review]:
        ! _OPTION_TO_TEMP_UNIT[option],
   [_warn_unknown_once] (richest: 1\select.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _warn_unknown_once(self, value: Any) -> None:
   [_work_mode_current] (richest: pre_transfer)  DOC=1 CODE=3
      (A) doc/comment-only:
        + entity._warn_unknown_once(value)  # noqa: SLF001
      (B) CODE-line mismatch [review]:
        ! raw = entity._properties.get(FIELD_WORK_MODEL)  # noqa: SLF001
        ! raw = task_plan_value(entity._task_plan, FIELD_WORK_MODEL)  # noqa: SLF001
        ! mode_hint = safe_int(entity._price.get(FIELD_DYNAMIC_OR_SINGLE))  # noqa: SLF001
   [_work_mode_select] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! await entity.coordinator.async_set_work_model(entity._device_id, mode)  # noqa: SLF001
   [async_select_option] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! except Exception as err:  # noqa: BLE001
   [async_setup_entry] (richest: 1\select.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(

==========================================================================================
custom_components/jackery_solarvault/sensor.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (54) --
   [JackeryBatteryPackSensor] (richest: 21.05\21.05\custom_components\jackery_solarvault\sensor.py)  DOC=14 CODE=9
      (A) doc/comment-only:
        + """Initialise the entity from the coordinator and description."""
        + """Return the current value from a BatteryPackSub payload."""
        + """Return diagnostic attributes for a BatteryPackSub payload."""
        + """Recompute state and attributes once per coordinator update."""
        + """Return the entity's current value."""
        + """Return device-registry metadata for this entity."""
      (B) CODE-line mismatch [review]:
        ! def _value_from_pack(self, pack: dict[str, Any]) -> Any:
        ! def native_value(self) -> Any:
        ! base_name = first_nonblank(
        ! self._system.get(FIELD_DEVICE_NAME),
        ! self._discovery.get(FIELD_DEVICE_NAME),
        ! self._properties.get(FIELD_WNAME),
        ! sn = first_nonblank(
        ! model = first_nonblank(
   [JackeryBleTransportSensor] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=5 CODE=1
      (A) doc/comment-only:
        + """Initialise the entity from the coordinator."""
        + """Return the number of frames successfully decoded since setup."""
        + """Surface counters + last-frame metadata for the BLE listener."""
        + attrs.pop("unrouted_frames_by_cmd", None)
        + attrs.pop("routed_frames_by_cmd", None)
      (B) CODE-line mismatch [review]:
        ! return safe_int(self._observation().get("frames_decoded")) or 0
   [JackeryConversionLossPowerSensor] (richest: 1\sensor.py)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! produced = (
        ! c["pv_power"] + c["battery_discharge_power"] + c["grid_side_input_power"]
        ! consumed = c["battery_charge_power"] + c["grid_side_output_power"]
   [JackeryGridNetPowerSensor] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + "formula": "inOngridPw/gridInPw/inGridSidePw - outOngridPw/gridOutPw/outGridSidePw",  # noqa: E501, RUF100
   [JackeryHomeConsumptionPowerSensor] (richest: pre_transfer)  DOC=2 CODE=2
      (A) doc/comment-only:
        + "source": "otherLoadPw_preferred_then_smart_meter_ct_plus_jackery_ac_grid_side_fields",  # noqa: E501, RUF
        + "scope": "Jackery-corrected home load; external non-Jackery generation must be measured separately",  # no
      (B) CODE-line mismatch [review]:
        ! meter_net = JackerySmartMeterSensor._net_power(ct)  # noqa: SLF001
        ! phases = JackerySmartMeterSensor._signed_phase_values(ct)  # noqa: SLF001
   [JackeryMeterHeadSensor] (richest: 21.05\21.05\custom_components\jackery_solarvault\sensor.py)  DOC=16 CODE=20
      (A) doc/comment-only:
        + """Initialise the entity from the coordinator and description."""
        + f"meter_head_{meter_head_index}_{description.key}",
        + """Return the entity's current value."""
        + """Return device-registry metadata for this meter head."""
        + "SolarVault",
        + meter_head.get(FIELD_DEVICE_SN),
      (B) CODE-line mismatch [review]:
        ! meter_heads = self._payload.get(PAYLOAD_METER_HEADS) or []
        ! if not isinstance(meter_heads, list):
        ! try:
        ! meter_head = meter_heads[self._meter_head_index - 1]
        ! except IndexError:
        ! return meter_head if isinstance(meter_head, dict) else {}
        ! def native_value(self) -> Any:
        ! base_name = first_nonblank(
   [JackeryRawPropertiesSensor] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=3 CODE=7
      (A) doc/comment-only:
        + """Diagnostic: full properties JSON as state attributes."""
        + """Return diagnostic attributes for the current state."""
        + json.dumps(v)
      (B) CODE-line mismatch [review]:
        ! attrs: dict[str, Any] = {}
        ! for k, v in self._properties.items():
        ! try:
        ! attrs[k] = v
        ! except TypeError, ValueError:
        ! attrs[k] = str(v)
        ! return attrs
   [JackerySavingsDetailSensor] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! def native_value(self) -> Any:  # dynamic sensor state value  # noqa: ANN401, RUF100
        ! return self.entity_description.transform(raw)
   [JackerySensor] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def native_value(self) -> Any:
   [JackerySmartMeterSensor] (richest: 21.05\21.05\custom_components\jackery_solarvault\sensor.py)  DOC=5 CODE=10
      (A) doc/comment-only:
        + """Build diagnostic attributes from a CT payload."""
        + """Return device-registry metadata for this entity."""
        + "SolarVault",
        + first_nonblank(ct.get(FIELD_SCAN_NAME), "Smart Meter") or "Smart Meter"
        + ct.get(FIELD_DEVICE_SN), ct.get(FIELD_SN), ct.get(FIELD_MAC)
      (B) CODE-line mismatch [review]:
        ! def _value_from_ct(self, ct: dict[str, Any]) -> Any:
        ! def native_value(self) -> Any:
        ! base_name = first_nonblank(
        ! self._system.get(FIELD_DEVICE_NAME),
        ! self._discovery.get(FIELD_DEVICE_NAME),
        ! self._properties.get(FIELD_WNAME),
        ! scan_name = (
        ! manufacturer = "Shelly" if "shelly" in scan_name.lower() else MANUFACTURER
   [JackerySmartPlugSensor] (richest: 1\sensor.py)  DOC=5 CODE=2
      (A) doc/comment-only:
        + """Initialise the entity from coordinator, sorted index/serial and description."""
        + f"smart_plug_{plug_index}_{description.key}",
        + """Return the entity's current value."""
        + """Return the app period boundary for daily smart-plug totals."""
        + """Return diagnostic attributes for the current smart plug."""
      (B) CODE-line mismatch [review]:
        ! plug_index, self._plug
        ! def native_value(self) -> Any:
   [JackerySmartPlugSensorDescription] (richest: 21.05\21.05\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + implementation notes §2 "Smart-Plug-/Socket-Appmodell".
   [JackeryStatSensor] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=46 CODE=63
      (A) doc/comment-only:
        + """Initialise the entity from the coordinator and description."""
        + """Local reset boundary for app period-total statistics.
        + Anchored to the request metadata of the data we actually have,
        + NOT to the wall clock. Background:
        + After 0:00 the wall clock immediately points at the new day,
        + but the Jackery cloud still serves yesterday's period values
      (B) CODE-line mismatch [review]:
        ! Recorder sees ``value=4.77 kWh, last_reset=today 00:00`` and
        ! By anchoring last_reset to the begin_date that travelled with
        ! def _local_timezone(self) -> Any:
        ! which happens for cached / non-period sources and during the
        ! if either side is missing, we treat the data as fresh and
        ! if section == PAYLOAD_LOCAL_DAILY_ENERGY:
        ! source = self._payload.get(PAYLOAD_LOCAL_DAILY_ENERGY) or {}
        ! def _non_negative_period_raw(self, raw: Any) -> Any:
   [JackerySystemMetaSensor] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def native_value(self) -> Any:
   [JackeryTaskPlanSensor] (richest: 1\sensor.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! return dict(plan)
        ! return {}
   [JackeryTimestampSensor] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=6
      (A) doc/comment-only:
        + """Return the entity's current value."""
      (B) CODE-line mismatch [review]:
        ! raw_ts_ms = self._device_meta.get(self._source_key)
        ! if not raw_ts_ms:
        ! ts_ms = safe_int(raw_ts_ms)
        ! if ts_ms is None:
        ! return datetime.fromtimestamp(ts_ms / 1000, tz=UTC)
        ! except OSError, OverflowError, ValueError:
   [JackeryWeatherPlanSensor] (richest: 1\sensor.py)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! plan = self._weather_plan
        ! if isinstance(plan, dict):
        ! return dict(plan)
        ! return {}
   [_add_new_entities] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_attrs_from_ct] (richest: 1\jackery_solarvault-main\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Build diagnostic attributes from a CT payload."""
      (B) CODE-line mismatch [review]:
        ! attrs["calculation"] = self.entity_description.calculation
        ! if self.entity_description.calculation in {"net_import", "net_export"}
   [_attrs_from_pack] (richest: 1\sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return diagnostic attributes for a BatteryPackSub payload."""
   [_calculated_power] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + @property
   [_collect_entities] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=4 CODE=7
      (A) doc/comment-only:
        + # Firmware version from implementation notes §2 /v1/device/ota/list
        + pack_desc.entity_registry_enabled_default
        + and pack_desc.entity_category
        + meter_head
      (B) CODE-line mismatch [review]:
        ! enabled_default=(
        ! != EntityCategory.DIAGNOSTIC
        ! meter_heads = payload.get(PAYLOAD_METER_HEADS) or []
        ! if isinstance(meter_heads, list):
        ! valid_meter_heads = [
        ! for meter_head in meter_heads
        ! if isinstance(meter_head, dict)
   [_current_day_bucket_from_period_chart] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Use today's week/month chart bucket when the day endpoint is empty."""
      (B) CODE-line mismatch [review]:
        ! today = dt_util.now().date()
   [_div] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! def _f(value: Any) -> float | None:
        ! parsed = safe_float(value)
        ! if parsed is None:
        ! return round(parsed / divisor, 2)
   [_f] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=0 CODE=4
      (B) CODE-line mismatch [review]:
        ! def _f(value: Any) -> float | None:
        ! parsed = safe_float(value)
        ! if parsed is None:
        ! return round(parsed / divisor, 2)
   [_getter] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _getter(props: dict[str, Any]) -> Any:
   [_identity] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _identity(value: Any) -> Any:
   [_is_period_data_future] (richest: 1\sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Detect whether the source data belongs to a future local period."""
   [_is_period_data_stale] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=5 CODE=2
      (A) doc/comment-only:
        + """Detect whether the source data is from a previous period.
        + Returns True when the wall-clock period (computed via
        + ``_period_start``) is strictly newer than the period stamped
        + on the source's request metadata. The boundary is conservative:
        + publish normally.
      (B) CODE-line mismatch [review]:
        ! if either side is missing, we treat the data as fresh and
        ! wall_clock_start = _period_start(self._reset_period)
   [_local_timezone] (richest: 1\sensor.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Return the Home Assistant local timezone for period sensors."""
      (B) CODE-line mismatch [review]:
        ! def _local_timezone(self) -> Any:
   [_local_today] (richest: 1\sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return Home Assistant local date for app chart lookups."""
   [_meter_head] (richest: 1\1101\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=6
      (A) doc/comment-only:
        + """Return the meter-head entry corresponding to this entity's configured index.
      (B) CODE-line mismatch [review]:
        ! meter_heads = self._payload.get(PAYLOAD_METER_HEADS) or []
        ! if not isinstance(meter_heads, list):
        ! try:
        ! meter_head = meter_heads[self._meter_head_index - 1]
        ! except IndexError:
        ! return meter_head if isinstance(meter_head, dict) else {}
   [_non_negative_period_raw] (richest: pre_transfer)  DOC=8 CODE=4
      (A) doc/comment-only:
        + self,
        + raw: Any,  # noqa: ANN401, RUF100
        + """Guard against negative energy period totals.
        + parses `raw` as a float and returns `None` when the parsed value is less than zero.
        + false-zero spike in the HA Energy Dashboard (clamping to 0 causes a negative
        + delta on the next positive reading).
      (B) CODE-line mismatch [review]:
        ! def _non_negative_period_raw(
        ! ) -> Any:  # passes through dynamic state value  # noqa: ANN401, RUF100
        ! Returning `None` renders the sensor unavailable for that update, which avoids a
        ! return None
   [_observation] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_pack] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + @property
   [_path] (richest: custom_components\jackery_solarvault\sensor.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! def _path(
        ! props: dict[str, Any], *keys: str
   [_payload_http_prop] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _getter(payload: dict[str, Any]) -> Any:
   [_period_begin_from_meta] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=4 CODE=2
      (A) doc/comment-only:
        + """Read the begin_date stamped on this sensor's source by the API.
        + Returns ``None`` when the source has no request metadata yet —
        + very first coordinator update before the period endpoint has
        + been polled.
      (B) CODE-line mismatch [review]:
        ! which happens for cached / non-period sources and during the
        ! section = self.entity_description.section
   [_period_start] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=1 CODE=10
      (A) doc/comment-only:
        + """Return the local start timestamp of the current statistic period."""
      (B) CODE-line mismatch [review]:
        ! def _period_start(reset_period: StatResetPeriod) -> datetime:
        ! now = dt_util.now()
        ! if reset_period == DATE_TYPE_DAY:
        ! return now.replace(hour=0, minute=0, second=0, microsecond=0)
        ! if reset_period == DATE_TYPE_WEEK:
        ! start = now - timedelta(days=now.weekday())
        ! return start.replace(hour=0, minute=0, second=0, microsecond=0)
        ! if reset_period == DATE_TYPE_MONTH:
   [_plug] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_prop_any] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _getter(props: dict[str, Any]) -> Any:
   [_pv_channel_power] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _getter(props: dict[str, Any]) -> Any:
   [_refresh_cache] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=19 CODE=53
      (A) doc/comment-only:
        + # implementation notes §2 fallback — try documented alternate
        + fb_source,
        + fb_section,
        + fb_stat_key,
        + raw is None
        + f"{APP_SECTION_HOME_TRENDS}_"
      (B) CODE-line mismatch [review]:
        ! series_key = trend_series_key(section, stat_key)
        ! used_derived_home_fallback = False
        ! fb_total = trend_series_total(
        ! series_key = trend_series_key(section, stat_key)
        ! if (
        ! and self._derived_home_energy_fallback_enabled()
        ! and self.entity_description.stat_key == APP_STAT_TOTAL_HOME_ENERGY
        ! and self.entity_description.section.startswith(
   [_resolve_period_value] (richest: 21.05\21.05\custom_components\jackery_solarvault\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! Replaces the previous triple call (``trend_series_total`` ->
   [_signed_diff] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _signed_diff(merged_value: Any, http_value: Any) -> int | None:
   [_source_for_section] (richest: 24.05\24.05\custom_components\jackery_solarvault\sensor.py)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! if section == PAYLOAD_LOCAL_DAILY_ENERGY:
        ! source = self._payload.get(PAYLOAD_LOCAL_DAILY_ENERGY) or {}
   [_stat_description_has_value] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _stat_description_has_value(  # noqa: PLR0911
   [_temp_unit_label] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _temp_unit_label(value: Any) -> str | None:
   [_value_from_ct] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _value_from_ct(self, ct: dict[str, Any]) -> Any:
   [_value_from_pack] (richest: pre_transfer)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! def _value_from_pack(
        ! self, pack: dict[str, Any]
        ! ) -> Any:  # dynamic sensor state value  # noqa: ANN401, RUF100
   [async_setup_entry] (richest: 1\sensor.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(
   [device_info] (richest: 21.05\21.05\custom_components\jackery_solarvault\sensor.py)  DOC=8 CODE=7
      (A) doc/comment-only:
        + """Return device-registry metadata for this entity."""
        + "SolarVault",
        + pack.get(FIELD_DEVICE_SN), pack.get(FIELD_DEV_SN), pack.get(FIELD_SN)
        + pack.get(FIELD_MODEL),
        + pack.get(FIELD_MODEL_NAME),
        + pack.get(FIELD_TYPE_NAME),
      (B) CODE-line mismatch [review]:
        ! base_name = first_nonblank(
        ! self._system.get(FIELD_DEVICE_NAME),
        ! self._discovery.get(FIELD_DEVICE_NAME),
        ! self._properties.get(FIELD_WNAME),
        ! sn = first_nonblank(
        ! model = first_nonblank(
        ! version = first_nonblank(
   [last_reset] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\sensor.py)  DOC=12 CODE=6
      (A) doc/comment-only:
        + """Local reset boundary for app period-total statistics.
        + Anchored to the request metadata of the data we actually have,
        + NOT to the wall clock. Background:
        + After 0:00 the wall clock immediately points at the new day,
        + but the Jackery cloud still serves yesterday's period values
        + until the next refresh tick. With wall-clock anchoring HA
      (B) CODE-line mismatch [review]:
        ! Recorder sees ``value=4.77 kWh, last_reset=today 00:00`` and
        ! By anchoring last_reset to the begin_date that travelled with
        ! return _period_start(self._reset_period)
        ! return _period_start(self._reset_period)
        ! return _period_start(self._reset_period)
        ! tzinfo=dt_util.now().tzinfo,
   [native_value] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! def native_value(self) -> Any:  # dynamic sensor state value  # noqa: ANN401, RUF100
        ! return self.entity_description.transform(raw)

==========================================================================================
custom_components/jackery_solarvault/services.py
==========================================================================================
-- MISSING FUNCTIONS/CLASSES (1) --
   _handle_send_device_schedule  <- 01.06\custom_components\jackery_solarvault\services.py, 02.06\custom_components\jackery_solarvault\services.py, 1\1101\custom_components\jackery_solarvault\services.py, 1\1249\custom_components\jackery_solarvault\services.py, jackery_solarvault-Bigdaddy1990-patch-1\custom_components\jackery_solarvault\services.py, pre_transfer
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (11) --
   [_async_handle_rename] (richest: pre_transfer)  DOC=8 CODE=16
      (A) doc/comment-only:
        + call,
        + SERVICE_FIELD_SYSTEM_ID,
        + else:
        + elif len(new_name) > 64:  # noqa: PLR2004
        + else:
        + "error": name_error,
      (B) CODE-line mismatch [review]:
        ! system_id = _text_field(
        ! placeholder_key="system_id",
        ! numeric=True,
        ! raw_name = call.data.get(SERVICE_FIELD_NEW_NAME)
        ! new_name = ""
        ! if not isinstance(raw_name, str):
        ! name_error = "new_name must be text"
        ! new_name = raw_name.strip()
   [_ble_body_from_service] (richest: pre_transfer)  DOC=7 CODE=7
      (A) doc/comment-only:
        + """Parse a BLE command `body` value from a service call into a dict.
        + Parameters:
        + raw_body (Any): Mapping or JSON string encoding an object to use as the BLE body.
        + device_id (str): Jackery device identifier included in validation error placeholders.
        + Returns:
        + Raises:
      (B) CODE-line mismatch [review]:
        ! def _ble_body_from_service(raw_body: object, device_id: str) -> dict[str, Any]:
        ! Accepts a mapping (returned as a shallow copy) or a JSON-encoded object string. If `raw_body` is a string
        ! dict[str, Any]: Parsed body mapping to send with the BLE command.
        ! ServiceValidationError: If `raw_body` is neither a mapping nor a JSON object string, or if JSON parsing fa
        ! return dict(raw_body)
        ! parsed = json.loads(raw_body.strip())
        ! return parsed
   [_coerce_service_float] (richest: 21.05\21.05\custom_components\jackery_solarvault\services.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! except (TypeError, ValueError) as err:
   [_handle_query_third_party_mqtt] (richest: 01.06\custom_components\jackery_solarvault\services.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Forward a "query third-party MQTT config" service call to the integration's async handler."""
   [_handle_send_ble_command] (richest: pre_transfer)  DOC=8 CODE=3
      (A) doc/comment-only:
        + """Forward a Home Assistant send-BLE-command service call to the integration's internal handler.
        + Parameters:
        + call (ServiceCall): Service call whose data must include the send-BLE-command fields:
        + - `device_id`: target device identifier (string)
        + - `cmd`: command id (int)
        + - `body`: command payload (dict or JSON string)
      (B) CODE-line mismatch [review]:
        ! - `flags` (optional): flags for the command (int)
        ! - `wait_for_ack` (optional): whether to wait for an acknowledgement (bool)
        ! if not hass.services.has_service(DOMAIN, SERVICE_SEND_DEVICE_SCHEDULE):
   [_handle_set_third_party_mqtt] (richest: pre_transfer)  DOC=3 CODE=0
      (A) doc/comment-only:
        + """Dispatches the Home Assistant service call to the integration handler that applies a device's third-par
        + The `call` data must conform to `SET_THIRD_PARTY_MQTT_SCHEMA`.
        + """  # noqa: E501, RUF100
   [_loaded_coordinators] (richest: pre_transfer)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """Collect active JackerySolarVaultCoordinator instances from loaded config entries.
        + Returns:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! list[JackerySolarVaultCoordinator]: Active coordinators for loaded Jackery config entries.
   [_resolve_jackery_device_id] (richest: 27.05\custom_components\jackery_solarvault\services.py)  DOC=13 CODE=9
      (A) doc/comment-only:
        + """Map an HA device-registry id (UUID) to the parent Jackery device id.
        + the matching ``(DOMAIN, jackery_device_id)`` tuple. Subdevices (battery
        + a ``via_device`` link pointing at the parent SolarVault — they share the
        + parent's BLE/MQTT radio per PROTOCOL.md §3 and §4. This resolver walks
        + the ``via_device`` chain so a subdevice pick from the UI ends up at the
        + parent's numeric id, then strips any remaining documented subdevice
      (B) CODE-line mismatch [review]:
        ! packs, smart meter, plugs, meter heads, collectors) are registered with
        ! if device is not None:
        ! seen: set[str] = set()
        ! while device.via_device_id and device.via_device_id not in seen:
        ! parent = registry.async_get(device.via_device_id)
        ! if parent is None:
        ! device = parent
        ! return _strip_jackery_subdevice_suffix(str(identifier))
   [_service_bool] (richest: pre_transfer)  DOC=0 CODE=11
      (B) CODE-line mismatch [review]:
        ! def _service_bool(call: ServiceCall, field: str, label: str) -> bool:
        ! raw = call.data.get(field)
        ! if isinstance(raw, bool):
        ! return raw
        ! if isinstance(raw, str):
        ! value = raw.strip().lower()
        ! if value in {"true", "1", "yes", "on", "enable", "enabled"}:
        ! return True
   [_strip_jackery_subdevice_suffix] (richest: 27.05\custom_components\jackery_solarvault\services.py)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """Return the parent Jackery device id by stripping any subdevice suffix.
        + callers (and legacy main-device ids that already have the parent form)
        + stay backwards-compatible.
      (B) CODE-line mismatch [review]:
        ! Pass-through for inputs that do not start with digits so non-Jackery
   [async_setup_services] (richest: pre_transfer)  DOC=19 CODE=8
      (A) doc/comment-only:
        + """Register the integration's domain-scoped Home Assistant services and their handlers.
        + """  # noqa: E501, RUF100
        + """Dispatches the Home Assistant service call to the integration handler that applies a device's third-par
        + The `call` data must conform to `SET_THIRD_PARTY_MQTT_SCHEMA`.
        + """  # noqa: E501, RUF100
        + """Forward a "query third-party MQTT config" service call to the integration's async handler."""  # noqa:
      (B) CODE-line mismatch [review]:
        ! Registers the following services (if not already present) and wires each to the integration's internal asy
        ! - `flags` (optional): flags for the command (int)
        ! - `wait_for_ack` (optional): whether to wait for an acknowledgement (bool)
        ! if not hass.services.has_service(DOMAIN, SERVICE_SEND_DEVICE_SCHEDULE):
        ! async def _handle_send_device_schedule(call: ServiceCall) -> None:
        ! call (ServiceCall): Service call data containing `device_id` (target device identifier), `action_id` (acti
        ! await _async_handle_send_device_schedule(hass, call)
        ! schema=SEND_DEVICE_SCHEDULE_SCHEMA,

==========================================================================================
custom_components/jackery_solarvault/switch.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (16) --
   [JackeryDescriptionSwitch] (richest: pre_transfer)  DOC=3 CODE=2
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! except Exception as err:  # noqa: BLE001
        ! except Exception as err:  # noqa: BLE001
   [JackerySmartPlugPrioritySwitch] (richest: 1\switch.py)  DOC=5 CODE=1
      (A) doc/comment-only:
        + """Initialise the smart-plug priority switch from its sorted index/serial."""
        + f"smart_plug_{plug_index}_priority_enabled",
        + """Return True when smart-plug priority is enabled."""
        + """Raise a translatable HA action error for this switch."""
        + """Set smart-plug priority through ControlSubDevice."""
      (B) CODE-line mismatch [review]:
        ! plug_sn = _smart_plug_serial(self._plug)
   [JackerySmartPlugSwitch] (richest: 1\switch.py)  DOC=6 CODE=4
      (A) doc/comment-only:
        + """Initialise the smart-plug switch from its sorted index and serial."""
        + """Return True when the smart plug reports an active output."""
        + """Raise a translatable HA action error for this switch."""
        + """Set the smart-plug state through ControlSubDevice."""
        + """Turn the smart plug on."""
        + """Return diagnostic attributes for the smart-plug switch."""
      (B) CODE-line mismatch [review]:
        ! super().__init__(coordinator, device_id, f"smart_plug_{plug_index}_switch")
        ! plug_index, self._plug
        ! plug_sn = _smart_plug_serial(self._plug)
        ! if plug_sn is None:
   [_add_new_entities] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_append_unique] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\switch.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + # APP_POLLING_MQTT.md documents SolarVault advanced controls as app
   [_async_set_state] (richest: 1\switch.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Set the smart-plug state through ControlSubDevice."""
      (B) CODE-line mismatch [review]:
        ! plug_sn = _smart_plug_serial(self._plug)
        ! if plug_sn is None:
   [_cloud_device_id] (richest: 01.06\custom_components\jackery_solarvault\switch.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if raw in (None, ""):
   [_collect_entities] (richest: pre_transfer)  DOC=2 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + @callback
   [_jackery_device_sn] (richest: 01.06\custom_components\jackery_solarvault\switch.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if raw in (None, ""):
   [_plug] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_standby_is_on] (richest: 3.06\switch.py)  DOC=4 CODE=10
      (A) doc/comment-only:
        + """Convert a raw autoStandby payload value into an on/off state.
        + Parameters:
        + raw (Any): Raw payload value from the device; may be None, a number, or another truthy/falsey representati
        + Returns:
      (B) CODE-line mismatch [review]:
        ! def _standby_is_on(raw: Any) -> bool | None:  # noqa: ANN401  # arbitrary payload value, coerced at runtim
        ! `True` if the value represents on (for example, integer `1` or an equivalent truthy representation), `Fals
        ! if raw is None:
        ! return None
        ! try:
        ! return int(raw) == 1
        ! except ValueError:
        ! return safe_bool(raw)
   [async_setup_entry] (richest: 1\switch.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(
   [async_turn_off] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\switch.py)  DOC=4 CODE=0
      (A) doc/comment-only:
        + """Turn the entity off."""
        + # ---------------------------------------------------------------------------
        + # Setup
        + # ---------------------------------------------------------------------------
   [async_turn_on] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
      (B) CODE-line mismatch [review]:
        ! except Exception as err:  # noqa: BLE001
   [extra_state_attributes] (richest: 1\switch.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return diagnostic attributes for the smart-plug switch."""
   [is_on] (richest: 1\switch.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return True when the entity is on."""

==========================================================================================
custom_components/jackery_solarvault/text.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (9) --
   [JackeryGridStandardText] (richest: custom_components\jackery_solarvault\text.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if raw in {None, ""}:
   [JackerySystemNameText] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\text.py)  DOC=3 CODE=0
      (A) doc/comment-only:
        + """Return the entity's current value."""
        + """Forward a text write to the device."""
        + _LOGGER.error("Failed to rename system %s: %s", system_id, err)
   [JackeryThirdPartyMqttText] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! def __init__(  # noqa: PLR0913
        ! except Exception as err:  # noqa: BLE001
   [_add_new_entities] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_append_unique] (richest: pre_transfer)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
   [_collect_entities] (richest: pre_transfer)  DOC=6 CODE=2
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + isinstance(system, dict)
        + and FIELD_GRID_STANDARD in system
        + and coordinator.device_supports_advanced(dev_id)
        + ):
        + @callback
      (B) CODE-line mismatch [review]:
        ! if (
        ! if coordinator.device_supports_third_party_mqtt(dev_id):
   [async_set_value] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\text.py)  DOC=2 CODE=0
      (A) doc/comment-only:
        + """Forward a text write to the device."""
        + _LOGGER.error("Failed to rename system %s: %s", system_id, err)
   [async_setup_entry] (richest: 1\text.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! async def async_setup_entry(
   [native_value] (richest: 1\text.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return the entity's current value."""

==========================================================================================
custom_components/jackery_solarvault/util.py
==========================================================================================
-- SHARED FUNCTIONS WITH SOURCE-ONLY LINES (68) --
   [AppDataQualityWarning] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Return a diagnostics-safe mapping for coordinator payloads."""
      (B) CODE-line mismatch [review]:
        ! if self.reference_chart_series_key is not None:
        ! payload[DATA_QUALITY_KEY_REFERENCE_CHART_SERIES_KEY] = (
        ! self.reference_chart_series_key
   [TrendStatisticPoint] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + start_date: date
   [_add_warning] (richest: pre_transfer)  DOC=25 CODE=16
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + # §2.2: day ≤ week — today's value cannot exceed the current week total.
        + day_in_current_week
        + and week_inside_current_year
        + and day is not None
        + and week + tolerance < day
      (B) CODE-line mismatch [review]:
        ! def _add_warning(  # noqa: PLR0913
        ! day_in_current_week = True  # today is always within the current week
        ! day = _period_total(prefix, DATE_TYPE_DAY, stat_key)
        ! reason=DATA_QUALITY_REASON_WEEK_LESS_THAN_DAY,
        ! source_section=_section(prefix, DATE_TYPE_WEEK),
        ! source_value=week,
        ! reference_section=_section(prefix, DATE_TYPE_DAY),
        ! reference_value=day,
   [_app_period_bound_to_date] (richest: pre_transfer)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! def _app_period_bound_to_date(value: str | date | datetime, *, field_name: str) -> date:
        ! raise ValueError(f"Jackery app period {field_name} must not be empty")  # noqa: TRY003
        ! raise ValueError(  # noqa: TRY003
   [_calculated_savings_from_year] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _calculated_savings_from_year(  # noqa: PLR0914
   [_chart_series_key_for_section] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=0 CODE=3
      (B) CODE-line mismatch [review]:
        ! if isinstance(source, dict):
        ! return trend_series_key(section, stat_key)
        ! return None
   [_chart_series_key_for_stat] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=7
      (A) doc/comment-only:
        + """Return the app chart-series key for one section/stat pair."""
      (B) CODE-line mismatch [review]:
        ! if stat_key == APP_STAT_TOTAL_SOLAR_ENERGY:
        ! if stat_key == APP_STAT_PV1_ENERGY:
        ! if stat_key == APP_STAT_PV2_ENERGY:
        ! if stat_key == APP_STAT_PV3_ENERGY:
        ! return APP_CHART_SERIES_Y3
        ! if stat_key == APP_STAT_PV4_ENERGY:
        ! return APP_CHART_SERIES_Y4
   [_compact_year_parts] (richest: 24.05\24.05\custom_components\jackery_solarvault\util.py)  DOC=9 CODE=14
      (A) doc/comment-only:
        + """Return previous/current-month parts for a candidate Jackery compact year value.
        + See ``implementation notes §8`` ("Device year compact bucket expansion") and
        + into one current-month slot, e.g. ``13.26`` means previous month ``13`` and
        + current month ``26`` (yearly total ``39``). This parser only extracts the
        + *candidate* split. Whether to apply it is decided by
        + ``expanded_year_series_values`` after cross-checking the documented
      (B) CODE-line mismatch [review]:
        ! def _compact_year_parts(value: Any) -> tuple[float, float] | None:
        ! ``implementation notes §1 "Repair roadmap"`` ("Device year compact bucket expansion"): for device
        ! ``dateType=year`` payloads Jackery can encode two adjacent monthly buckets
        ! Not used for week/month payloads or system trend endpoints — those always
        ! if value is None:
        ! if isinstance(value, bool):
        ! text = str(value).strip()
        ! text = text.replace(",", ".")
   [_format_request_range] (richest: 1\1101\custom_components\jackery_solarvault\util.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _format_request_range(request: Any) -> str | None:
   [_is_day_period_payload] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """Return True for app ``dateType=day`` payloads.
        + Day responses carry power curves in W plus documented scalar totals.
        + Only unsuffixed base trend sections may rely on request metadata here;
        + explicit week/month/year section names keep their section contract even
      (B) CODE-line mismatch [review]:
        ! if a malformed payload reports the wrong dateType.
   [_nonzero_months] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! if abs(safe_float(value) or 0.0) > 0.00001  # noqa: PLR2004
   [_parse_day_chart_minute] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=5
      (A) doc/comment-only:
        + """Parse an app day-chart label into minutes after local midnight."""
      (B) CODE-line mismatch [review]:
        ! def _parse_day_chart_minute(value: Any) -> int | None:
        ! match = re.fullmatch(r"\s*(\d{1,2}):(\d{2})\s*", value)
        ! hour = int(match.group(1))
        ! minute = int(match.group(2))
        ! if hour < 0 or hour > 23 or minute < 0 or minute > 59:
   [_parse_iso_date] (richest: 1\1101\custom_components\jackery_solarvault\util.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def _parse_iso_date(value: Any) -> date | None:
   [_payload_debug_passthrough] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=3 CODE=1
      (A) doc/comment-only:
        + """Recursively normalise ``value`` to JSON-serializable types only.
        + Mirrors the shape transforms in :func:`_payload_debug_redacted` (lists,
        + tuple-to-list) but skips redaction. Used in dev mode.
      (B) CODE-line mismatch [review]:
        ! def _payload_debug_passthrough(value: Any) -> Any:
   [_payload_debug_redacted] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=2 CODE=3
      (A) doc/comment-only:
        + """Return a recursively redacted, JSON-serializable debug payload."""
        + else _payload_debug_redacted(item)
      (B) CODE-line mismatch [review]:
        ! def _payload_debug_redacted(value: Any) -> Any:
        ! return [_payload_debug_redacted(item) for item in value]
        ! return [_payload_debug_redacted(item) for item in value]
   [_pv_revenue_value] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! revenue = safe_float(source.get(APP_STAT_TOTAL_SOLAR_REVENUE))
        ! profit = safe_float(source.get(APP_STAT_PV_PROFIT))
   [_record] (richest: pre_transfer)  DOC=4 CODE=1
      (A) doc/comment-only:
        + # Earliest local hour at which a zero day-total is considered suspicious.
        + # Before this, a 0 for "today" is normal (PV makes nothing overnight, little
        + # has charged since local midnight), so the §2.2 §5 zero-confirmation rule must
        + # not raise a repair issue — otherwise it fires a false positive every night.
      (B) CODE-line mismatch [review]:
        ! ZERO_CONFIRM_MIN_LOCAL_HOUR: Final = 20
   [_savings_publish_decision] (richest: pre_transfer)  DOC=1 CODE=1
      (A) doc/comment-only:
        + year_revenue: float | None,
      (B) CODE-line mismatch [review]:
        ! def _savings_publish_decision(  # noqa: PLR0911, PLR0913
   [_scalar_day_energy_points] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! local_tzinfo: TZInfo | None = None,
   [_stat_source_shape] (richest: 24.05\24.05\custom_components\jackery_solarvault\util.py)  DOC=28 CODE=20
      (A) doc/comment-only:
        + PAYLOAD_STATISTIC,
        + PAYLOAD_DEVICE_STATISTIC,
        + PAYLOAD_PRICE,
        + PAYLOAD_PV_TRENDS,
        + PAYLOAD_HOME_TRENDS,
        + PAYLOAD_BATTERY_TRENDS,
      (B) CODE-line mismatch [review]:
        ! stat_source_keys = (
        ! for prefix in (
        ! for date_type in APP_PERIOD_DATE_TYPES
        ! sig: list[Any] = []
        ! for dev_id in sorted(coordinator_data):
        ! payload = coordinator_data.get(dev_id) or {}
        ! plugs = sorted_smart_plugs(payload.get(PAYLOAD_SMART_PLUGS))
        ! plug_keys = tuple(smart_plug_serial(p) for p in plugs)
   [_trend_date_type] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! for suffix in (DATE_TYPE_WEEK, DATE_TYPE_MONTH, DATE_TYPE_YEAR):
   [active_redact_keys] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=2
      (A) doc/comment-only:
        + """Return ``REDACT_KEYS`` or an empty set depending on dev-mode switches.
        + Imported by ``diagnostics.py`` so both redaction surfaces (JSONL log
        + and HA diagnostics download) honour the same toggle. Cast to
        + ``REDACT_KEYS``.
      (B) CODE-line mismatch [review]:
        ! def active_redact_keys(entry: Any | None = None) -> frozenset[str]:
        ! ``frozenset[str]`` for type consistency with the module-level
   [app_data_quality_warnings] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! now: datetime | None = None,
        ! zero_confirm_min_hour: int = ZERO_CONFIRM_MIN_LOCAL_HOUR,
   [app_month_request_kwargs] (richest: pre_transfer)  DOC=0 CODE=2
      (B) CODE-line mismatch [review]:
        ! if month < 1 or month > 12:  # noqa: PLR2004
        ! raise ValueError(f"Unsupported Jackery app month: {month!r}")  # noqa: TRY003
   [app_period_range] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=2 CODE=3
      (A) doc/comment-only:
        + """Return the documented Jackery app begin/end range for a period.
        + and coordinator diagnostics from drifting apart.
      (B) CODE-line mismatch [review]:
        ! APP_POLLING_MQTT.md requires explicit full ranges for period endpoints:
        ! day=today..today, week=Monday..Sunday, month=first..last day,
        ! year=Jan 1..Dec 31. Keeping this in one helper prevents the REST client
   [append_payload_debug_line] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=11
      (A) doc/comment-only:
        + """Append one redacted JSONL diagnostic event and rotate at a small limit."""
        + debug_path.replace(backup)
        + file.write(
        + file.write("\n")
      (B) CODE-line mismatch [review]:
        ! def append_payload_debug_line(path: str | Path, event: dict[str, Any]) -> None:
        ! debug_path = Path(path)
        ! debug_path.parent.mkdir(parents=True, exist_ok=True)
        ! if debug_path.exists() and debug_path.stat().st_size > PAYLOAD_DEBUG_LOG_MAX_BYTES:
        ! backup = debug_path.with_name(debug_path.name + PAYLOAD_DEBUG_LOG_BACKUP_SUFFIX)
        ! with contextlib.suppress(OSError):
        ! backup.unlink()
        ! with contextlib.suppress(OSError):
   [append_unique_entity] (richest: 1\1101\custom_components\jackery_solarvault\util.py)  DOC=2 CODE=0
      (A) doc/comment-only:
        + entity: Any,
        + logger: Any,
   [as_dict] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=3
      (A) doc/comment-only:
        + """Return a diagnostics-safe mapping for coordinator payloads."""
      (B) CODE-line mismatch [review]:
        ! if self.reference_chart_series_key is not None:
        ! payload[DATA_QUALITY_KEY_REFERENCE_CHART_SERIES_KEY] = (
        ! self.reference_chart_series_key
   [backfill_year_payload_from_months] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def backfill_year_payload_from_months(  # noqa: PLR0912
   [calculated_smart_meter_power] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def calculated_smart_meter_power(  # noqa: PLR0911
   [chart_series_debug] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=2
      (A) doc/comment-only:
        + """Return raw/parsed chart-series diagnostics for app payload arrays.
        + This exists to avoid the ambiguous HA UI representation where comma is both
        + list separator and decimal separator, e.g. ``0, 0, 14,84, 0``. Each item is
        + visible without guessing.
      (B) CODE-line mismatch [review]:
        ! def chart_series_debug(source: Any) -> dict[str, Any]:
        ! logged with raw value, Python type and parsed float so parser bugs are
   [compact_json] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Return compact JSON for diagnostic attributes."""
      (B) CODE-line mismatch [review]:
        ! def compact_json(value: Any) -> str:
   [config_entry_bool_option] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=5 CODE=1
      (A) doc/comment-only:
        + """Return a bool option with legacy setup-data fallback.
        + Home Assistant options live in ``entry.options``. Older releases of this
        + custom integration briefly stored these toggles in ``entry.data`` during
        + setup, so every caller must use the same lookup order to avoid platform
        + drift after upgrades.
      (B) CODE-line mismatch [review]:
        ! def config_entry_bool_option(entry: Any, key: str, default: bool) -> bool:
   [config_entry_int_option] (richest: 27.05\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=2
      (A) doc/comment-only:
        + """Return an int option with legacy setup-data fallback.
        + Mirrors :func:`config_entry_bool_option`. Strings are coerced via
        + ``int(...)``; invalid values fall back to ``default`` so a corrupted
        + options dict cannot break entry setup.
      (B) CODE-line mismatch [review]:
        ! def config_entry_int_option(entry: Any, key: str, default: int) -> int:
        ! except (TypeError, ValueError):
   [config_entry_str_option] (richest: 27.05\custom_components\jackery_solarvault\util.py)  DOC=5 CODE=1
      (A) doc/comment-only:
        + """Return a str option with legacy setup-data fallback.
        + Mirrors :func:`config_entry_bool_option`. Used by config-flow code
        + that reads mixed-type options (e.g. the Third-Party MQTT bridge
        + settings from PROTOCOL.md §5) without re-implementing the
        + options-then-data fallback per call site.
      (B) CODE-line mismatch [review]:
        ! def config_entry_str_option(entry: Any, key: str, default: str) -> str:
   [day_power_energy_points] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def day_power_energy_points(  # noqa: PLR0911, PLR0912, PLR0913, PLR0914, PLR0915
   [dev_mode_redactions_disabled] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=7 CODE=3
      (A) doc/comment-only:
        + """Return True when the JACKERY_DEV_MODE env var is truthy.
        + Development-only switch: when set the payload_debug logger and the
        + diagnostics export pass values through without applying ``REDACT_KEYS``
        + so users developing locally can see the raw ``bluetoothKey``,
        + ``mqttPassWord`` and friends. Off by default so production diagnostics
        + stay safe to share. implementation notes §13 ("Privacy / diagnostics") still
      (B) CODE-line mismatch [review]:
        ! applies for anything that gets uploaded — flip this off before
        ! import os
        ! return raw.strip().lower() in {"1", "true", "yes", "on"}
   [diagnostic_redactions_disabled] (richest: 24.05\24.05\custom_components\jackery_solarvault\util.py)  DOC=5 CODE=6
      (A) doc/comment-only:
        + """Return True when diagnostics should bypass sensitive-field redaction.
        + raw ``bluetoothKey`` / ``mqttPassWord`` / serial-number values cannot be
        + leaked by a user toggling a UI checkbox and sharing a diagnostics ZIP.
        + The developer who needs unredacted output sets the env var on the HA
        + previous signature and is intentionally ignored.
      (B) CODE-line mismatch [review]:
        ! def diagnostic_redactions_disabled(_entry: Any | None = None) -> bool:
        ! Gated **only** by the ``JACKERY_DEV_MODE=1`` environment variable so the
        ! process (``JACKERY_DEV_MODE=1`` in ``addon_config`` / ``compose`` /
        ! ``run.sh``) for the brief debugging window and turns it off again.
        ! The ``_entry`` parameter is kept for backward compatibility with the
        ! return dev_mode_redactions_disabled()
   [expanded_year_series_values] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def expanded_year_series_values(  # noqa: PLR0911
   [first_nonblank_int] (richest: pre_transfer)  DOC=5 CODE=6
      (A) doc/comment-only:
        + """Return the first integer-parseable, non-blank token of ``value``.
        + Accepts a scalar or a whitespace/comma-separated string and returns the
        + first token that parses as an integer, or None when the value is
        + None/blank/unparseable. Used by the cloud client to coerce loosely typed
        + id/code fields that may arrive as zero-padded or whitespace-wrapped strings.
      (B) CODE-line mismatch [review]:
        ! def first_nonblank_int(value: object) -> int | None:
        ! return int(value)
        ! for token in text.replace(",", " ").split():
        ! parsed = safe_int(token)
        ! if parsed is not None:
        ! return parsed
   [first_power_value] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=2 CODE=1
      (A) doc/comment-only:
        + """Return the first available numeric power value for the given keys."""
        + continue
      (B) CODE-line mismatch [review]:
        ! if key not in source or source.get(key) is None:
   [format_data_quality_warning] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=2 CODE=5
      (A) doc/comment-only:
        + """Return a compact, deterministic repair/diagnostic warning example."""
        + f"{reference_section}: {reference_request or 'unknown'}]"
      (B) CODE-line mismatch [review]:
        ! text = (
        ! f"{metric}: {source_section}={source_text} "
        ! f"< {reference_section}={reference_text}"
        ! text += (
        ! f" [{source_section}: {source_request or 'unknown'}; "
   [guard_statistic_totals_from_year] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=30 CODE=52
      (A) doc/comment-only:
        + """Guard app total KPIs with corrected current-year period values.
        + The Jackery ``systemStatistic`` endpoint can suffer the same month-only bug
        + as the app year charts. Generation and carbon remain lower-bound guarded by
        + the corrected PV year total. ``totalRevenue`` itself stays the raw app KPI
        + because Jackery often reports PV revenue there. A separate
        + ``_savings_calculation`` payload exposes the real local savings calculated
      (B) CODE-line mismatch [review]:
        ! def guard_statistic_totals_from_year(payload: dict[str, Any]) -> None:
        ! consumption and reduced by CT export if a CT period payload exists.
        ! statistic = payload.get(PAYLOAD_STATISTIC)
        ! pv_year = payload.get(_period_section(APP_SECTION_PV_STAT, DATE_TYPE_YEAR))
        ! if not isinstance(statistic, dict) or not isinstance(pv_year, dict):
        ! return
        ! year_generation = effective_period_total_value(
        ! year_revenue = _pv_revenue_value(pv_year)
   [is_device_year_period_section] (richest: pre_transfer)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """  # noqa: E501, RUF100
        + APP_SECTION_PV_TRENDS,
        + APP_SECTION_HOME_TRENDS,
        + APP_SECTION_BATTERY_TRENDS,
      (B) CODE-line mismatch [review]:
        ! `true` if the section's request dateType is year and the section name starts with a device statistic or tr
   [jackery_grid_side_input_power] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """AC power drawn by Jackery from the grid/home side.
        + Prefer the on-grid fields observed in live diagnostics. The older
        + ``inGridSidePw`` name is kept as a compatibility fallback.
        + props, FIELD_IN_ONGRID_PW, FIELD_GRID_IN_PW, FIELD_IN_GRID_SIDE_PW
      (B) CODE-line mismatch [review]:
        ! return first_power_value(
   [jackery_grid_side_output_power] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """AC power supplied by Jackery to the grid/home side.
        + Prefer the on-grid fields observed in live diagnostics. The older
        + ``outGridSidePw`` name is kept as a compatibility fallback.
        + props, FIELD_OUT_ONGRID_PW, FIELD_GRID_OUT_PW, FIELD_OUT_GRID_SIDE_PW
      (B) CODE-line mismatch [review]:
        ! return first_power_value(
   [jackery_online_state] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=1
      (A) doc/comment-only:
        + """Return a parsed Jackery online/offline marker, or None when unknown."""
      (B) CODE-line mismatch [review]:
        ! def jackery_online_state(value: Any) -> bool | None:
   [jackery_reported_home_load_power] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=5 CODE=0
      (A) doc/comment-only:
        + """Return Jackery's reported live home/other-load power if available.
        + SolarVault 3 Pro Max diagnostics show ``otherLoadPw`` as the app's live
        + home-load value. Prefer this direct app field over reconstructing the load
        + from grid-side fields, because grid-side field availability differs by
        + firmware and message source.
   [normalized_data_quality_warnings] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! warnings: list[dict[str, Any]],
   [parse_utc_datetime] (richest: 24.05\24.05\custom_components\jackery_solarvault\util.py)  DOC=2 CODE=6
      (A) doc/comment-only:
        + """Parse a Jackery timestamp and normalize it to timezone-aware UTC."""
        + elif isinstance(value, int | float) and not isinstance(value, bool):
      (B) CODE-line mismatch [review]:
        ! def parse_utc_datetime(value: Any) -> datetime:
        ! except OverflowError as err:
        ! if not isfinite(timestamp):
        ! raise ValueError(f"invalid UTC timestamp: {value!r}")
        ! if not isfinite(timestamp):
        ! raise ValueError
   [redacted_json_safe_payload] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=1
      (A) doc/comment-only:
        + """Return a JSON-safe payload with sensitive Jackery fields redacted.
        + This is stricter than diagnostics export redaction: HA entity attributes
        + can be written to the state machine and recorder, so they stay redacted
        + even when the developer-only unredacted diagnostics option is enabled.
      (B) CODE-line mismatch [review]:
        ! def redacted_json_safe_payload(value: Any) -> Any:
   [safe_bool] (richest: 24.05\24.05\custom_components\jackery_solarvault\util.py)  DOC=3 CODE=3
      (A) doc/comment-only:
        + """Convert a Jackery payload value to bool, returning None when unknown.
        + Accepts bool, int, float, and common string truthy markers. Mirrors the
        + logic that has been duplicated across switch.py and binary_sensor.py.
      (B) CODE-line mismatch [review]:
        ! def safe_bool(value: Any) -> bool | None:
        ! except ValueError, OverflowError:
        ! except TypeError, ValueError, OverflowError:
   [safe_float] (richest: 24.05\24.05\custom_components\jackery_solarvault\util.py)  DOC=6 CODE=8
      (A) doc/comment-only:
        + """Convert a Jackery payload value to float, returning None on error.
        + The Jackery cloud normally returns numeric JSON numbers or dot-decimal
        + strings. Some app-facing payloads/exports can use a locale decimal comma
        + (``"40,96"``). Treat that as ``40.96`` and never strip the comma away;
        + removing it would turn 40.96 kWh into 4096 kWh. Thousands separators are not
        + accepted because the Jackery app protocol docs do not define them.
      (B) CODE-line mismatch [review]:
        ! def safe_float(value: Any) -> float | None:
        ! if isinstance(value, bool):
        ! parsed = float(candidate)
        ! except ValueError, OverflowError:
        ! return parsed if isfinite(parsed) else None
        ! parsed = float(value)
        ! except TypeError, ValueError, OverflowError:
        ! return parsed if isfinite(parsed) else None
   [safe_int] (richest: pre_transfer)  DOC=0 CODE=13
      (B) CODE-line mismatch [review]:
        ! def safe_int(value: Any) -> int | None:  # arbitrary payload value, coerced at runtime  # noqa: ANN401, PL
        ! if value is None or isinstance(value, bool):
        ! if isinstance(value, int):
        ! return value
        ! if isinstance(value, float):
        ! return int(value) if value.is_integer() else None
        ! if isinstance(value, str):
        ! candidate = value.strip()
   [signed_phase_power_values] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return signed CT phase powers; positive=grid import, negative=export."""
   [smart_meter_net_power] (richest: pre_transfer)  DOC=7 CODE=6
      (A) doc/comment-only:
        + _LOGGER.debug(
        + "CT meter per-phase sum (%.1f W) deviates from tPhasePw "
        + "(%.1f W) by %.1f W (%.1f%%). Shelly 3EM ADC sampling "
        + "jitter; using tPhasePw as authoritative.",
        + phase_sum,
        + total,
      (B) CODE-line mismatch [review]:
        ! if phases is not None:
        ! phase_sum = sum(phases)
        ! drift = abs(phase_sum - total)
        ! threshold = max(50.0, abs(total) * 0.05)
        ! if drift > threshold:
        ! 100 * drift / abs(total) if total else 0,
   [smart_plug_serial] (richest: 21.05\21.05\custom_components\jackery_solarvault\util.py)  DOC=6 CODE=2
      (A) doc/comment-only:
        + """Return the deviceSn for a smart-plug subdevice payload, or None.
        + Smart plugs arrive in an unordered list. The serial is the only stable
        + cross-payload identifier; sorting plugs by this value gives every plug a
        + deterministic position that survives cloud-side re-ordering, so the
        + positional ``smart_plug_<N>_*`` unique-ID suffix stays stable without
        + placing a serial in the unique ID itself (implementation notes §0 rule 7).
      (B) CODE-line mismatch [review]:
        ! def smart_plug_serial(plug: Any) -> str | None:
        ! raw = plug.get(FIELD_DEVICE_SN) or plug.get(FIELD_DEV_SN) or plug.get(FIELD_SN)
   [sorted_meter_heads] (richest: custom_components\jackery_solarvault\util.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! entries.sort(key=operator.itemgetter(0))
   [sorted_smart_plugs] (richest: 1\1101\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=2
      (A) doc/comment-only:
        + """Return plug entries sorted by their serial numbers.
      (B) CODE-line mismatch [review]:
        ! def sorted_smart_plugs(plugs: Any) -> list[dict[str, Any]]:
        ! list[dict[str, Any]]: The input entries that contain a serial (as determined by `smart_plug_serial`), sort
   [statistic_id_part] (richest: 1\1101\custom_components\jackery_solarvault\util.py)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def statistic_id_part(value: Any) -> str:
   [subdevice_branding] (richest: 27.05\custom_components\jackery_solarvault\util.py)  DOC=13 CODE=1
      (A) doc/comment-only:
        + """Look up DeviceInfo branding for a subdevice ``scanName``.
        + Returns ``(manufacturer, model_label)``. Both are ``None`` when the
        + ``scan_name`` is empty or not in the documented accessory catalog
        + (``SUBDEVICE_SCAN_NAME_*`` in const.py — sourced from
        + ``docs/html/jackery_smali_home_assistant_report.html``). Callers must
        + fall back to a sensible default themselves: this helper deliberately
      (B) CODE-line mismatch [review]:
        ! def subdevice_branding(scan_name: Any) -> tuple[str | None, str | None]:
   [task_plan_value] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=5 CODE=2
      (A) doc/comment-only:
        + """Read a value from the task-plan shapes documented in MQTT_PROTOCOL.md.
        + App/MQTT task-plan responses can expose values directly, inside ``body``
        + or as items in ``tasks``. Platforms should use this helper instead of
        + duplicating shape-specific fallbacks.
        + continue
      (B) CODE-line mismatch [review]:
        ! def task_plan_value(task_plan: dict[str, Any], *keys: str) -> Any:
        ! if not isinstance(item, dict):
   [trend_series_has_value] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def trend_series_has_value(  # noqa: PLR0911
   [trend_series_key] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=7 CODE=35
      (A) doc/comment-only:
        + """Return the chart series key for app week/month/year trend payloads.
        + Jackery's period endpoints expose the app values as chart arrays:
        + - PV/home period payloads use ``y``
        + - battery charge uses ``y1``
        + - battery discharge uses ``y2``
        + All week/month/year sensors must use this same chart-series key selection.
      (B) CODE-line mismatch [review]:
        ! Device dateType=year payloads additionally expand compact monthly buckets
        ! (for example raw May value 13.26 -> April 13, May 26) before totals or
        ! if section.startswith((APP_SECTION_PV_TRENDS, APP_SECTION_HOME_TRENDS)):
        ! return APP_CHART_SERIES_Y
        ! if section.startswith(APP_SECTION_PV_STAT):
        ! if stat_key == APP_STAT_TOTAL_SOLAR_ENERGY:
        ! return APP_CHART_SERIES_Y
        ! if stat_key == APP_STAT_PV1_ENERGY:
   [trend_series_points] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def trend_series_points(  # noqa: PLR0912
   [trend_series_total] (richest: pre_transfer)  DOC=0 CODE=1
      (B) CODE-line mismatch [review]:
        ! def trend_series_total(  # noqa: PLR0911
   [utc_now] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=1 CODE=0
      (A) doc/comment-only:
        + """Return the current UTC time as a timezone-aware datetime."""
   [validate_app_period_date_type] (richest: 1\jackery_solarvault-0.1.0\custom_components\jackery_solarvault\util.py)  DOC=4 CODE=0
      (A) doc/comment-only:
        + """Return a supported Jackery app period type or raise ValueError.
        + Silent fallback to ``day`` would make malformed callers issue a plausible
        + but wrong cloud request. Keep invalid values loud so request-contract bugs
        + are caught during development instead of producing day-like statistics.
