# Runtime dependencies for jackery_solarvault Home Assistant custom integration.
#
# NOTE: Home Assistant loads the actual runtime requirements from
# custom_components/jackery_solarvault/manifest.json (`requirements` array).
# This file exists as a convenience mirror for local development environments
# that install with `pip install -r requirements.txt`. Keep it in sync with
# the manifest and requirements-test.txt; the manifest remains the source of truth.

aiomqtt==2.5.1
bleak-retry-connector>=4.6.1
cryptography>=48.0.0
